# Feature Backlog Working Queue

Status date: 2026-06-09

This queue reconciles the official 36-row tracker in `CURRENT_STATUS.md` /
`MISSING_FEATURE_MATRIX.md` with the code-first split in
`FEATURE_IMPLEMENTATION_AUDIT.md` when that local audit is present. It is a
working queue, not a retail-parity claim. Each slice must still finish as one of
`Implemented`, `Mapped only`, `Rejected`, or `Blocked`.

## Source Ledgers

| Ledger | Role |
| --- | --- |
| `CURRENT_STATUS.md` | Official 36-row implementation snapshot and consolidated producer gaps. |
| `Decomp/Analysis/MISSING_FEATURE_MATRIX.md` | Feature-row ownership, missing surfaces, decode targets, and next tasks. |
| `Decomp/Analysis/GAMEPLAY_ECONOMY_SOCIAL_STATUS.md` | Gameplay/economy/social details, including rows that are implementation-complete but retail-partial. |
| `Decomp/Analysis/MATCHING_IMPLEMENTATION_STATUS.md` | Group, raid, matching, queue, and replacement state. |
| `Decomp/Analysis/ENTITY_AUX_DECODE_ROADMAP.md` | Shape-mapped aux packet inventory and producer blockers. |
| `Decomp/Analysis/BLOCKER_EVIDENCE_PLAN.md` | Live-client and server-log evidence passes. |
| `FEATURE_IMPLEMENTATION_AUDIT.md` | Local code-first split of the same backlog into 43 concrete implementation surfaces. |

## Queue Rules

- Prefer small implementation-ready slices before broad discovery.
- Do not emit shape-only packets or rename neutral fields without producer,
  consumer, or live-payload proof.
- Keep client diagnostics and unknown fields non-mutating until an
  opcode-specific owner is proven.
- Runtime code must not query `wildstar_client`, `jabbithole`, or `nf_map_*`.
- Update the official row tracker and focused tracker after each closed slice.

## Implementation-Ready Candidates

These rows have real code paths and likely next work is source-local hardening,
focused tests, tracker cleanup, or live-smoke validation before a parity claim.
Each still needs a slice-specific evidence check before editing behavior.

| Priority | Official row | Code-audit split | Candidate slice | Current state / gate |
| ---: | --- | --- | --- | --- |
| 1 | F-005/F-013 | Audit F-005/F-006/F-018 | Marketplace, CREDD, and mail settlement edge tests | Core behavior exists; source-local offline settlement-credit atomicity and auction selector-table crash guards are closed. Remaining work is live migration/order-book validation, retail filters/precision, and aux producer evidence. |
| 2 | F-027/F-028 | Audit F-034/F-035 | Options/keybinds and support/stuck readback gaps | Core persistence and parsing exist; extra option types and support case lifecycle need proof or explicit product design before widening. |
| 3 | F-014/F-026 | Audit F-019/F-033 | Loot, salvage, item/unlock/pet/costume precision | Core item and loot systems exist; source-local generated-loot reward-table and creature DropLoot static-data crash guards are closed. Remaining gaps are producer timing, eligibility precision, and live capture of parent/source edges. |
| 4 | F-032..F-036 | Audit F-039..F-043 | Leaderboards, challenges, archive, achievements, zone completion edge coverage | Core managers exist; remaining work is focused category/filter/reward/trigger parity rather than foundational implementation. |
| 5 | F-029 | Audit F-036 | DataMapping/setup/load-test tracker validation | Tooling exists; content parity remains case-by-case and should close only after reviewed data or smoke proof. |

## Needs Decompile Or Protocol Evidence

| Official row | Code-audit split | Blocking question |
| --- | --- | --- |
| F-001 | Audit F-001 | STS token crypto, optional auth routes, optional envelope fields, and optional-route state ordering need safe crypto/token semantics. |
| F-002 | Audit F-002 | `Client0x00C8`, `00ED`, `011B`, `011D`, `012D`, `0550`, `062A`, `0634`, `063E`, `0701`, `07E3`, `0928`, and structural realm/addon rows need opcode-specific native senders or consumers. |
| F-003 | Audit F-003/F-024 | `Server0x0015`, spell aux wrappers, and the 50 blocked aux emitters need producer/apply path or live payload proof. |
| F-004 | Audit F-004 | Housing neighborhood/community field names, `0x0501`/`0x0506` producer, edit-mode ack/broadcast, and visit/session precision. |
| F-007 | Audit F-009 | Reward rotation `0x07CD` apply/flag/throttle semantics and claim delivery catalog linkage. |
| F-008 | Audit F-010/F-011 | Crafting discovery, current-craft cadence, `0x084B`/`0x0855`, `0x056C`, service-key names, and non-success sigil rules. |
| F-009 | Audit F-012/F-013 | Taxi embark/completion, transport service-token bypass, global route state, vehicle/passenger producer semantics. |
| F-010 | Audit F-014/F-015 | Replacement backfill/merge, non-zero raid queue status, `0x05CF`, role-selection tails, and `Client0x062A/0634`. |
| F-011 | Audit F-016 | Recruitment persistence/updates, guild bank economy/state, boss-token inventory/cast acceptance/results, and warplot plug state. |
| F-012 | Audit F-017 | Chat aux `0x01B8/0x01C1/0x01C4/0x01EF`, ICComm entitlement/persistence, and directed-vs-ordered delivery precision. |
| F-016..F-021 | Audit F-021..F-025 | Spell cast result leading field, spell triplets/four-uint packets, proc tails, immunity and wrapper lifecycle, LAS async update transaction. |
| F-025 | Audit F-028/F-031 | Entity-stat aux, map-tracked-unit producer timing, compact visual flags, deferred action/current-target semantics. |
| F-026/F-030 | Audit F-033/F-037 | Item/options aux, generic unlock deltas, pet lifecycle timing, realm-transfer/PTR queue and handoff semantics. |

## Needs Live-Client Or Content Smoke

| Official row | Code-audit split | Evidence pass |
| --- | --- | --- |
| F-004 | Audit F-004 | Housing UI, neighborhood cache, edit mode, neighbor/community, wallpaper/decor negative cases. |
| F-008 | Audit F-010/F-011 | Crafting station, discovery/hot-cold, current-craft, rune/microchip, and non-success sigil captures. |
| F-009 | Audit F-012/F-013 | Rapid transport nodes, taxi/flight purchases, vehicle embark/disembark, route rejection branches. |
| F-010/F-011/F-012 | Audit F-014..F-017 | Group finder, guild bank/recruitment, ICComm/chat/friendship two-client flows. |
| F-015/F-016..F-020 | Audit F-020..F-023 | Duel/PvP observer/reward, spell fixture/proc/loot/reward captures. |
| F-022/F-023/F-024 | Audit F-026..F-030 | Quest/path/public event, NPE/Rider's Reef, Evil from the Ether, expeditions, dungeons, raids, PvP adventure, and event-instance playthroughs. |
| F-031 | Audit F-038 | Fortune rewards, active rotation, card deal/flip/payout/reload, invalid flip, and insufficient-currency cases. |
| F-032..F-036 | Audit F-039..F-043 | Leaderboard score ingestion, challenge rewards, archive/datacube pickups, achievement triggers, zone-completion rewards. |

## Rejected Or Guarded Claims

| Claim | Current disposition |
| --- | --- |
| Production TODO/FIXME search proves unfinished source | Rejected after scoped audit; production TODO/FIXME/NotImplemented markers are not the active backlog. |
| Entity-stat aux emitted from `Player.AddVisible()` | Rejected; only entity-create aux has a proven emitter. |
| Housing neighborhood emits from residence `AddEntity` | Rejected; packet models exist without a proven send site. |
| Crafting `0x084B`/`0x0855` emit before success | Rejected as a current-code claim; models are wire-mapped only. |
| `ClientItemContextAction` delegates to `ClientItemUse` | Rejected; selected branch remains diagnostic-only. |
| Reward rotation schedules prove Fortune active rotation | Rejected; Fortune weights need `ServerFortuneRewards` or storefront catalog proof. |
| Dungeon Chase hidden SMC item `86919` is seedable now | Rejected for current schema/transport. |
| Matching `0x05CF` apply owner from `.pdata` xref | Rejected; needs real dispatch index or live payload. |

## Hard-Blocked Until External Evidence

| Official row | Blocker |
| --- | --- |
| F-001 | Safe STS crypto/token semantics: exact RSA key format, signature/trust anchor, premaster derivation, `TokenKeyData` reply fields, post-token session/crypto transition, and startup route ordering remain unproven. |
| F-002/F-003 | Native sender/apply/producer proof for diagnostics and shape-only output packets. |
| F-004 | Live housing UI/realm-login sniff or native `0x0506` server-push path. |
| F-008 | Native current-craft producer or live crafting capture for aux timing/discovery. |
| F-025 | Entity-stat aux producer/apply handler or sniff order. |
| F-031 | Retail `ServerFortuneRewards` capture or storefront-server catalog dump. |

## Closure Checkpoint - 2026-06-09

This checkpoint covers the current decompile/protocol-gated queue only. It does
not re-enable live-client smoke, content validation, runtime setup, broad
implementation, or data-mapping validation.

| Row / slice | Current closure state | Exact next evidence source |
| --- | --- | --- |
| F-001 STS token/RSA optional auth | Mapped only / Blocked | Native crypto proof or accepted startup STS captures proving RSA key format, signature trust, premaster/auth-token derivation, reply fields, crypto transition, and route ordering. |
| F-002 diagnostic client rows | Mapped only / Diagnostic-only | Opcode-specific native sender/consumer, callback/table owner, indirect send rail, or accepted payload capture for each remaining `Client0xNNNN` row. |
| F-003 `Server0x0015` | Mapped only / Blocked | Native `0x0015` apply/producer path, client post-read consumer, or accepted `0x0015` payload proving semantic field names and emit timing. |
| F-003 spell/cluster aux wrappers | Mapped only / Blocked | Per-opcode producer/apply path or dynamic dispatch proof before neutral wrapper fields are renamed or emitted. |
| F-004 housing neighborhood/community aux | Mapped only / Blocked | Native server-push path or accepted housing UI/realm-login packet capture proving trigger, row backing, field population, and realm/session timing. |
| F-005 marketplace aux `0x06DF` / `0x07D5` | Mapped only / Blocked | Native marketplace apply/producer path, server producer witness, or accepted marketplace packet capture proving field semantics and emit timing. |
| F-007 reward rotation `0x07CD` | Mapped only / Blocked | Retail `0x07CD` payload, dynamic apply-dispatch breakpoint, or accepted reward-rotation capture proving `Flag`, throttle-slot assignment, and content selection. |
| F-008 crafting current-craft / aux / microchips | Mapped only / Blocked | Native producer timing or accepted crafting/item-replication capture for current-craft cadence, discovery/hot-cold behavior, non-success sigil rules, and `0x056C` microchip patch timing. |
| F-009 transport service-token / taxi / vehicle | Partial / Mapped-only blockers | Native transport service-token branch, global route-state producer, taxi embark/completion proof, or accepted rapid/taxi/vehicle capture proving cost, route-state, teleport, passenger/seat, and deployable vehicle timing. |
| F-010 matching, raid queue, and replacement | Mapped only / Blocked where not implemented | Real matching dispatch/apply index or accepted capture for `0x05CF`, `0x0600`, standalone non-zero `0x0718`, `Client0x062A/0634`, replacement anchor timing, and replacement merge. |
| F-011 guild recruitment packet family | Mapped only / Blocked | Native/server recruitment producer, persistent recruitment store, callback/table owner, or accepted recruitment UI capture proving list/detail population, subscribe/update timing, and availability semantics. |
| F-011 war-party boss-token packet boundary | Implemented packet-boundary / Blocked state | Native/server boss-token inventory owner, persistent backing state, accepted `0x094F` cast/result capture, match-results producer proof, or warplot plug evidence before accepting boss-token casts, populating token rows, or mutating warplot state. |
| F-012 guild/social/ICComm/chat aux | Partial / Mapped-only blockers | Native producer/callback proof or accepted two-client capture for chat aux emit timing, ICComm entitlement/persistence, directed-vs-ordered delivery, guild bank economy, recruitment, and guild-bank tab/money state. |
| F-016..F-021 spell/proc/wrapper/LAS gaps | Partial / Mapped-only blockers | One concrete native spell-effect consumer, wrapper producer/apply path, proc-tail proof, immunity/effect evidence, or action-set update transaction model before mutating runtime semantics. |
| F-025 entity-stat aux | Mapped only / Blocked | Per-opcode `WorldSocket+0x15b0` `vtable+0x58` apply handler, apply-table classification, native producer, or accepted sniff/order witness. |
| F-025 map tracked units | Mapped only / Blocked | Native server send site or accepted public-event marker capture proving tracked-unit id allocation, update cadence, disable lifetime, and `TrackingSlotId` selection. |
| F-026/F-030 item/unlock/costume/pet/realm-transfer/PTR | Partial / Mapped-only blockers | Native aux producer/send site, unlock-list delta proof, pet lifecycle producer timing, realm-transfer handoff/success proof, or PTR queue/copy producer for the specific row being widened. |
| F-031 Fortune weights/active rotation | Mapped only / Blocked | Retail `ServerFortuneRewards` capture, storefront-server catalog dump, or native/server producer artifact proving item ids, probabilities, and active rotation. |

## Next Slice

Start with an implementation-ready candidate whose evidence can be checked in
source and tests without live-client dependencies. If no safe behavior change is
available, close that slice as `Mapped only` or `Blocked` and move to the next
candidate rather than broadening behavior from adjacency.

## Slice Log

| Date | Slice | Result | Evidence | Next action |
| --- | --- | --- | --- | --- |
| 2026-06-09 | F-011 war-party boss-token packet boundary (`0x094F`, `0x0951`, `0x0956`) cached-export/source recheck | Implemented / packet-boundary; state blocked | Existing cached `WildStar64.exe` fragments and selected exports were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) registers `ClientWarPartyBossTokensRequest` (`0x0956`) size `0x10` at `selected_decompiled.c:13334`, `ClientCastGuildBossToken` (`0x094F`) size `0x18` at `selected_decompiled.c:13336`, and `ServerWarPartyBossTokens` (`0x0951`) size `0x20` at `selected_decompiled.c:13363`. `FUN_14057ef20` sends `0x0956` when type-3 boss-token state is absent; `140092990` reads guild identity, row count, and token rows as 18-bit token item id plus `uint32` count; `GuildBossToken_SendClientCastGuildBossToken` (`1403991b0`) sends `0x094F` after type-3 token lookup and spell target validation, with writer `140091a80` packing guild identity, an 18-bit token item id, and a 32-bit context token. Source now reads `ClientCastGuildBossToken.Item2Id` at the mapped 18-bit width, with focused protocol coverage. | Keep token-list responses empty and boss-token casts rejected through `BossTokenNotReady` until native/server boss-token inventory ownership, persistent backing state, accepted cast/result capture, match-results producer proof, or warplot plug evidence proves state mutation and emit timing. |
| 2026-06-09 | F-011 guild recruitment packet family (`0x049F`, `0x0767..0x076F`) cached-export/source recheck | Mapped only / source-aligned | Existing cached `WildStar64.exe` fragments and selected exports were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) registers `ClientRecruitmentGuildGetDetailedGuildInfo` (`0x076E`) size `0x10` at `selected_decompiled.c:13324` with the shared identity shape; `RecruitmentGuild_SendClientGetDetailedGuildInfo` (`140584840`) is called by `Lua_GameRecruitmentGuild_GetDetailedGuildInfo` (`14069e5c0`) and sends `0x076E` through `Network_SendOpcodePayloadOrPackedHelper` (`1403f4740`) using current realm id plus selected guild identity. Adjacent registrations at `selected_decompiled.c:13369-13377` bind `0x049F` and `0x0767..0x076D` to reader fragments `140092c10`, `140092d40`, `140092cd0`, `140091ef0`, `140092f80`, `140092ec0`, `140086e70`, and `140092e00`, matching current identity, demands, description, detail, list/update, minimum-level, and recruiter row packet models/tests. Source handlers for `0x076E` and `0x076F` remain log-only. | Keep recruitment runtime non-mutating until native/server producer evidence, persistent recruitment state, callback/table owner, or accepted UI capture proves list/detail population, subscribe/update timing, and availability semantics. |
| 2026-06-09 | F-009 `ClientSpellCastWithServiceToken` (`0x00C2`) cached-export/source recheck | Mapped only / source-aligned | Existing cached `WildStar64.exe` fragments and selected exports were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) registers `0x00C2` size `8` at `selected_decompiled.c:13077` to `ClientSpellCastWithServiceToken_WritePayload` (`140089570`). The cached writer consumes an 18-bit context token and 32-bit `Spell4` id; `ServiceToken_HandleCastResult` (`140520c10`) gates on `Spell4.PropertyFlags & 0x20000000`; `ServiceToken_SendClientSpellCastWithServiceToken` (`1403994f0`) checks service-token balance, returns `0x014B` for insufficient funds, or sends `0x00C2` through `Network_SendOpcodePayloadHelper`. Current source already mirrors the shape and cost gate through `ClientSpellCastWithServiceTokenHandler`, `SpellParameters.UseServiceTokenCost`, and `Spell.TryConsumeServiceTokenCost`. | Keep broad transport service-token/global-route bypass blocked until a native/live transport branch or accepted capture proves `0x00C2` is used for transport bypass plus route-state, cost, and teleport timing. |
| 2026-06-09 | F-004 housing community plot reservation `ServerHousingCommunityPlotReservation` (`0x051F`) cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments and selected exports were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) registers `0x051F` size `0x18` at `selected_decompiled.c:13683` to `ServerHousingCommunityPlotReservation_ReadPayload` (`140086e70`). The cached reader consumes target residence identity (14-bit realm plus 64-bit residence id) and one `uint32` plot index; selected xrefs are label-only and selected call edges stay reader-local, aside from `ServerLootWinner_ReadPayload` row-helper reuse. `Lua_HousingLib_GetReservedCommunityPlotIndex` (`140737470`) reads the type-7 community cache through `Housing_FindCommunityResidenceEntryByIdentity` (`14057ff90`) and exposes `nPlotIndex` / `bHasReservation` only when the stored value is not `0xffffffff`. Client positive controls are `Housing_SendClientCommunityPlotReservation` (`14057fe90`) sending `ClientGuildOperation` (`0x04B1`) operation `0x29` with the requested plot index and `Housing_SendClientCommunityPlotReservationRemoval` (`14057ff10`) sending operation `0x2A` with the target name. Current source emits the server packet from `CommunityOperations.SendCommunityPlotReservation`, with packet tests pinning the `uint.MaxValue` no-reservation sentinel. | Keep current source behavior narrow and do not widen plot-reservation permission, placement, teleport/unload, persistence, or broadcast semantics until a native apply/consumer owner, callback/table owner, accepted community-reservation capture, or backing-store evidence proves timing and scope. |
| 2026-06-09 | F-004 housing community donate `ServerHousingCommunityDonateUpdate` (`0x04FE`) cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments and selected exports were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) registers `0x04FE` size `0x18` at `selected_decompiled.c:13681` to `ServerHousingCommunityDonateUpdate_ReadPayload` (`14009e930`). The cached reader consumes one `uint32` count and two allocated parallel `uint32` arrays; selected xrefs for `14009e930` are label-only and selected call edges stay reader-local through bit-read, allocation, and raw-copy helpers. The separate client positive control registers `0x04F5` at `selected_decompiled.c:13648` to `ClientHousingCommunityDonate_WritePayload` (`14009da00`), and `Housing_SendClientCommunityDonate` (`1404b9ca0`) sends one selected `DecorInfo` row after resolving `HousingDecorInfo` and rejecting rows with `Flags & 0x8`. Current source emits `ServerHousingCommunityDonateUpdate` from `ClientHousingCommunityDonateHandler` after copying donated crate decor and deleting the source decor, while `ServerHousingCommunityDonateUpdate.cs` and `HousingPacketShapeTests.cs` keep `Value0`/`Value1` neutral. | Keep `ServerHousingCommunityDonateUpdate.Entry.Value0`/`Value1` neutral and do not widen community donation resource, contribution, transfer, ownership, or unlock semantics until a native apply/consumer owner, callback/table owner, accepted donate capture, or backing-store evidence proves field meanings and timing. |
| 2026-06-09 | F-026 supply-satchel aux `ServerSupplySatchelAux` (`0x019A`) cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments and selected exports were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) registers `0x019A` size `8` at `selected_decompiled.c:12829` to shared `MatchingQueueResultWaitTime_ReadPayload` (`14007fcf0`). The cached reader consumes one 6-bit field and one `uint32`; selected xrefs for `14007fcf0` are label-only and selected call edges stay reader-local through `FUN_14006c090`. The same reader is reused by `ServerCharacterDeleteResult` (`0x00E6`), `ServerMatchingMatchKickCooldownUpdate` (`0x0616`), and `ServerMatchingMatchOperationResult` (`0x0623`); managed emitters for those packets are separate positive controls and do not prove a `0x019A` producer. Current source emits regular `ServerSupplySatchelUpdate` (`0x0199`) from `SupplySatchelManager.SendSupplySatchelStackUpdate` and finds `ServerSupplySatchelAux` only in `GameMessageOpcode.cs`, `ServerClusterAuxPackets.cs`, and `PacketPlaceholderNamingTests.cs`. | Keep `ServerSupplySatchelAux.UInt6Value` and `Value` neutral and non-emitted until a native server producer/send site, post-read apply/consumer owner, callback/table owner, dynamic dispatch proof, accepted supply-satchel packet capture, or broader item/supply readback evidence proves semantics and timing; do not infer supply semantics from character-delete or matching positive controls. |
| 2026-06-09 | F-026 costume aux `ServerCostumeItemAux` (`0x037F`) cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments and selected exports were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) registers `0x037F` size `0x18` at `selected_decompiled.c:13026` to `ServerCostumeItemAux_ReadPayload` (`1400874a0`). The cached reader consumes one 14-bit value, three `uint32` fields, and two trailing 1-bit flags; selected xrefs for `1400874a0` are label-only and selected call edges stay reader-local through `FUN_14006c090`. Adjacent `ClientEmote` (`0x037E`) has separate writer `140087270`, and costume unlock/forget/save positive controls dispatch named result events without proving a `0x037F` producer. Current source finds `ServerCostumeItemAux` only in `GameMessageOpcode.cs`, `ServerClusterAuxPackets.cs`, and `PacketPlaceholderNamingTests.cs`. | Keep `ServerCostumeItemAux.UInt14Value`, `Value0..Value2`, and `Flag0`/`Flag1` neutral and non-emitted until a native server producer/send site, post-read apply/consumer owner, callback/table owner, dynamic dispatch proof, or accepted costume/emote capture proves semantics and timing. |
| 2026-06-09 | F-016 spell wrapper follow-up `0x0818` / `0x0819` cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments and selected exports were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) registers `0x0819` size `8` at `selected_decompiled.c:13429` to shared `ServerTwoUInt32_ReadPayload` (`14007a040`) and `0x0818` size `0x20` at `selected_decompiled.c:13430` to `ServerOpcode0818_ReadSpellWrapperIdAndTierEntry` (`140095e60`). The `0x0818` reader consumes a leading `SpellWrapperId` then `ServerSpellList_ReadTierEntry` at `+0x8`; durable labels map dispatcher `1403ee403` through `SpellService_LookupSpellWrapperByWrapperId` (`140561c30`) into `SpellWrapper_ApplyEntityVariantTierEntryAndBroadcast` (`14053f710`) and map dispatcher `1403ee3af` into `SpellWrapperNode_PruneChildrenAndRefresh` (`140718af0`) with field zero as wrapper-node entity id and field one as spell-wrapper id. Selected xrefs are label-only or reader/apply-local, and current source finds the two packets only in `GameMessageOpcode.cs`, packet models, `SpellBroadcastPacketShapeTests.cs`, and negative tutorial guards. | Keep `ServerSpellWrapperTierEntry` and `ServerSpellWrapperNodeRemove` non-emitted until a wrapper lifecycle capture, native producer timing proof, callback/table owner, or selector-tail semantic correlation proves when the server should send them. |
| 2026-06-09 | F-026/F-003 `ServerItemContextActionAck` (`0x00B7`) cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments and selected exports were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) registers `0x00B7` size `1` at `selected_decompiled.c:13226` to shared `ServerEmpty_ReadPayload` (`14007d8e0`); the cached reader returns success without bit or field reads when a payload pointer is present. Exact selected scans found `0x00B7` only at that registration row, selected xrefs for `14007d8e0` are label-only, selected call edges show no opcode-specific producer or post-read consumer, and current source finds `ServerItemContextActionAck` only in `GameMessageOpcode.cs`, `ServerClusterAuxPackets.cs`, and packet-placeholder tests. Positive controls stayed separate: `ItemUse_SendClientItemUse` (`140398cc0`) sends client opcode `0x0943`, `Friendship_SendClientFriendshipBlock` (`1405e39e0`) sends `0x00B8`, and `Friendship_SetAutoResponseMessagesAndSend` (`1405de2e0`) / `ClientFriendshipSetAutoResponseMessage_WritePayload` (`14007e4b0`) sends `0x03B7`. | Keep `ServerItemContextActionAck` zero-field, neutral, and non-emitted until a native server producer/send site, post-read apply/consumer owner, callback/table owner, dynamic dispatch proof, or accepted item-context/friendship payload proves intent and timing; do not infer semantics from item-use or friendship positive controls. |
| 2026-06-09 | F-004 housing basics follow-up `0x010D` / `0x0110` cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments and selected exports were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) registers `ServerHousingBasicsEmpty` (`0x010D`) size `1` at `selected_decompiled.c:13225` to shared `ServerEmpty_ReadPayload` (`14007d8e0`) and `ServerHousingBasicsFollowup` (`0x0110`) size `0x10` at `selected_decompiled.c:13216` to `ServerHousingBasicsFollowup_ReadPayload` (`14008de70`). The `0x0110` reader consumes `uint32`, 18-bit scalar, `uint32`, and 8-bit scalar. The adjacent `ServerHousingBasics` (`0x010E`) positive control uses `ServerHousingBasics_ReadPayload` (`14008e610`) and client apply writer `1403cd6d0` dispatching `HousingBasicsUpdated` / `HousingPrivacyUpdated`, but selected xrefs for `14008de70` are label-only and selected call edges are reader-local. Current source emits a controlled seven-packet residence-session compatibility bundle through `ResidenceManager.SendHousingBasics` -> `HousingAuxiliaryPacketEmitter`, with `0x0110` fields neutral/defaulted and packet-shape tests only. | Keep `ServerHousingBasicsFollowup.Value0..Value3` neutral/defaulted and do not widen housing-basics follow-up semantics until a native follow-up producer/apply owner, callback/table owner, dynamic dispatch proof, or accepted housing-login/return capture proves field meanings and emit timing. |
| 2026-06-09 | F-003/F-025 `ServerTimeOfDayAuxUInt32` (`0x0846`) cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments and selected exports were sufficient for registration evidence. `Network_RegisterServerOpcode_0351` (`14006c290`) registers `0x0846` size `4` at `selected_decompiled.c:12870` to unlabelled reader slot `LAB_140080c60`; `0x01A6` reuses the same slot at `selected_decompiled.c:13281`. No standalone `140080c60` fragment is present in the selected cache. Selected exact scans found no `Network_SendOpcodePayloadHelper` / `Network_SerialiseBufferedMessageById` producer, apply owner, or post-read consumer for `0x0846` beyond registration. Current source keeps `ServerTimeOfDayAuxUInt32` as a neutral `ServerUnresolvedUIntPayload` shape covered by packet-placeholder tests, while runtime clock sync emits separate `ServerTimeOfDay` (`0x0845`) from `Player.SendInGameTime`; `Prerequisite_CheckTimeOfDay` (`14049dd10`) only proves the client-side prerequisite comparison path. | Keep `ServerTimeOfDayAuxUInt32.Value` neutral and non-emitted until a native server producer/send site, apply/consumer owner, callback/table owner, or accepted `0x0846` capture proves semantics and timing; do not alias it to the implemented `0x0845` time sync or prerequisite comparison path. |
| 2026-06-09 | F-002 `Client0x0701` cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5` were sufficient for writer and registration evidence. `Network_RegisterServerOpcode_0351` (`14006c290`) registers `0x0701` size `8` at `selected_decompiled.c:13899` / setup literal `140079e05` with `LAB_1400a69c0` and `ClientUInt2UInt32_WritePayload` (`1400a69d0`). The cached writer serializes one 2-bit field followed by one `uint32`; no standalone `1400a69c0` fragment is present in the selected cache. Selected xrefs for `1400a69d0` are label-only, selected call edges are writer-local plus generic bitstream helper calls, exact selected scans found `0x0701` only at registration/comment evidence, no selected `Network_SendOpcodePayloadHelper(...0x0701...)`, and no selected `140dd0a74` pointer-owner evidence. Positive controls stayed separate: challenge-choice senders `140710c10`/`140710d60`/`140711ea0`/`140711f10` send `0x00C5`, matching queue dispatch `14076c830` sends `0x05EF`/`0x05F3`/`0x05F8`/`0x05F9`, and matching replacement `14076aa30` sends `0x05D5`. Current source keeps `Client0x0701.LeadingBits` / `TrailingValue` neutral and log-only. | Keep `Client0x0701` diagnostic-only until an opcode-specific native sender, consumer, callback/table owner, indirect send rail, or accepted payload proves semantics; do not alias from shared writer shape, registration adjacency, public-event/queue neighbors, or positive controls for other opcodes. |
| 2026-06-09 | F-002 `Client0x07E3` cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5` were sufficient. `ClientWorldOpcodeRegister_MovementSpline` (`1400a8190`) registers `0x07E3` size `4` at `1400a8282` with `ClientUInt32_ReadPayload` (`14007d000`), shared `ClientTradeskillResetTalents_WritePayload` (`14007d010`), and `ServerUInt32_ReadPayload` (`14007ab50`). The helpers prove only one raw `uint32` in each direction; selected xrefs are label-only and selected call edges for `14007d010` are writer-local plus `ClientCompoundTradeskillUInt32_WriteCluster` (`14007dc80`), not a `0x07E3` owner. Exact selected scans found `0x07E3` only at registration/comment evidence, no selected `Network_SendOpcodePayloadHelper(...0x07E3...)`, and no selected `140c27018` static name-rail evidence. Positive controls stayed separate: matching replacement `14076aa30` sends `0x05D5`, movement control ack `14057a630` sends `0x0635` plus possible `0x063A`, movement speed `1404dafb0` sends `0x063B`, and tradeskill reset `14059acb0` sends `0x0858`. Current source keeps `Client0x07E3.Value` neutral and log-only. | Keep `Client0x07E3` diagnostic-only until an opcode-specific native sender, consumer, callback/table owner, indirect send rail, or accepted payload proves semantics; do not alias from shared `uint32` helpers, static name-rail gaps, numeric adjacency, or positive controls for other opcodes. |
| 2026-06-09 | F-010/F-002 `Client0x062A`/`Client0x0634` cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5` were sufficient. `ClientWorldOpcodeRegister_MovementSpline` (`1400a8190`) registers `0x062A` size `4` at `1400a82b6` and `0x0634` size `4` at `1400a872c` with `ClientUInt32_ReadPayload` (`14007d000`), shared `ClientTradeskillResetTalents_WritePayload` (`14007d010`), and `ServerUInt32_ReadPayload` (`14007ab50`). `14007d000` advances 32 bits, `14007d010` writes one raw `uint32`, and `14007ab50` reads one raw `uint32`; selected xrefs are label-only and selected call edges for `14007d010` are writer-local plus `ClientCompoundTradeskillUInt32_WriteCluster` (`14007dc80`), not an opcode owner. Exact selected scans found `0x062A`/`0x0634` only at registration/comment evidence, no selected `Network_SendOpcodePayloadHelper(...0x062A/0x0634...)`, and no selected `140c25488`/`140c25528` static name-rail evidence. Positive controls stayed separate: matching replacement `14076aa30` sends `0x05D5`, and movement control ack `14057a630` sends `0x0635` plus possible `0x063A` follow-up. Current source keeps both `Value` fields neutral and handlers log-only. | Keep `Client0x062A` and `Client0x0634` diagnostic-only until an opcode-specific native sender, consumer, callback/table owner, indirect send rail, or accepted payload proves semantics; do not alias from shared `uint32` helpers, static name-rail gaps, numeric adjacency, or positive controls for `0x05D5`/`0x0635`. |
| 2026-06-09 | F-002 `Client0x0550` cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5` were sufficient. `ClientWorldOpcodeRegister_MovementSpline` (`1400a8190`) registers `0x0550` size `4` with `ClientUInt32_ReadPayload` (`14007d000`), shared `ClientTradeskillResetTalents_WritePayload` (`14007d010`), and `ServerUInt32_ReadPayload` (`14007ab50`). `14007d000` advances 32 bits and `14007d010` writes one raw `uint32`; selected call edges for `14007d010` are writer-local plus `ClientCompoundTradeskillUInt32_WriteCluster` (`14007dc80`), not a `0x0550` owner. Exact selected scans found `0x0550`/`0x550` only at the registration row and writer-label comments, found no selected `Network_SendOpcodePayloadHelper(...0x0550...)`, and found no selected `140c247e0` static name-rail evidence. Positive controls stayed separate for ICComm `0x0546`/`0x054B`, `ServerSpellList` `0x0551`, matching replacement `0x05D5`, movement ack `0x0635`, tradeskill reset `0x0858`, ability-book `0x017A`, combat-log options `0x0248`/`0x0249`, and P2P trade `0x0192`. Current source keeps `Client0x0550.Value` neutral and log-only. | Keep `Client0x0550` diagnostic-only until an opcode-specific native sender, consumer, callback/table owner, indirect send rail, or accepted `0x0550` payload proves semantics; do not alias from shared `uint32` helpers, static name-rail gaps, numeric adjacency, or positive controls for other opcodes. |
| 2026-06-09 | F-002 `Client0x063E` cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5` were sufficient. `ClientWorldOpcodeRegister_MovementSpline` (`1400a8190`) registers `0x063E` size `8` with `LAB_14007ae30`, shared `ClientSuggest_WritePayload` (`14007ae80`), and `LAB_140080d20`; `14007ae80` only calls `NetworkBitWriter_WriteWideString` (`140336040`). `LAB_140080d20` has no selected standalone body/label, is reused by `0x0724`, `0x058C`, and `0x00CE`, and nearby `140080d30`/`140080d70` are different server readers. Exact `0x063E` cache/export scans found only the registration row and shared-slot references, while positive controls stayed separate for support suggest `0x0833`, account-item `0x0233`/`0x07C6`, support ticket `0x06D1`, marketplace commodity info `0x03E6`, and marketplace filter `0x07DC`. Current source keeps `Client0x063E.Text` neutral and log-only. | Keep `Client0x063E` diagnostic-only until an opcode-specific native sender, consumer, callback/table owner, indirect send rail, or accepted `0x063E` payload proves semantics; do not alias to suggest, account-item, support-ticket, marketplace/auth/status, or filter behavior from shared string helpers, shared `LAB_140080d20`, or numeric adjacency. |
| 2026-06-09 | F-002 `Client0x012D` cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5` were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) still registers `0x012D` size `8` with `LAB_14007ae30` and shared `ClientSuggest_WritePayload` (`14007ae80`), whose body only calls `NetworkBitWriter_WriteWideString` (`140336040`). The same writer is reused by `0x0833`, `0x0233`, `0x07C6`, `0x063E`, `0x03E0`, `0x03AC`, and `0x039B`; selected xrefs for `14007ae80` are label-only and selected call edges only show the wide-string write. Positive controls stay separate: `Support_SendClientSuggest` (`14063b9f0`) sends `0x0833`, account-item claim/return (`140006ba0`/`140006c50`) send `0x0233`/`0x07C6`, and realm transfer (`140027d80`) sends `0x0142` plus `0x0244` follow-up traffic. Exact `0x012D` cache/export scans found only the registration row plus unrelated struct-offset/switch-helper hits, not a sender or consumer owner. Current source keeps `Client0x012D.Text` neutral and log-only. | Keep `Client0x012D` diagnostic-only until an opcode-specific native sender, consumer, callback/table owner, indirect send rail, or accepted `0x012D` payload proves semantics; do not alias to support suggest, account-item claim/return, realm-transfer, or pet UI behavior from shared string helpers or numeric adjacency. |
| 2026-06-09 | F-002 `Client0x00C8` cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5` were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) still registers decimal `200` (`0x00C8`) size `4` with `LAB_14008a140` / `ClientMatchType_ReadPayload` and `LAB_14008a150` / `ClientMatchType_WritePayload`; `0x05B5` and `0x05B6` reuse the same helper pair. `14008a140` advances 5 bits, `14008a150` masks/writes one `0x1f` value, selected xrefs for `14008a150` are label-only, selected call edges are writer-local, and selected send-helper scans found no `0x00C8` sender or consumer. Challenge-choice positive controls send `0x00C5` through `140710c10`/`140710d60`/`140711ea0`/`140711f10`; matching queue dispatch `14076c830` sends `0x05EF`/`0x05F3`/`0x05F8`/`0x05F9`, not `0x00C8`. Current source keeps `Client0x00C8.MatchType` neutral and log-only. | Keep `Client0x00C8` diagnostic-only until an opcode-specific native sender, consumer, callback/table owner, indirect send rail, or accepted `0x00C8` payload proves semantics; do not alias to queue-leave, challenge choice, or matching queue dispatch from shared helpers or numeric adjacency. |
| 2026-06-09 | F-002 `Client0x0928` cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5` were sufficient. `ClientWorldOpcodeRegister_MovementSpline` (`1400a8190`) still registers `0x0928` size `8` with read-advance `LAB_140089240`, `ClientUInt32UInt5_WritePayload` (`1400898b0`), and `ServerUInt32UInt5_ReadPayload` (`14008ce80`), proving only one `uint32` plus one 5-bit field. The positive pet-stance controls stay separate: `0x068E` has real sender `Pet_SetStance_SendClientPetSetStance` (`14050a270`), and `0x068F` applies through `Pet_ApplyStanceChangedPayload` (`1403c0a80`) with cache reads via `Pet_GetStance_ReadCachedStance` (`14050a130`). Selected xrefs for `1400898b0` are label-only, and selected send-helper evidence is pet-only; current source keeps `Client0x0928.LeadingValue` / `TrailingBits` neutral and log-only. | Keep `Client0x0928` diagnostic-only until an opcode-specific native sender, consumer, callback/table owner, indirect send rail, or accepted `0x0928` payload proves semantics; do not alias to pet stance, cooldown UI, reward-property, movement, or buff behavior from shared helpers. |
| 2026-06-09 | F-030 PTR queue/copy handoff cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5` were sufficient. `ClientInitiatePTRCharacterCopy_SendFromLuaDispatch` (`140022270`) sends `0x06E7` with selected character id and follow-up `ClientEncrypted` `0x0244`; `ClientPtrCopy_SendFromLuaDispatch` (`14063f540`) and `ClientPtrCopy_SendFromLuaDispatch2` (`140707d80`) send separate zero-byte `0x06E8` payloads; `ServerPtrCharacterCopyQueued_DispatchLuaEvent` (`140020ea0`) consumes `0x06EA` by dispatching Lua `PTRCharacterCopyQueued`. `Network_RegisterServerOpcode_0351` (`14006c290`) registers `0x06EA` size `1` to `ServerEmpty_ReadPayload`, while adjacent `ServerPtrCharacterCopyQueued_WritePayloadCluster` (`14007dd40`) is registered for `0x0592`, not `0x06EA`. Current source keeps `ClientInitiatePTRCharacterCopyHandler` and `ClientPtrCopyHandler` diagnostic-only and does not emit `ServerPtrCharacterCopyQueued`. | Keep `ServerPtrCharacterCopyQueued` non-emitted and PTR copy state non-mutating until a native `0x06EA` producer/apply path, queue/copy-state mutation, callback/table owner, dynamic dispatch proof, or accepted PTR-copy capture proves timing and handoff semantics. |
| 2026-06-09 | F-010 standalone `ServerRaidQueueStatus` cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5` were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) still registers `0x0718` size `0x20` to `ServerRaidQueueStatus_ReadPayload` (`14008bf80`) and adjacent `0x071A` size `0x10` to `ServerRaidInfoResponse_ReadPayload` (`14008c010`). `0x0718` still reads saved-instance id, 15-bit world id, FILETIME, days float, and prime level; `0x071A` still reads count-plus-row arrays and is the only selected code caller into `14008bf80`. `Group_DispatchRaidInfoResponse` (`1406042b0`) maps those rows to `RaidInfoResponse` UI fields and dispatches the named event, but selected xrefs/call edges do not prove a standalone non-zero `0x0718` producer. Current source keeps only the zero-value raid-info compatibility emit and packet-shape/placeholder tests. | Keep standalone non-zero `0x0718` blocked until a real non-`.pdata` producer/apply table or accepted non-zero `0x0718` capture proves queue-state semantics and timing. |
| 2026-06-09 | F-010 `ServerMatchingGroupMemberRoleSelection` cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5` were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) still registers `0x0600` size `0x18` to `ServerHousingCommunityPlotReservation_ReadPayload` (`140086e70`), the same identity-plus-`uint32` reader reused by seven other registration rows including `0x051F`. The selected xref is label-only; selected call edges are reader-local `FUN_14006c090` / `NetworkBitReader_ReadUInt64` calls plus two `ServerLootWinner_ReadPayload` (`1400a4e50`) row-helper calls, not matching role-selection ownership. Matching role-check/UI helpers (`14076b770`, `1405c0e90`, `1405c0760`, `1405c3d30`) remain separate and current source keeps `TrailingValue` neutral with packet-shape-only coverage and no runtime emit. | Keep `0x0600` blocked until a real apply/consumer table, native producer, callback owner, or accepted live payload ties the trailing value to role-selection state. |
| 2026-06-09 | F-010 `ServerMatching0x05CF` cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5` were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) still registers `0x05CF` size `4` to `ServerUInt32_LocalReadThunk` (`140099110`), shared with `0x085D`; selected xrefs are label-only and reader call edges only reach `FUN_14006c090`. `MatchingManager_ApplyManagerUInt32Field0xA0` (`1405c41c0`) still writes payload uint32 to matching-manager `+0xa0` / optional subobject `+0x98` and calls `FUN_1400a8020`, but no selected call edge or non-`.pdata` xref proves opcode dispatch/index or `ClientEvent` ownership. Current source keeps `ServerMatching0x05CF` as neutral packet-shape-only `ServerUnresolvedUIntPayload` with no runtime emit. | Keep `0x05CF` blocked until a real non-`.pdata` matching apply dispatcher/index binds the opcode to `1405c41c0`, or an accepted live capture observes `0x05CF` with `0x05CA`/`0x05CC` queue and match-ready context. |
| 2026-06-09 | F-026 item-data aux `0x056B` / `0x056C` / `0x056D` cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5` were sufficient. `Network_RegisterServerOpcode_0351` (`14006c290`) still registers `0x056B` size `0x20` to `ServerItemModdableData_ReadPayload` (`1400a3ce0`: item guid, threshold data, random glyph data, random circuit data), `0x056C` size `0x28` to `ServerItemMicrochips_ReadPayload` (`1400a3d50`: item guid, maker character id, random circuit data, 18-bit power-core item id, 3-bit counted microchip item ids), and `0x056D` size `0x18` to `ServerItemGlyphs_ReadPayload` (`1400a3e40`: item guid, random glyph data, 4-bit counted glyph item ids). Selected xrefs are label-only and selected call edges stay reader-local. Client apply helpers `1403b8540`/`14056aa20` and `1403b85a0`/`14056aba0` prove item patch/application plus `ItemModified`, but not server producer timing; current source has these packets only in packet models and packet-shape tests. | Keep `0x056B`/`0x056C`/`0x056D` non-emitted until a native server producer path, apply-table classification, callback/table owner, or accepted item-replication capture proves runtime semantics and send timing. |
| 2026-06-09 | F-030 `0x03EF` realm-transfer aux cached-export/source recheck | Mapped only | Existing cached `WildStar64.exe` fragment `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5/14007d790.fragment.c` reconfirmed `ServerRealmTransferDestinationsAux_ReadPayload` (`14007d790`) as `uint32`, `uint32` byte count, allocation of that count, and raw byte copy. `Network_RegisterServerOpcode_0351` (`14006c290`) registers `0x03EF` size `0x10` with null write/handler slots, and also registers the same reader for `0x01B4`. Selected xrefs are label-only, selected call edges only show bit-read/allocation/raw-copy helpers, and current source uses `ServerTransferDestinationRealmList` for `ClientGetRealmTransferDestinations` while finding `ServerRealmTransferDestinationsAux` only in the packet model and packet-shape test. | Keep `0x03EF` non-emitted and its raw payload neutral until a native producer/apply path, callback/table owner, dynamic dispatch proof, accepted realm-transfer packet capture, or server catalog/realm-list artifact explains destination semantics and transfer/PTR timing. |
| 2026-06-09 | F-016..F-021 spell auxiliary producer/apply cached-export recheck | Mapped only | Existing cached `WildStar64.exe` fragments were sufficient; no export refresh or label change was needed. `Network_RegisterServerOpcode_0351` (`14006c290`) still registers `0x07FC` size `0x0c` to `ServerSpellCastResult_ReadPayload` (`140094fb0`: leading `uint32`, 18-bit `Spell4Id`, 9-bit `CastResult`), `0x080F`/`0x0810` size `0x10` to `ServerSpellUInt32TripletList_ReadPayload` (`140095da0`: `uint32` count plus `140080bf0` triplet rows), and `0x0812` size `0x10` to `ServerSpellFourUInt32_ReadPayload` (`14007fef0`: four `uint32` fields). Selected xrefs are label-only and selected call edges show only bit-reader/allocation/row-parser calls; current source has mixed `ServerSpellCastResult` emitters but no proof for `Unknown0` semantics, and `ServerSpellUInt32TripletList`, `ServerSpellUInt32TripletListVariant`, and `ServerSpellFourUInt32` remain model/test-only. | Keep `ServerSpellCastResult.Unknown0` neutral and keep the triplet/four-uint spell aux packets non-emitted until a native apply/producer path, callback/table owner, dynamic dispatch proof, or accepted spell packet capture proves field semantics and emit timing. |
| 2026-06-09 | F-012 chat aux / ICComm social producer cached-export recheck | Mapped only | Ghidra MCP discovery reported no running instances, so this pass used tracked labels plus cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5`. `Network_RegisterServerOpcode_0351` (`14006c290`) still binds `0x01B8` to row writer/reader `140085af0`/`140085ca0`, `0x01C4` to envelope writer/reader `140085e30`/`140085fe0`, `0x01C1` to `1400861b0`/`140086410`, and `0x01EF` to reader-only `1400a0890`. Selected call edges only show row/envelope relationships and wrapper helpers `140086520`/`140086600`; current source still finds `ServerChatAuxBulk`, `ServerChatAuxPayload`, `ServerChatAuxPayloadAlt`, and `ServerChatAuxNotification` only in packet models and packet-shape tests. | Keep chat aux packets non-emitted and field names neutral until a native producer/apply path, callback/table owner, dynamic dispatch proof, accepted packet capture, or two-client social flow proves emit timing and runtime semantics. |
| 2026-06-09 | F-005 marketplace aux `0x06DF` / `0x07D5` producer cached-export/source recheck | Mapped only | Ghidra MCP discovery reported no running instances, so this pass used tracked labels plus cached `WildStar64.exe` fragments. `ServerAuctionPostAux_ReadPayload` (`140090090`) still reads count, counted `uint32` array, counted byte array of the same count, and trailing `uint32`; `ServerAuctionsByFilterAux_ReadPayload` (`14008fe80`) still reads one 14-bit value, three `uint32` fields, and one flag. `Network_RegisterServerOpcode_0351` (`14006c290`) still only registers `0x06DF` at size `0x20` and `0x07D5` at size `0x14`; selected call edges show reader-local parsing helpers, and current source finds the packets only in models and packet-shape tests. | Keep both marketplace aux packets non-emitted until a native marketplace apply/producer path, retail/live marketplace capture, or server producer witness proves field semantics and emit timing. |
| 2026-06-09 | F-002 structural realm/addon diagnostic cached-export/source recheck | Mapped only | Ghidra MCP discovery reported no running instances, so this pass used tracked labels plus cached `WildStar64.exe` fragments. Realm structural rows remain serializer-owned: `ClientAccountRealmData_WritePayload` (`1400aba70`) serializes one nested account-realm row, `Client0x0760_WritePayload` (`1400abd30`) / `Client0x0762_WritePayload` (`1400ac410`) are called by `ServerRealmList_WritePayload` (`1400ac770`) for realm/message rows, and no standalone client send or consumer event surfaced. Addon structural naming remains supported by `ClientUnresolvedDiagnosticPacket07B6_WritePayload` (`140080220`), row helper `14007ff70`, `Client0x07B6_SendFromModuleList` (`1403f42e0`), and matching `Lua_GetAddons` (`140043370`) module-list walking, but server consumer intent is still unproven. Current source/tests keep all four handlers log-only with no gameplay mutation or server emit. | Keep `ClientAccountRealmData`, `ClientRealmListRealmRow`, `ClientRealmListMessageRow`, and `ClientAddonModuleList` non-mutating until a standalone sender/consumer, callback/table owner, indirect send rail, or live payload proves event semantics. |
| 2026-06-09 | F-002 `Client0x011B`/`Client0x011D` diagnostic ownership cached-export/source recheck | Mapped only | Ghidra MCP discovery reported no running instances, so this pass used tracked labels plus cached `WildStar64.exe` fragments. `ClientCraftingAbandon_WritePayload` (`140001ba0`) still proves only an empty shared writer for `0x011B`; selected caller/xref evidence remains shared pointer/table context rather than an opcode-specific owner. `ClientTradeskillResetTalents_WritePayload` (`14007d010`) still proves a raw `uint32` writer for `0x011D`, with selected code caller `ClientCompoundTradeskillUInt32_WriteCluster` (`14007dc80`) remaining a shared compound serializer rather than an `0x011D` owner. Current source/tests keep `Client0x011B` empty, `Client0x011D.Value` neutral, reject loot-bind/mail/loot-vacuum/tradeskill-reset aliases, and pin both handlers as log-only with no plaintext or encrypted server emit. | Keep `Client0x011B` and `Client0x011D` diagnostic-only until an opcode-specific native sender, consumer, callback/table owner, indirect send rail, or live payload proves request intent and field semantics. |
| 2026-06-09 | F-002 `Client0x00ED` diagnostic ownership cached-export/source recheck | Mapped only | Ghidra MCP discovery reported no running instances, so this pass used tracked labels plus cached `WildStar64.exe` fragments. `ClientUnresolvedDiagnosticPacket00ED_WritePayload` (`1400a6200`) still writes one `uint64`, one `uint32`, one `uint64`, and three trailing bits; selected xrefs/call edges show only the label, internal jumps, and generic bitstream helpers, with no opcode-specific gameplay sender or consumer. Current source/tests keep `Client0x00ED.Value0`..`Value5` neutral, reject duel/mail/path aliases, and pin the handler as log-only with no plaintext or encrypted server emit. | Keep `Client0x00ED` diagnostic-only until an opcode-specific native sender, consumer, callback/table owner, indirect send rail, or live payload proves request intent and field semantics. |
| 2026-06-09 | F-001 STS token/optional auth cached-export/source recheck | Mapped only | Ghidra MCP discovery reported no running instances, so this pass used tracked labels plus cached `StsConnLib64.MT.dll` fragments. `StsConn_SendLoginTokenStart` (`180003d70`) proves the token branch starts with `ClientRand` on transaction `0x61`; `StsConn_SendTokenKeyData` (`18000a730`) reads `ServerRand`, `ServerPublicKey`, and `ServerSignature`, validates through `StsConn_ValidateTokenServerKeyMaterial` (`180012de0`), creates an RSA client through `StsCrypt_CreateRsaClient` (`180037cb0`), and sends `PremasterSecret`, `AuthnToken`, optional `AuthProviderCode`, and `AppId` on transaction `0x3b`. `StsConn_SendRequestToken` (`180004c40`), `StsConn_SendAssociateMyExternalAccount` (`1800067d0`), and `StsConn_OnAuthnTokenResponse` (`180008230`) remain mapped support paths. Current source has implemented SRP/login, key-data, login-finish, request/consume game-token, user-info, verified-IP, game-account, and presence compatibility routes, but no optional token/auth route handlers. | Keep `/Auth/LoginTokenStart`, `/Auth/TokenKeyData`, `/Auth/RequestToken`, and `/Auth/AssociateMyExternalAccount` blocked until native crypto proof or accepted startup STS captures prove server key material, signature validation, premaster/auth-token derivation, reply fields, post-token crypto transition, and route ordering. |
| 2026-06-09 | F-003 `Server0x0015` shared-reader cached-export/source recheck | Mapped only | Ghidra MCP discovery reported no running instances, so this pass used tracked labels plus cached `WildStar64.exe` fragments. Cached `140081f00` still proves only the shared 5-bit plus `uint32` reader registered for `0x0015` and matching `0x0628`; cached `140081f60` calls that reader only as a `ServerFortuneRewards` money-reward row helper. The positive matching path remains cached `1405c0e00`, which consumes the row as match type plus average wait and dispatches `MatchingAverageWaitTimeUpdated` for `0x0628`; no opcode-specific `0x0015` apply owner, producer, or post-read consumer surfaced. Current source/tests keep `Server0x0015.Value0`/`Value1` neutral and non-emitted. | Keep `Server0x0015` neutral until a native `0x0015` apply/producer path, client post-read consumer, or live `0x0015` payload capture proves semantic field names and production timing. |
| 2026-06-09 | F-007 reward rotation `0x07CD` apply/flag/throttle cached-export recheck | Mapped only | Ghidra MCP discovery reported no running instances, so this pass used tracked labels plus cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5`. Cached `14008fcb0` still proves only the `0x07CD` content-context wire shape; cached `140635840` initializes seven request-throttle slots at `manager + 0x150 + index * 0x14`; cached `140636ba0` sends only the content-type index through `0x07CC`; cached `140636c40` proves refresh-by-index / loaded-schedule lookup rather than `0x07CD` field application. Current source keeps `ServerRewardRotationContentContext.UInt0`/`UInt1`/`UInt3`/`Flag` neutral and evidence-artifact tests preserve the blocker text. | Keep `0x07CD` apply helper, `Flag` meaning, dynamic throttle-slot assignment, post-claim item/currency/property delivery, and exact retail schedule/content selection blocked until a retail `0x07CD` capture, dynamic runtime apply-dispatch breakpoint, or accepted live Content Finder/storefront reward-rotation bundle proves the semantics. |
| 2026-06-09 | F-031 Fortune retail weights / active rotation cached-export recheck | Mapped only | Ghidra MCP discovery reported no running instances, so this pass used tracked labels plus cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5`. Cached `140081f60` still proves only the `ServerFortuneRewards` transport shape: item2 ids, money rows, and parallel item/money probability arrays. Cached `1407292a0` copies server-provided arrays into Fortune UI state, cached `140766370` exposes cached item2 rows and `fProbability = serverFloat * 100`, cached `1404d60f0` dispatches `0x03CF`-`0x03D2`, and cached `1400a0b10` remains the card-state reader. Current source still emits `ServerFortuneRewards` from emulator rarity-tier `FortuneRewardPool`; no retail active-rotation source, per-item weight table, or storefront-server catalog proof surfaced. | Keep exact Madame Fay per-item weights, active rotation catalog, and money/probability arrays blocked until a retail `ServerFortuneRewards` capture, storefront-server catalog dump, or native/server producer artifact proves item ids and probabilities. |
| 2026-06-09 | F-025 entity-stat aux producer cached-export recheck | Mapped only | Ghidra MCP discovery reported no running instances, so this pass used tracked labels plus cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5`. Cached `140080bf0` still proves only the shared triplet reader used by `0x0889`; cached `1400980f0` proves only the shared `uint32` plus wide-string reader used by `0x08CC`; cached `140097620`, `140097ee0`, `140097690`, and `140097f70` prove only the `0x08F4`, `0x0939`, `0x093D`, and `0x093E` reader shapes. Current source still finds the six `ServerEntityStat*` aux packets only in packet models, packet-shape tests, placeholder naming guards, and negative entity-create emission guards, while runtime stat sends remain regular `ServerEntityStatUpdateFloat` / `ServerEntityStatUpdateInteger`. | Keep `0x0889`, `0x08CC`, `0x08F4`, `0x0939`, `0x093D`, and `0x093E` producers and semantic field names blocked until a per-opcode `WorldSocket+0x15b0` `vtable+0x58` apply handler, apply-table classification, or live sniff/order witness proves field semantics and emit timing. |
| 2026-06-09 | F-004 housing neighborhood producer cached-export recheck | Mapped only | Ghidra MCP discovery reported no running instances, so this pass used tracked labels plus cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5`. Cached `14009cbe0` and `14009ebf0` still prove only the `0x0501` row reader and `0x0506` realm-plus-counted-row list reader; cached `1404ba4f0` proves client cache clear/rebuild plus `HousingNeighborhoodRecieved` dispatch; cached `140205900` proves `HousingNeighborhoodInfo.tbl` table-loader presence only. Current source still finds `ServerHousingNeighborhoodEntry` / `ServerHousingNeighborhoodList` only in packet models, packet-shape tests, placeholder naming guards, and residence-session negative emission guards. | Keep `0x0501`/`0x0506` producers blocked until a native server-push path or accepted live housing UI/realm-login capture proves trigger, row backing, row-tail field population, and realm/session timing. |
| 2026-06-09 | F-008 crafting current-craft / aux producer cached-export recheck | Mapped only | Ghidra MCP discovery reported no running instances, so this pass used tracked labels plus cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5`. Cached `1400a3af0` and `140081df0` still prove only the `0x084B` / `0x0855` reader shapes; cached `1400a46b0` / `1405e6830` prove `ServerCraftingCurrentCraft` read/apply and `CraftingUpdateCurrent` dispatch; cached `1400a3d50`, `1403b8540`, and `14056aa20` prove `0x056C` item microchip read/apply into item state and `ItemModified` dispatch. Current source still emits `ServerCraftingFinish` only for fixed-recipe success while tests guard against blocked current-craft/aux emissions. | Keep `ServerCraftingCurrentCraft`, `0x084B`, `0x0855`, and `0x056C` producers blocked until native producer timing or accepted live crafting/item-replication capture proves enqueue cadence, discovery/hot-cold behavior, non-success sigil rules, and microchip patch timing. |
| 2026-06-09 | F-025 map-tracked-unit producer evidence recheck | Mapped only | Ghidra MCP discovery reported no running instances, so this pass used the tracked labels plus cached `WildStar64.exe` fragments under `selected_decompiled_cache/functions/sha256_231bb2bb3fc6c37f3e8a43a0ba965cc3645287bbc6ccad83d073a495c396b3e5`. Cached `1400a6c10`, `1403f4170`, `1403f4200`, `140430f80`, `140511c80`, `14068adb0`, and `140690500` still show read/apply/Lua consumer behavior only: `0x0849` reads tracked-unit id, XYZ, and a 15-bit `TrackingSlotId`; update/disable apply paths mutate the client cache and dispatch Lua events; `GetMapTrackedUnitData` and public-event/objective getters enumerate cached state. Current source search still finds `ServerMapTrackedUnitUpdate` / `ServerMapTrackedUnitDisable` only in packet models, packet-shape tests, and the negative entity-create emission guard. | Keep runtime producer blocked until native server send-site evidence or an accepted live public-event marker capture proves tracked-unit id allocation, update cadence, disable lifetime, and `TrackingSlotId` selection for `0x0849`/`0x0848`. |
| 2026-06-08 | F-022 / audit F-027 active path helper static-table guard | Implemented | Active path helper methods now resolve `PathMission`, `PathSoldierTowerDefense`, `PathSoldierAssassinate`, `PathSettlerImprovementGroup`, and `PathSettlerHub` through nullable lookups. Explorer explore-zone completion, active object-id completion, Soldier tower/assassinate progress, Settler hub improvement progress, and Settler hub progress percent helpers now treat unavailable static data like missing rows and return no-progress/zero-progress. `PathManagerTests` pin the missing-table no-completion boundaries. Focused PathManager verification passed 81/81 and broader path verification passed 172/172 from alternate output directories. | Switch priority toward evidence-producing passes: path reward presentation, unlock sequencing, current-zone activation proof, per-mission producer timing, exact Soldier/Settler retail semantics, and broader path/content smoke remain evidence-gated. |
| 2026-06-08 | F-022 / audit F-027 path mission static-table guard | Implemented | `PathManager.CompleteMission()` now resolves `PathMission` through a nullable lookup and treats unavailable mission static data like missing mission metadata: direct completion still completes and grants configured mission rewards while skipping mission achievement/XP metadata. Explorer vista and power-map helpers now treat missing `PathMission`, `PathExplorerNode`, and `PathExplorerPowerMap` tables like missing rows and return no-progress. `PathManagerTests` pin direct missing-table completion plus Explorer missing-table no-completion boundaries. Focused PathManager verification passed 72/72 and broader path verification passed 163/163 from alternate output directories. | Keep path reward presentation, unlock sequencing, current-zone activation proof, per-mission producer timing, exact Explorer node/power-map retail semantics, and broader content smoke evidence-gated. |
| 2026-06-08 | F-004 / audit F-004 residence entrance static-data guard | Implemented | `GlobalResidenceManager.GetResidenceEntrance()` now resolves `HousingPropertyInfo` and `WorldLocation2` through nullable lookups and preserves the existing `HousingException` boundary for unavailable property/location rows. `ResidenceEntrance` now preserves the same boundary for missing `World` static data. `GlobalResidenceManagerTests` pin missing-table, empty-table, and table-backed entrance construction. Focused housing entrance verification passed 7/7 and broader housing verification passed 121/121 from alternate output directories. | Keep neighborhood `0x0501`/`0x0506` producer triggers/fields, edit-mode ack/broadcast, community/session precision, decor ownership/unlock/refund semantics, and exact residence entrance UI/timing evidence-gated. |
| 2026-06-08 | F-035 / audit F-042 achievement primary-table startup guard | Implemented | `GlobalAchievementManager.Initialise()` now resolves `Achievement` through a nullable table lookup and treats unavailable achievement static data like an empty cache. Missing character DB registration is treated as no persisted realm-first preload. Unknown achievement ids still return `null`, character/guild achievement lists still return empty sequences for unavailable types, and table-backed player/guild indexing remains unchanged. `GlobalAchievementManagerPartialTableTests` pin missing-table, empty-table, and table-backed boundaries. Focused global achievement verification passed 3/3 and broader achievement verification passed 32/32 from alternate output directories. | Keep Steam payload grammar, achievement-id correlation, data-only/unowned achievement families, exact realm-first UI/timing, and broader achievement UI edge cases evidence-gated. |
| 2026-06-08 | F-014 / audit F-019 creature DropLoot Creature2 partial-table guard | Implemented | `GlobalLootManager.DropLoot(IPlayer, IWorldEntity)` now resolves `Creature2` through a nullable table lookup. Missing or empty `Creature2` static data behaves like a missing creature row and returns the existing false/no-drop boundary before recipient selection, loot-instance creation, or loot notify emission. `GlobalLootManagerTests.DropLoot_WithUnavailableCreatureTableReturnsFalseWithoutLoot` pins missing-table and empty-table behavior, including no active loot instance and no encrypted notify. Focused creature DropLoot verification passed 2/2 and broader loot verification passed 93/93 from alternate output directories. | Keep standalone `ServerLootCanLoot`, exact parent/source selection, bind-on-pickup confirmation policy, roll/master UI parity, static-item/currency chat precision, and loot aux producer timing evidence-gated. |
| 2026-06-08 | F-022 / audit F-026 global quest-manager cache partial-table guard | Implemented | `GlobalQuestManager` now resolves `Quest2`, `Creature2`, and `CommunicatorMessages` through nullable table lookups during startup cache construction. Missing `Quest2` data behaves like an empty quest-info cache, missing `Creature2` data behaves like empty quest giver/receiver relation caches, and missing `CommunicatorMessages` data behaves like empty communicator, quest-delivery, and quest-state trigger caches. Null quest/communicator id arrays are also treated like empty arrays, while public getters preserve existing `null` or empty-sequence missing-data behavior. `GlobalQuestManagerPartialTableTests` pin missing-table, empty-table, table-backed quest relation, delivered communicator, and state-trigger boundaries. Focused global quest-manager verification passed 3/3 and broader quest/communicator-adjacent verification passed 322/322 from alternate output directories. | Keep exact objective guidance UI, receiver/visibility content smoke, public-event producer timing, path target semantics, and broader quest/path content parity evidence-gated. |
| 2026-06-08 | F-016/F-021 spell metadata cache partial-table guard | Implemented | `GlobalSpellManager` now resolves `Spell4`, `Spell4Effects`, `Spell4Telegraph`, `TelegraphDamage`, `Spell4Thresholds`, and `Spell4Base` through nullable table lookups. Missing primary spell tables behave like empty caches, missing `Spell4Base` data preserves the existing invalid-spell `ArgumentOutOfRangeException`, `SpellBaseInfo.GetSpellInfo()` returns `null` for unavailable or out-of-range tier data, and `SpellBaseInfo` / `SpellInfo` secondary metadata lookups preserve missing-row behavior without partial-table startup crashes. `GlobalSpellManagerPartialTableTests` pin missing-table, empty-table, table-backed cache ordering, missing dependency-table materialization, and sparse/out-of-range tier lookup boundaries. Focused spell-manager verification passed 8/8 and broader spell/action-set/pet verification passed 597/597 from alternate output directories. | Keep spell aux producer timing, proc tails, immunity/effect evidence, wrapper lifecycle, current/action-set update transactions, and retail spell semantics evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 item-manager item/item-slot partial-table guard | Implemented | `ItemManager` now resolves `Item` and `ItemSlot` through nullable table lookups when building static item and equipped-slot caches. Missing `Item` static data leaves the item cache empty so `GetItemInfo()` returns null for unavailable rows, and missing `ItemSlot` static data leaves equipped-slot indexes empty so `GetEquippedBagIndexes()` returns the existing empty sequence. `ItemManagerPartialTableTests` pin missing-table, empty-table, and table-backed equipped-slot indexing boundaries. Focused item-manager verification passed 5/5 and broader item/costume/marketplace-adjacent verification passed 117/117 from alternate output directories. | Keep item/error aux producers, exact visual/equip update timing, remaining item eligibility precision, and deeper costume/unlock lifecycle parity evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 item display-source partial-table guard | Implemented | `AssetManager` now resolves `ItemDisplaySourceEntry` through a nullable table lookup and treats a missing display-source table like an empty cache. `ItemInfo.GetDisplayId()` treats unavailable display-source rows like an empty candidate set before returning the existing zero-display fallback, while table-backed single-row and power-level fallback visual selection remain unchanged. `ItemInfoDisplaySourceTests` pin missing-table, empty-table, single-row, and multi-row fallback boundaries. Focused item-display-source verification passed 6/6 and broader item/costume/marketplace display-adjacent verification passed 104/104 from alternate output directories. | Keep item/error aux producers, exact visual update timing, remaining item eligibility precision, and deeper costume/unlock lifecycle parity evidence-gated. |
| 2026-06-08 | F-022 / audit F-026 quest target-group cache partial-table guard | Implemented | `AssetManager` now resolves `TargetGroup` and `QuestObjective` through nullable table lookups for creature-target-group and quest-objective target caches, treating missing sources like empty caches or missing target-group rows while preserving table-backed creature-group indexing and nested quest-objective target expansion. `TargetGroupCriteriaEvaluator` preserves the existing missing nested-row pass-through when recursive criteria are evaluated without `TargetGroup` static data. `AssetManagerTargetGroupTests` and `TargetGroupCriteriaEvaluatorTests` pin missing-table, empty-table, and table-backed cache/recursive boundaries. Focused target-group verification passed 49/49 and broader quest/target-group-adjacent verification passed 371/371 from alternate output directories. | Keep exact objective guidance UI, receiver/visibility content smoke, public-event producer timing, path target semantics, and broader quest/path content parity evidence-gated. |
| 2026-06-08 | F-007 / audit F-009 reward-property modifier-source partial-table guard | Implemented | `AssetManager` now resolves `RewardPropertyPremiumModifier` through a nullable table lookup and treats a missing modifier table like an empty premium modifier source, preserving table-backed Hybrid filtering and tier fall-through behavior. `AssetManagerRewardPropertyTests` pin missing-table, empty-table, and table-backed grouping boundaries. Focused reward-property verification passed 8/8 and broader Reward verification passed 172/172 from alternate output directories. | Keep reward-rotation claim delivery, `0x07CD` apply/flag/throttle semantics, exact schedule selection, and reward/content context producers evidence-gated. |
| 2026-06-08 | F-035 / audit F-042 achievement checklist metadata partial-table guard | Implemented | `AchievementInfo` now resolves `AchievementChecklist` through a nullable table lookup and treats missing checklist static data like an empty checklist source, preserving table-backed filtering by `AchievementId`. `AchievementInfoTests` pin missing-table, empty-table, and table-backed filtering boundaries. Focused achievement metadata verification passed 13/13 and broader achievement verification passed 29/29 from alternate output directories. | Keep Steam payload grammar, achievement-id correlation, data-only/unowned achievement families, exact realm-first UI/timing semantics, and broader achievement UI edge cases evidence-gated. |
| 2026-06-08 | F-004 / audit F-004 residence visual static-data partial-table guard | Implemented | `Residence` visual setters now resolve `HousingWallpaperInfo` and `HousingDecorInfo` through nullable lookups, preserving `ArgumentOutOfRangeException` before visual mutation or dirty-state changes when tables are missing or empty. Persisted residence decor load now preserves `DatabaseDataException` for unavailable wallpaper/decor static data. `ResidenceTests` pin missing-table, empty-table, table-backed visual setter, and persisted decor load boundaries. Focused residence verification passed 15/15 and broader residence/interior-wallpaper/vendor-list/decor item-use verification passed 27/27 from alternate output directories. | Keep housing edit-mode ack/broadcast, neighborhood `0x0501`/`0x0506` producer triggers/fields, community/session precision, decor ownership/unlock/refund semantics, and exact residence visual UI timing evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 costume-save dye partial-table guard | Implemented | `CostumeManager.SaveCostume()` now resolves `ItemDisplay` and `DyeColorRamp` through nullable table lookups and returns the existing `CostumeSaveResult.InvalidDye` before costume mutation or success packets when nonzero dye static data is unavailable. `CostumeItem.GenerateDyeMask()` now rejects unknown nonzero dye ramps instead of null-refing. `CostumeManagerTests` pin missing-table, empty-table, no-dye missing-display, and table-backed save/mask boundaries. Focused costume save verification passed 9/9 and broader costume / packet-shape verification passed 18/18 from alternate output directories. | Keep costume aux producer semantics, exact costume save UI timing, deeper costume lifecycle parity, item/error aux producers, generic unlock deltas, and pet lifecycle timing evidence-gated. |
| 2026-06-08 | F-021 / audit F-025 ActionSet AMP direct-load partial-table guard | Implemented | `ActionSet.AddAmp(ushort)` and `ActionSet.AddAmp(CharacterActionSetAmpModel)` now resolve the singleton `EldanAugmentation` table through nullable lookups, preserving the existing invalid-AMP `ArgumentException` when the table or row is unavailable. `ActionSetAmpTests` pin missing-table, empty-table, and table-backed direct/persisted AMP materialization, including owner-save behavior. Focused AMP/action-set verification passed 28/28 and broader Spell verification passed 221/221 from alternate output directories. | Keep `UpdateSpellInProgress`, async spell-update transaction, exact lock/spec sequencing, authoritative attribute allocation/refund, bonus ability/AMP unlock persistence, ability-book activation edge cases, and spell aux producer timing evidence-gated. |
| 2026-06-08 | F-011 / audit F-016 guild-standard partial-table guard | Implemented | `GuildStandard.GuildStandardPart` now resolves `GameTableManager.Instance.GuildStandardPart` through a nullable lookup, preserving the existing invalid standard-part `ArgumentException` when the table or row is unavailable. `GuildStandardPart.Validate()` now resolves `DyeColorRamp` through a nullable lookup, so missing dye data returns the existing invalid-standard validation result instead of crashing. `GuildStandardTests` pin missing-table, empty-table, and table-backed standard/dye boundaries. Focused guild-standard verification passed 5/5 and broader Guild verification passed 13/13 from alternate output directories. | Keep guild bank economy, influence/tab state, permissions/limits, perks, holomark producer precision, recruitment subscriptions/details, war-party/warplot behavior, boss-token inventory, and exact standard UI timing evidence-gated. |
| 2026-06-08 | F-014/F-026 character-currency CurrencyType partial-table guard | Implemented | `CurrencyManager` now resolves the injected `CurrencyType` table through nullable lookups for `CanAfford()`, `CurrencyAddAmount()`, and `CurrencySubtractAmount()`. Missing `CurrencyType` static data follows the existing missing-row policy: throw the invalid static-currency `ArgumentNullException` before affordability success, currency-state mutation, or `ServerChannelUpdateLoot` / `ServerCombatReward` emission. `CurrencyManagerTests` pin missing-table, empty-table, and table-backed zero-affordability boundaries. Focused character-currency verification passed 7/7 and broader character-currency / loot-adjacent verification passed 32/32 from alternate output directories. | Keep exact retail currency chat/floater policy, item/error aux producers, `ServerLootCanLoot`, exact parent/source selection, bind-on-pickup confirmation policy, roll/master UI parity, and loot aux producer timing evidence-gated. |
| 2026-06-08 | F-006 / audit F-007 storefront offer-item AccountItem partial-table guard | Implemented | `OfferItemData` now resolves `GameTableManager.Instance.AccountItem` through a nullable lookup. Missing `AccountItem` static data follows the existing missing-row policy: throw the invalid `ItemId` catalog-data exception before store offer item-data packet rows are built. `OfferItemDataTests` pin missing-table, empty-table, and table-backed build boundaries. Focused offer-item verification passed 3/3 and broader Storefront verification passed 45/45 from the alternate output directory. | Keep Protobucks/VC request-confirm flow, non-empty `0x026A` owned-order tails, live `0x0986`/`0x098F` emits, non-zero `096A..096C` producers, type-1/type-2 offer-item effects, coupon native sender proof, exact catalog/dirty producer timing, offline transfer persistence, TTL/mail fallback, and coupon-aware routing evidence-gated. |
| 2026-06-08 | F-006/F-026 / audit F-008/F-033 entitlement-manager partial-table guard | Implemented | `AccountEntitlementManager`, `CharacterEntitlementManager`, and shared `EntitlementManager.UpdateEntitlement()` now resolve `GameTableManager.Instance.Entitlement` through nullable lookups. Missing entitlement static data follows existing missing-row policy: persisted account/character entitlement loads throw `DatabaseDataException`, and direct updates throw the existing invalid entitlement exception before mutation or entitlement packet emission. `EntitlementManagerTests` pin account persisted-load, character persisted-load, and direct update missing/empty-table boundaries. Focused entitlement verification passed 4/4, entitlement-adjacent verification passed 20/20, and broader account verification passed 140/140 from the alternate output directory. | Keep Protobucks/VC request-confirm flow, non-empty `0x026A` owned-order tails, live `0x0986`/`0x098F` emits, non-zero `096A..096C` producers, type-1/type-2 offer-item effects, coupon native sender proof, entitlement/update producer timing, character unlock-list deltas, offline transfer persistence, TTL/mail fallback, and coupon-aware routing evidence-gated. |
| 2026-06-08 | F-006 / audit F-008 account-currency partial-table guard | Implemented | `AccountCurrency` and `AccountCurrencyManager` now resolve `GameTableManager.Instance.AccountCurrencyType` through nullable lookups. Missing account-currency static data follows existing missing-row policy: persisted balances still load and serialize for wallet readback, while add/subtract creation throws the existing invalid static-currency exception before mutation or `ServerAccountCurrencyGrant` / `ServerWalletUpdate` emission. `AccountCurrencyManagerTests` pin persisted readback plus missing-table and empty-table creation boundaries. Focused account-currency verification passed 4/4, loot-bag/account-currency verification passed 16/16, and broader account verification passed 136/136 from the alternate output directory. | Keep Protobucks/VC request-confirm flow, non-empty `0x026A` owned-order tails, live `0x0986`/`0x098F` emits, non-zero `096A..096C` producers, type-1/type-2 offer-item effects, coupon native sender proof, offline transfer persistence, TTL/mail fallback, and coupon-aware routing evidence-gated. |
| 2026-06-08 | F-006 / audit F-008 account-item entitlement-table guard | Implemented | `AccountInventoryManager` now resolves `GameTableManager.Instance.Entitlement` through nullable lookups in both entitlement grant-plan branches. Missing entitlement static data follows the existing missing-row path: return `InvalidAccountItem` before entitlement update, currency grant, item delete, or cooldown mutation. `AccountItemHandlerTests` pin immediate account-entitlement and mixed account-item grant-plan boundaries. Focused account-item verification passed 19/19 and broader account-inventory verification passed 100/100 from the alternate output directory. | Keep Protobucks/VC request-confirm flow, non-empty `0x026A` owned-order tails, live `0x0986`/`0x098F` emits, non-zero `096A..096C` producers, type-1/type-2 offer-item effects, coupon native sender proof, offline transfer persistence, TTL/mail fallback, and coupon-aware routing evidence-gated. |
| 2026-06-08 | F-006 / audit F-008 account-item table guard | Implemented | `AccountInventoryManager.CanAddItem()` now resolves `GameTableManager.Instance.AccountItem` through a nullable lookup, so unavailable account-item static data behaves like an unknown account item and returns false before grant eligibility. `AccountInventoryItem` construction now uses the same nullable lookup, preserving the existing invalid account-item exception for persisted/create item materialization instead of crashing on a missing table. `AccountItemHandlerTests` pin both boundaries. Focused account-item verification passed 17/17 and broader account-inventory verification passed 98/98 from the alternate output directory. | Keep Protobucks/VC request-confirm flow, non-empty `0x026A` owned-order tails, live `0x0986`/`0x098F` emits, non-zero `096A..096C` producers, type-1/type-2 offer-item effects, coupon native sender proof, offline transfer persistence, TTL/mail fallback, and coupon-aware routing evidence-gated. |
| 2026-06-08 | F-006 / audit F-008 account-item cooldown-group guard | Implemented | `AccountInventoryManager` now resolves `GameTableManager.Instance.AccountItemCooldownGroup` through a nullable table lookup when seeding configured cooldown placeholders, so unavailable cooldown-group static data behaves like an empty configured list. Persisted cooldown rows still load first and active persisted cooldowns still emit through `ServerAccountItemCooldowns`. `AccountItemCooldownTests` pin missing-table persisted cooldown readback. Focused cooldown verification passed 8/8 and broader account-inventory verification passed 96/96 from the alternate output directory. | Keep Protobucks/VC request-confirm flow, non-empty `0x026A` owned-order tails, live `0x0986`/`0x098F` emits, non-zero `096A..096C` producers, type-1/type-2 offer-item effects, coupon native sender proof, offline transfer persistence, TTL/mail fallback, and coupon-aware routing evidence-gated. |
| 2026-06-08 | F-006 / audit F-008 daily-login reward-table guard | Implemented | `DailyLoginRewardManager` now resolves `GameTableManager.Instance.DailyLoginReward` through a nullable table lookup and treats unavailable reward data like an empty configured schedule. Login update refresh emits zero available rewards instead of crashing, and claim evaluation returns the existing no-reward result before inventory checks or item grants. `DailyLoginRewardManagerTests` pin missing-table update and claim boundaries. Focused daily-login verification passed 7/7 and broader account-inventory verification passed 95/95 from the alternate output directory. | Keep Protobucks/VC request-confirm flow, non-empty `0x026A` owned-order tails, live `0x0986`/`0x098F` emits, non-zero `096A..096C` producers, type-1/type-2 offer-item effects, coupon native sender proof, offline transfer persistence, TTL/mail fallback, and coupon-aware routing evidence-gated. |
| 2026-06-08 | F-024 / audit F-030 map-instance pending-removal formula-table guard | Implemented | `MapInstance.EnqueuePendingRemoval()` now resolves formula `1123` through a nullable `GameFormula` lookup, so missing table data follows the already-documented missing-row path: keep the 30-second client-default pending removal timer and still send `ServerPendingWorldRemoval`. `MapInstancePendingRemovalTests` pin both the missing-table fallback and a table-backed override. Focused pending-removal verification passed 2/2 and broader map verification passed 459/459 from the alternate output directory. | Keep Evil from the Ether and broader instance content manual smoke, exact trigger rows, door/interactable cleanup, cinematic payloads, encounter mechanics, rewards, and content/script choreography evidence-gated. |
| 2026-06-08 | F-007 / audit F-009 reward-property partial-table guard | Implemented | `RewardPropertyManager` now resolves premium modifier reward-property and entitlement static rows through nullable table lookups, skipping unsafe modifier rows instead of crashing account reward-property setup or emitting guessed values. Type-based reward-property updates no-op when `RewardProperty` static data is unavailable, and `SpellHandler.TryResolveRewardPropertyModifier()` treats a missing `RewardProperty` table like an unknown reward-property row. `RewardPropertyManagerTests` pin missing reward-property table, missing entitlement table, table-backed entitlement value, no-packet update, and spell-resolver boundaries. Focused reward-property verification passed 5/5 and the broader reward/spell bucket passed 375/375 from the alternate output directory. | Keep reward-rotation claim item/currency/property delivery, `0x07CD` apply/flag/throttle semantics, exact schedule selection, and reward/content-context producer semantics evidence-gated. |
| 2026-06-08 | F-014 / audit F-019 generated loot reward-table guard | Implemented | `GlobalLootManager` now resolves generated loot account-currency, account-item, and virtual-item reward rows through nullable `AccountCurrencyType`, `AccountItem`, and `VirtualItem` table lookups, preserving the existing `invalid-loot-item` preflight result when a required static table is unavailable. `GlobalLootManagerTests.CanDeliverGeneratedLoot_WithMissingRewardTable_ReturnsInvalidLootItem` pins the direct preflight boundary, and `LootBagUsageTests.TryUseLootBag_AccountCurrencyMissingGameTable_DoesNotConsumeItem` pins no source-item consumption or reward delivery when a loot-bag account-currency table is unavailable. Focused GlobalLootManager / loot-bag verification passed 32/32 and broader loot verification passed 91/91 from the alternate output directory. | Keep standalone `ServerLootCanLoot`, exact parent/source selection, bind-on-pickup confirmation policy, roll/master UI parity, and loot aux producer timing evidence-gated. |
| 2026-06-08 | F-005 / audit F-005 marketplace auction search selector-table guard | Implemented | `GlobalMarketplaceManager.ValidateAuctionSearch()` now resolves nonzero family/category/type search selectors through nullable `Item2Family`/`Item2Category`/`Item2Type` lookups, preserving the existing `InvalidPacketValueException` invalid-request boundary when selector static tables are unavailable. `MarketplaceAuctionHandlerTests.ValidateAuctionSearch_WithMissingSelectorTable_RejectsBeforeSearch` pins missing family, category, and type tables. Focused marketplace auction verification passed 54/54 and marketplace/mail verification passed 89/89 from the alternate output directory. | Keep retail property/rune/equippable filter semantics, exact row caps/refresh cadence, live DB/order-book validation, CREDD owned-order tails, and marketplace aux `0x06DF`/`0x07D5` producer semantics evidence-gated. |
| 2026-06-08 | F-022 / audit F-027 path reward scanbot profile guard | Implemented | `PathManager.GrantPathReward()` now resolves `PathReward.PathScientistScanBotProfileId` through a nullable `PathScientistScanBotProfile` lookup before calling `PetCustomisationManager.UnlockScanBotProfile()`, so unavailable scanbot-profile static data skips only the scanbot reward portion. `PathManagerTests` now pass a table-backed scanbot profile for the supported mixed-reward case and pin a missing-table mixed item+scanbot reward that still grants the item while avoiding `IPetCustomisationManager.UnlockScanBotProfile()`. Focused path manager verification passed 67/67, path / title / spell / pet verification passed 373/373, and broader path / generic-unlock / pet / title / spell collection verification passed 410/410 from the alternate output directory. | Keep exact path reward presentation, overflow/flag semantics, reward-history persistence, unlock sequencing, scanbot profile UI/timing semantics, generic Soldier/Scientist/Settler semantics, and content smoke evidence-gated. |
| 2026-06-08 | F-022 / audit F-027 path reward Spell4 table guard | Implemented | `PathManager.GrantPathReward()` now resolves `PathReward.Spell4Id` through a nullable `Spell4` table lookup, so a missing `Spell4` table behaves like a missing spell row and skips only the spell reward portion. `PathManagerTests` pin a missing-table mixed item+spell reward that still grants the item and avoids `ISpellManager.AddSpell()`. Focused path manager verification passed 66/66, path / title / spell collection verification passed 81/81, and broader path / generic-unlock / pet / title / spell collection verification passed 409/409 from the alternate output directory. | Keep exact path reward presentation, overflow/flag semantics, reward-history persistence, unlock sequencing, broader `Spell4` reward semantics, generic Soldier/Scientist/Settler semantics, and content smoke evidence-gated. |
| 2026-06-08 | F-022 / audit F-027 path reward title partial-table guard | Implemented | `PathManager.GrantPathReward()` now resolves `PathReward.CharacterTitleId` through a nullable `CharacterTitle` lookup before calling `TitleManager.AddTitle()`, so missing title static data skips only the title reward portion and leaves supported sibling item/spell/scanbot rewards eligible. `PathManagerTests` now pass a table-backed `CharacterTitle` row for the supported mixed-reward case and pin a missing-title reward row that still grants the item while avoiding `ITitleManager.AddTitle()`. Focused path manager verification passed 65/65, path / title / spell collection verification passed 80/80, and broader path / generic-unlock / pet / title / spell collection verification passed 408/408 from the alternate output directory. | Keep exact path reward presentation, overflow/flag semantics, reward-history persistence, unlock sequencing, generic Soldier/Scientist/Settler semantics, and broader content smoke evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 title manager persisted-title partial-table guard | Implemented | `TitleManager` now skips persisted title rows whose `CharacterTitle` static data is unavailable, clears an active title that is no longer owned after partial-load filtering, resolves `AddTitle` and `RevokeTitle` through nullable `CharacterTitle` lookups before mutation, and treats missing `CharacterTitle` tables as an empty source for `AddAllTitles`. `TitleManagerTests` pin table-backed persisted load, missing-table persisted skip/active-title clear, missing-table add/revoke fail-closed behavior, table-backed add, and missing-table bulk-add empty-list output. Focused title manager verification passed 6/6, title / spell collection verification passed 33/33, and broader generic-unlock / pet / title / spell collection verification passed 343/343 from the alternate output directory. | Keep title lifecycle UI timing, account/character unlock-list deltas, item/error aux producers, and broader title reward/ownership parity evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 pet customisation persisted-flair partial-table guard | Implemented | `PetCustomisationManager` now skips persisted pet-flair rows whose `PetFlair` static data is unavailable, saved customisation slots with unavailable flair rows resolve to empty slots, and zero-flair slot clears no longer require `PetFlair` table availability. `PetCustomisationManagerTests` pin table-backed persisted load, missing-table persisted skip/zero, and zero-flair clear behavior. Focused pet customisation manager verification passed 3/3 and broader pet verification passed 293/293 from the alternate output directory. | Keep pet flair ownership/object/name validation, pet lifecycle producer timing/scope, item/error aux producers, unlock-list deltas, and broader pet UI sequencing evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 title spell effect partial-table guard | Implemented | `HandleEffectTitleGrant` and `HandleEffectTitleRevoke` now resolve `CharacterTitle` through nullable table lookups, so a missing title table follows the existing unknown-title diagnostic path before `TitleManager.HasTitle()`, `AddTitle()`, or `RevokeTitle()`. `SpellCollectionEffectTests` pin missing-table no-op behavior plus table-backed grant and revoke happy paths. Focused spell collection verification passed 9/9 and broader spell / generic-unlock / pet / title verification passed 334/334 from the alternate output directory. | Keep title lifecycle UI timing, account/character unlock deltas, item/error aux producers, and broader title reward/ownership parity evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 pet-flair spell effect partial-table guard | Implemented | `HandleEffectUnlockPetFlair` now resolves `PetFlair` through a nullable table lookup, so a missing `PetFlair` table follows the existing unknown-pet-flair diagnostic path before `PetCustomisationManager.HasFlair()` or `UnlockFlair()`. `SpellCollectionEffectTests` pin missing-table no-op behavior plus a table-backed pet-flair unlock happy path. Focused spell collection verification passed 5/5 and broader spell / generic-unlock / pet verification passed 312/312 from the alternate output directory. | Keep pet flair ownership/object validation, pet lifecycle timing, item/error aux producers, account/character unlock deltas, and broader pet-flair UI sequencing evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 collection-spell Spell4 partial-table guard | Implemented | `TryLearnCollectionSpell` now resolves `Spell4` through a nullable table lookup, so mount and vanity-pet collection unlock effects treat a missing `Spell4` table like an unknown spell before `SpellManager.AddSpell` or `ServerUnlockMount` / `ServerUnlockVanityPet` emission. `SpellCollectionEffectTests` pin missing-table no-op behavior plus a table-backed mount unlock happy path. Focused spell collection verification passed 3/3 and broader spell / generic-unlock verification passed 27/27 from the alternate output directory. | Keep account/character unlock-list delta timing, collection-spell retail parity, item/error aux producers, pet lifecycle timing, and broader mount/vanity-pet UI sequencing evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 learn-dye-color generic-unlock partial-table guard | Implemented | `HandleEffectLearnDyeColor` now resolves `GenericUnlockEntry` through a nullable table lookup, preserving the existing unknown-generic-unlock diagnostic and delegating missing static data to `GenericUnlockManager.Unlock()` for the mapped invalid result instead of throwing on partial table loads. `SpellCollectionEffectTests` pins the missing-table handoff. Focused spell collection verification passed 1/1 and broader spell / generic-unlock verification passed 25/25 from the alternate output directory. | Keep account/character unlock-list delta timing, generic-unlock readback precision, item/error aux producers, unsupported account-item payload effects, and broader collection-spell parity evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 account-item generic-unlock partial-table guard | Implemented | `AccountInventoryManager` now resolves account-item generic-unlock grant sets and entries through nullable `GenericUnlockSet` / `GenericUnlockEntry` lookups, returning the existing `InvalidAccountItem` result before unlock grant application or account-item deletion when either static table is unavailable. `AccountItemHandlerTests` pin missing set-table and entry-table no-delete boundaries. Focused account-item verification passed 15/15 and broader account-item / generic-unlock verification passed 67/67 from the alternate output directory. | Keep account/character unlock-list delta timing, generic-unlock readback precision, item/error aux producers, and unsupported account-item payload effects evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 persisted generic-unlock load guard | Implemented | `GenericUnlockManager` now resolves persisted `AccountGenericUnlock` rows through nullable `GenericUnlockEntry` lookups, skips missing table/row entries from runtime unlock state and readback without database mutation, and loads table-backed rows as non-dirty. `GenericUnlockManagerTests` pin missing-table persisted skip, table-backed persisted load, non-dirty persisted save, missing-table direct unlock, missing-table unlock-all, and table-backed direct success. Focused manager verification passed 6/6 and broader generic-unlock verification passed 22/22 from the alternate output directory. | Keep account/character unlock-list delta timing, remaining generic-unlock readback precision, item/error aux producers, and nearby account/item aux semantics evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 normal item-use table guard | Implemented | `ItemUseHelper` now treats missing `ItemSpecial` and `CurrencyType` tables like missing rows through the existing invalid-packet boundary before activated item casts, stack/charge consumption, currency-treasure consumption, or currency grants. `ClientItemUseHandlerTests` pin both no-side-effect boundaries. Focused item-use verification passed 9/9 and broader item-use verification passed 20/20 from the alternate output directory. | Keep item/error aux producers, remaining item eligibility precision, supply-satchel/costume aux producer semantics, pet lifecycle timing, unlock deltas, and deeper item-use parity evidence-gated. |
| 2026-06-08 | F-008 tradeskill request partial-table guard | Implemented | `TradeskillRequestHelper` now treats missing `Tradeskill`, `TradeskillBonus`, and `TradeskillTalentTier` tables like missing rows through the existing invalid-packet boundary for learn/drop, pick-talent, and reset requests. `TradeskillRequestHelperTests` pins the three missing-table boundaries plus an allowed zero drop and a valid talent row. Focused tradeskill request verification passed 5/5 and broader crafting verification passed 90/90 from the alternate output directory. | Keep discovery roll/unlock semantics, service-key names, current-craft cadence, `0x084B`/`0x0855` aux producer intent, non-success sigil rules, and `0x056C` microchip patch timing evidence-gated. |
| 2026-06-08 | F-008 additive modifier materialization partial-table guard | Implemented | `CraftingModifierSessionStore` now treats missing `Item`, `TradeskillAdditive`, and `TradeskillCatalyst` tables like missing rows when building queued additive/catalyst item debits, returning the existing invalid additive/catalyst reasons before inventory checks or item mutation. `CraftingAdditiveHandlerTests` pins all three boundaries. Focused additive/rune verification passed 63/63 and broader crafting verification passed 85/85 from the alternate output directory. | Keep discovery roll/unlock semantics, service-key names, current-craft cadence, `0x084B`/`0x0855` aux producer intent, non-success sigil rules, `0x056C` microchip patch timing, and full retail additive/rune eligibility semantics evidence-gated. |
| 2026-06-08 | F-008/F-011 rune/additive partial-table guard | Implemented | `ItemRuneInstallValidator`, `ItemRuneSocketMaskBuilder`, and `CraftingRuneRequestHelper` now treat missing `Item`, `Item2Category`, `TradeskillAdditive`, `TradeskillCatalyst`, and `ItemSpecial` tables like missing rows through the existing `MissingRune`, `InvalidSlot`, or invalid-packet boundaries. `ItemRuneSocketTests` and `CraftingRuneHandlerTests` pin the setup/partial-table cases. Focused rune verification passed 57/57 and broader crafting verification passed 82/82 from the alternate output directory. | Keep discovery roll/unlock semantics, service-key names, current-craft cadence, `0x084B`/`0x0855` aux producer intent, non-success sigil rules, `0x056C` microchip patch timing, and full retail socket eligibility/error semantics evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 scanbot rename and vanity-pet summon guards | Implemented | `ClientSummonVanityPet` now resolves the player's learned spell tier and requires the resulting `ISpellInfo.Effects` to include `SummonVanityPet` before calling `CastSpell`, preventing other learned spell families from using the vanity-pet request path. `ClientPathScientistSetScannerName` now rejects non-scanbot pet types and scanbot profile ids without an existing customisation before `RenamePet`. Focused Pet verification passed 287/287 from the alternate output directory. | Keep pet lifecycle producer timing/scope, broader flair/object/name validation, item/error aux producers, and deeper costume/unlock deltas evidence-gated. |
| 2026-06-08 | F-027 / audit F-034 combat-log disable-others boolean guard | Implemented | `ClientCombatLogDisableOthers` now preserves the raw `uint` payload while keeping the existing bool surface, and `ClientCombatLogDisableOthersHandler` rejects values greater than `1` before player combat-log preference mutation. `OptionHandlerTests` pin the invalid-boolean no-mutation boundary. Focused option verification passed 36/36 from the alternate output directory. | Keep additional option types, broader account-vs-character readback/init, exact combat-log preference breadth, and nearby item/options aux producers evidence-gated. |
| 2026-06-08 | F-034 / audit F-041 datacube table-unavailable guard | Implemented | `DatacubeManager.AddDatacube()` and `AddDatacubeVolume()` now treat missing `Datacube` / `DatacubeVolume` tables like missing rows, rejecting through the existing invalid-row path before state mutation or packet emission. `PathManagerTests` pin both table-unavailable boundaries. Focused archive/datacube verification passed 18/18 and broader PathManager verification passed 64/64 from the alternate output directory. | Verify one datacube or journal pickup chain end-to-end, decode remaining Codex progression/link UX semantics, and reconcile path-mission archive rules before claiming full Codex parity. |
| 2026-06-08 | F-028 / audit F-035 support-case readback | Blocked | Source/protocol recheck found only submit-side support packets (`ClientIncidentReport`, `ClientSupportTicket`, `ClientReportBug`, `ClientStuck`, `ClientSuggest`, `ClientCustomerSurveySubmit`), `ServerSupportTicketResult`, and mapped support aux packet shapes `0x0347..0x0351`. There is no modeled client support-case list/read request, server support-case readback payload, database case lifecycle, or moderation workflow backend to implement safely. Focused Support verification passed 126/126 from the alternate output directory. | Decode support-case result/readback protocol and design/prove a durable moderation/backend workflow before adding case readback, GM lifecycle state, or broader support-case responses. |
| 2026-06-08 | F-008 / audit F-010 crafting fixed-recipe partial-table guard | Implemented | `CraftingCraftRequestHelper` now treats missing `TradeskillSchematic2` and `Item` tables like missing rows and rejects before mutation, treats missing `TradeskillTier` like no XP tier row so table-backed crafts complete with zero XP, and treats missing `TradeskillMaterial` like no material row so material debit falls back to inventory-only counts. `CraftingSimpleCraftHandlerTests` pin all four partial-table boundaries. Focused partial-table verification passed 12/12 and broader crafting verification passed 78/78 from the alternate output directory. | Keep discovery roll/unlock producer semantics, station service-key names, `ServerCraftingCurrentCraft` cadence, `0x084B`/`0x0855` aux producer intent, non-success sigil rules, and `0x056C` microchip patch timing evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 repair-vendor formula-table guard | Implemented | `ClientRepairItemVendor` now treats a missing repair-cost `GameFormula` table/row `0x022F` like zero computable repair cost, returning before affordability checks, credit debit, or durability mutation. `ClientVendorRepairHandlerTests` pin the missing-table no-mutation boundary and a table-backed debit/repair path. Focused vendor verification passed 10/10 from the alternate output directory. | Keep repair durability-update producer timing, item/error aux producers, remaining item eligibility precision, and broader item/vendor UI timing evidence-gated. |
| 2026-06-08 | F-021 / audit F-025 action-set partial-table guards | Implemented | `ClientSetStanceHandler` now treats a missing `Class` table like a missing class row and throws before `IPlayer.InnateIndex` mutation or `ServerStanceChanged` emission. `ClientRequestActionSetChangesHandler` now treats missing `Spell4` and `EldanAugmentation` tables like missing rows, returning existing `UnknownSpellId` / `EldanAugmentationInvalidId` results before save/cache or AMP mutation. `ClientNonSpellActionSetChangesHandler` now treats a missing `Item` table like a missing bag-item row before shortcut mutation/save while known `Item2` rows still save. `ClientCommitAmpSpecHandler` now validates through injected `EldanAugmentation` data and commits resolved AMP entries without a second singleton lookup. `ClientRespecAmpsHandler` now treats missing `EldanAugmentationCategory` / `EldanAugmentation` tables like missing rows before mutation or refresh emits. `ClientSetStanceHandlerTests` and `ActionSetSaveRequestTests` pin the new boundaries. Focused stance verification passed 2/2, focused action-set request verification passed 21/21, and broader action-set/stance verification passed 633/633 from the alternate output directory. | Keep `UpdateSpellInProgress`, async spell-update transaction, exact lock/spec sequencing, authoritative attribute allocation/refund, bonus ability/AMP unlock persistence, and ability-book activation edge cases evidence-gated. |
| 2026-06-08 | F-012 / audit F-017 chat-link item/quest partial-table guard | Implemented | `ChatMessageBuilder.AppendItem()` and `AppendQuest()` now resolve `GameTableManager.Instance.Item` and `Quest2` through nullable lookups. Missing item/quest static data follows the existing missing-row policy: throw the invalid chat-link exception before appending text or adding `ChatFormatItemId` / `ChatFormatQuestId` rows. `ChatMessageBuilderTests` pin missing-table, empty-table, and table-backed item/quest boundaries. Focused chat-link verification passed 6/6 and broader chat verification passed 11/11 from the alternate output directory. | Keep chat aux producer/runtime semantics, exact chat-format retail text policy, ICComm persistence/entitlement gates, exact leave/logout signaling, durable social-option readback, and two-client social smoke evidence-gated. |
| 2026-06-08 | F-012 / audit F-017 emote partial-table guard | Implemented | `ClientEmoteHandler` now treats a missing `Emotes` table like an invalid emote id and throws before `ServerEmote` broadcast or targeted-emote achievement checks. `ClientEmoteHandlerTests` pin missing-table rejection and table-backed stand-state broadcast. Focused emote verification passed 2/2, and broader chat verification passed 6/6 from the alternate output directory. | Keep chat aux producer/runtime semantics, ICComm persistence/entitlement gates, exact leave/logout signaling, durable social-option readback, and two-client social smoke evidence-gated. |
| 2026-06-08 | Mapped small client request partial-table guard | Implemented | `ClientConvertResourceHandler` now treats missing `ResourceConversion`, `Item`, and `CurrencyType` tables like missing rows before inventory, currency, or reputation mutation. `ClientSpline2RequestHandler` treats a missing `Spline2` table like an unknown spline in its log-only path. `MappedRequestHandlerTests` pin missing conversion table, missing target item table, missing currency table, table-backed currency conversion, and missing spline table boundaries. Focused mapped-request verification passed 5/5 from the alternate output directory. | Keep exact retail resource-conversion selection/resource-field semantics, repair durability-update producer timing, dash-cast authority, and remaining small-request producer/consumer timing evidence-gated. |
| 2026-06-08 | F-036 / audit F-043 generic-map partial-table guard | Implemented | `ClientGenericMapNodeRequestHandler` and `ClientGenericMapNodeChosenHandler` now treat missing `GenericMapNode` tables like unknown node ids, and missing `WorldLocation2` tables like missing destination rows. `GenericMapHandlerTests` pin missing-table request/choice no-ops, no-destination acknowledgement, known-node state emits, missing-destination no-teleport, and known-destination teleport. Focused generic-map verification passed 6/6, and broader map verification passed 452/452 from the alternate output directory. | Keep generic-map content-context producer semantics, broader map UI timing, non-title zone-completion rewards, path-specific completion, and live category-total validation evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 generic-unlock manager partial-table guard | Implemented | `GenericUnlockManager.Unlock()` now treats a missing `GenericUnlockEntry` table like a missing row and sends `GenericUnlockResult.Invalid`; `UnlockAll()` treats a missing table like an empty table and emits nothing. `GenericUnlockManagerTests` pin missing-table direct unlock, missing-table unlock-all no-op, and a table-backed success path. Focused generic-unlock verification passed 19/19 from the alternate output directory. | Keep account/character unlock-list delta timing, generic unlock persistence/readback precision, item/error aux producers, and nearby account/item aux semantics evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 account-costume partial-table guard | Implemented | `AccountCostumeManager` now treats a missing costume unlock-limit `GameFormula` table/row like the documented missing-row fallback and uses the client default limit `1000`. `ForgetItem()` treats a missing `Item` table like a missing item row, sending `CostumeUnlockResult.InvalidItem` without deleting a known unlock. `AccountCostumeManagerTests` pin non-equippable unlock rejection, missing-formula unlock success/default limit, and missing-item-table forget rejection. Focused costume verification passed 7/7 from the alternate output directory. | Keep costume aux producer semantics, exact unlock/forget UI timing, deeper costume lifecycle parity, item/error aux producers, generic unlock deltas, and pet lifecycle timing evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 decor item-use partial-table guard | Implemented | `ClientItemUseDecorHandler` now treats a missing `HousingDecorInfo` table like invalid decor static data and throws before residence resolution, `Inventory.ItemUse`, or `DecorCreate`. `ClientItemUseDecorHandlerTests` pin the missing-table no-side-effect boundary alongside blocked residence, empty stack, consume failure, and success ordering. Focused decor item-use verification passed 5/5, nearby item-use verification passed 18/18, and focused F-026 item/unlock/costume/pet bucket passed 41/41 from the alternate output directory. | Keep item/error aux producers, remaining item eligibility precision, supply-satchel/costume aux producer semantics, pet lifecycle timing, pet flair/object/name validation, generic unlock deltas, and deeper costume lifecycle parity evidence-gated. |
| 2026-06-08 | F-022 / audit F-026 quest guidance partial-table guard | Implemented | `Quest` objective guidance now guards missing `QuestDirection` and `QuestDirectionEntry` tables. Missing direction tables resolve objective world-location packets to `WorldLocation2Id = 0` through the existing unresolved path, while table-backed single-entry directions still resolve normally. `QuestTests` pin missing-table, missing-entry-table, and happy-path direction resolution. Focused quest verification passed 284/284 from the alternate output directory. | Keep exact objective guidance UI behavior, direction ambiguity semantics, receiver/visibility content smoke, virtual loot cadence/probability, and wider quest/path public-event content parity evidence-gated. |
| 2026-06-08 | F-022 / audit F-026 quest-info reward partial-table guard | Implemented | `QuestInfo` now guards missing `Quest2Difficulty`, `Quest2`, `QuestObjective`, `Quest2Reward`, `XpPerLevel`, and reward-money `GameFormula` rows. Missing reference/reward tables produce empty quest-info collections, missing computed reward dependencies return zero, and explicit XP/cash overrides remain table-independent. `QuestInfoTests` pin missing-table construction, table-backed XP/money rewards, missing computed dependencies, and override behavior. Focused quest verification passed 281/281 from the alternate output directory. | Keep exact reward presentation, quest reward pane timing, content receiver/visibility smoke, virtual loot cadence/probability, and wider quest/path public-event content parity evidence-gated. |
| 2026-06-08 | F-022 / character-XP partial-table guard | Implemented | `XpManager` now guards missing `XpPerLevel` data across level resolution, XP grant level-up checks, GM `SetLevel`, and rested-XP span lookup. Missing next-level rows still allow earned XP while preventing unsupported level mutation, and missing target-level rows no-op before session/progression side effects. `XpManagerTests` pin missing-table, missing-next-level, and missing-target-level cases. Focused XP/rest-XP verification passed 237/237 from the alternate output directory. | Keep exact quest/path reward presentation, spell-derived XP effect precision, rest-XP persistence/login edge semantics, level-up unlock/LAS/AMP side effects, and broader content smoke evidence-gated. |
| 2026-06-08 | F-022 / audit F-027 path unlock/change formula guard | Implemented | `ClientPathUnlockHandler` and `ClientPathChangeRequestHandler` now guard missing service-token `GameFormula` rows `2365`/`2366`, sending `GenericError.ItemBadStaticData` through the existing path result packets before account-currency checks/debits, unlock mutation, or path activation. `PathSelectionHandlerTests` pin both fail-closed boundaries plus existing happy paths. Focused path-selection verification passed 4/4 from the alternate output directory. | Keep exact unlock sequencing, cooldown UI cadence, path reward presentation, reward-history persistence, generic Soldier/Scientist/Settler path semantics, and broader content smoke evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 supply-satchel unmapped-material guard | Implemented | `SupplySatchelManager` now returns the full remainder instead of throwing when an inventory item or material id cannot be mapped to `TradeskillMaterial`, reports unmapped items as full before `Inventory.ItemMoveToSupplySatchel` mutates inventory, and no-ops material-to-inventory conversion when a cached material has no live material entry. Tests pin mapped item conversion, unmapped item/material remainders, and stale conversion alongside existing stack-limit guards. Focused supply-satchel verification passed 8/8 from the alternate output directory. | Keep item/error aux producer semantics, remaining item eligibility precision, supply-satchel aux producer semantics, unlock deltas, pet flair/object/name validation, and pet lifecycle timing evidence-gated. |
| 2026-06-08 | F-022 / audit F-027 path-level partial-table guard | Implemented | `PathManager` now uses guarded current/target `PathLevel` lookups. Missing current-level rows skip XP and level-reward mutation without aborting mission completion or supported mission reward grants, while missing target-level rows make `AddLevels` fail closed after any known outstanding reward check. `PathManagerTests` pin missing target-level and missing path-level mission-completion cases. Focused path-manager verification passed 68/68 from the alternate output directory. | Keep exact path reward presentation, overflow/flag semantics, reward-history persistence, generic Soldier holdout wave simulation, generic Scientist scan-result/minigame packets, Settler built-group/resource/avenue state, exact unlock sequencing, and broader content smoke evidence-gated. |
| 2026-06-08 | F-009 / audit F-012 rapid-transport and flight-path partial-table guards | Implemented | `ClientRapidTransportHandler` treats missing `GameFormula` data or a zero rapid-transport spell id as `CastResult.RapidTransportInvalid`, returning before spell cooldown lookup, currency debit, or `CastSpell`. `TransportHandlerTests` now pin missing-formula and zero-spell cases plus existing `TaxiNode` / `WorldLocation2` table-unavailable rapid-transport rejection before debit/cast and flight-path rejection before debit/teleport, alongside table-backed route coverage. Focused `TransportHandlerTests` verification passed 17/17, and broader transport verification passed 77/77 from the alternate output directory. | Keep service-token bypass, global route-state snapshots, taxi embark/completion timing, broader charge/teleport parity, passenger/seat modes, deployable vehicle semantics, and `Server0x077E` semantics evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 pet customisation invalid type/slot guard | Implemented | `ClientPetCustomisationHandler` now sends `ServerPetCustomisationFailed` with `PetTypeNotSupported` for unsupported pet type enum values and `InvalidSlot` for flair slot indexes outside `PetCustomisationManager.MaxCustomisationFlairs` before `IPetCustomisationManager.AddCustomisation`. Tests pin valid delegation and both failure packets. Focused pet verification passed 282/282 from the alternate output directory. | Keep flair ownership, object/name validation, pet lifecycle producer timing/scope, and blocked `ServerPetStanceChanged` response timing evidence-gated. |
| 2026-06-08 | F-006 / audit F-008 account-item terminal missing-inventory guard | Implemented | `ClientAccountItemHandlers` now resolve the account inventory manager before account-item take, pending claim/return/gift, and reserved-zero gift refresh paths. Missing inventory sends the existing `ServerAccountOperationResult` with `GenericFail` and stops before manager mutation, account persistence, pending refresh, or character-list refresh. `ClientDailyLoginClaimRewardHandler` uses the same guard, and `CouponRedemptionService` returns `GenericFail` before coupon item validation/grant when account inventory is unavailable. `AccountItemHandlerTests` and `AccountTerminalHandlerTests` pin take, pending-claim, daily-login, and coupon fail-closed boundaries. Focused handler verification passed 20/20, and broader account-inventory verification passed 83/83 from the alternate output directory. | Keep Protobucks/VC request-confirm flow, non-empty `0x026A` owned-order tails, live `0x0986`/`0x098F` emits, non-zero `096A..096C` producers, type-1/type-2 offer-item effects, and coupon native sender proof evidence-gated. |
| 2026-06-08 | F-004 housing community-rename formula-table guard | Implemented | `ClientHousingCommunityRenameHandler` now resolves rename cost formula `2395` through a nullable `GameFormula` table lookup, reuses the resolved entry for the debit, and returns `HousingResult.Failed` before currency checks/debits or community/residence rename when the table is absent. `ClientHousingCommunityRenameHandlerTests` pin the missing-table no-side-effect boundary and a table-backed credit-cost success path. Focused rename verification passed 3/3 and broader housing verification passed 104/104 from the alternate output directory. | Keep neighborhood `0x0501`/`0x0506` producer triggers/fields, edit-mode ack/broadcast, community/session precision, decor ownership/unlock/refund semantics, and exact community rename persistence/UI timing evidence-gated. |
| 2026-06-08 | F-004 housing vendor-list partial-table guard | Implemented | `ClientHousingVendorListHandler` now treats a missing `HousingPlugItem` table like an empty vendor source and a missing `HousingContributionInfo` table like missing contribution rows, keeping costs at `0` instead of throwing. `ClientHousingVendorListHandlerTests` pin missing plug-table empty-list, missing contribution-table zero-cost, and table-backed first-positive cost behavior. Focused vendor-list verification passed 3/3 and broader housing verification passed 107/107 from the alternate output directory. | Keep plug ownership/entitlement/pricing precision, neighborhood `0x0501`/`0x0506` producer triggers/fields, edit-mode ack/broadcast, community/session precision, and exact vendor UI timing evidence-gated. |
| 2026-06-08 | F-011 / audit F-016 guild create-cost formula-table guard | Implemented | `ClientGuildRegisterHandler` now resolves guild/community create-cost formulas `764`/`1159` through nullable `GameFormula` lookups, returns `GuildResult.UnableToProcess` before affordability checks, currency debits, or `IGuildManager.RegisterGuild` when the table/row is absent, and reuses guarded lookups for post-success deduction. `ClientGuildRegisterHandlerTests` pin missing-table no-side-effect behavior across guild credit, community credit, and community token branches plus table-backed guild credit and community token success. Focused guild-register verification passed 5/5 and broader guild verification passed 21/21 from the alternate output directory. | Keep guild bank economy/influence/tab state, perks, holomarks, standards, recruitment subscriptions/details, war-party/warplot behavior, boss-token inventory, and exact create UI timing evidence-gated. |
| 2026-06-08 | F-035 / audit F-042 Steam achievement ingest boundary | Blocked | Source/protocol recheck keeps `ClientSteamAchievements` diagnostic-only. The packet model reads `SteamGameId`, byte length, and ASCII `AchievementData`; `ClientSteamAchievementsHandler` only logs the game id/data length and tests assert no achievement packets, `CheckAchievements`, or `SetAchievementProgress` calls. Focused Steam achievement verification passed 2/2 and focused achievement verification passed 25/25 from the alternate output directory. | Map Steam payload grammar and achievement-id correlation before ingesting client Steam data; keep data-only/unowned achievement type families and exact realm-first UI/timing evidence-gated. |
| 2026-06-08 | F-036 / audit F-043 zone-completion non-title reward source | Blocked | Source/table recheck found only the title reward surface: `ZoneCompletionEntry` exposes `CharacterTitleIdReward` and no non-title reward payload, while `ZoneMapManager.TryGrantZoneCompletionRewards()` delegates to `ZoneCompletionRewardResolver.TryGetTitleReward()` and only calls `TitleManager.AddTitle`. Focused zone-completion verification passed 12/12 and broader map verification passed 457/457 from the alternate output directory. | Decode non-title reward sources, path-specific completion semantics, live UX/timing, category-total validation, and generic-map content-context producer semantics before awarding broader zone-completion rewards. |
| 2026-06-08 | F-033 / audit F-040 challenge reward-track, medal, and share-init semantics | Blocked | Source/protocol recheck found no safe runtime hook for the remaining reward/share edges: `ChallengeEntry.RewardTrackId` and `RewardTrack*` tables exist, but no challenge reward-track manager or packet surface is mapped; `ServerChallengeUpdate` writes tier/count/timer fields, `ServerChallengeResult.Data` writes only the mapped tier/localized-string value, and `ShareWithTarget()` has no mapped client share-init request. Focused challenge verification passed 45/45 from the alternate output directory. | Decode or capture challenge reward-track/medal transport and a client share-init request before adding reward rolls or exposing share initiation; keep `Client0x00C8` and full result/reward sequence evidence-gated. |
| 2026-06-08 | F-032 / audit F-039 leaderboard medal/season filter semantics | Blocked | Source/protocol recheck found no mapped season or medal request selector: `ClientLeaderboardPveRequest` reads only type/map/prime, `ClientLeaderboardPvpRequest` reads only type, and `LeaderboardCategoryRules`/`DatabaseLeaderboardStore` scope by those fields. `LeaderboardPlayerPve.RewardedTier` remains response-row data, not proof of a bronze/silver/gold request filter. Focused leaderboard verification passed 17/17 from the alternate output directory. | Decode or capture a season/medal selector before adding filters; keep exact row caps/refresh cadence and broader live score-ingestion hooks evidence-gated. |
| 2026-06-08 | F-031 / audit F-038 Fortune reward-pool partial-table guard | Implemented | `FortuneRewardPool` now treats a missing `AccountItem` table as an empty catalog/card pool and a missing `Item2` table like a missing item row with `RewardRarity.Normal` fallback for otherwise item-backed rewards. `FortuneRewardPoolTests` pin both table-unavailable paths plus the existing item-backed/non-item reward boundary. Focused reward-pool verification passed 6/6, and broader Fortune verification passed 27/27 from the alternate output directory. | Keep exact per-item retail weights, money/probability arrays, active rotation catalog, and card-safe non-item reward payloads blocked until retail `ServerFortuneRewards`, storefront-server catalog, or native/server producer evidence exists. |
| 2026-06-08 | F-031 / audit F-038 Fortune flip missing-inventory guard | Implemented | `FortuneSessionManager.FlipCard()` now verifies reward delivery is possible before mutating card state. When an account lacks an inventory manager and the selected card still needs an account-item grant, the handler sends the mapped click-empty reset and leaves the card unflipped without emitting `ServerFortuneCardUpdate`. `FortuneSessionManagerTests.FlipCard_WithoutInventoryManagerSendsResetWithoutFlipping` pins the fail-closed boundary. Focused Fortune-session verification passed 14/14, and broader Fortune verification passed 25/25 from the alternate output directory. | Keep exact per-item retail weights, money/probability arrays, active rotation catalog, and card-safe non-item reward payloads blocked until retail `ServerFortuneRewards`, storefront-server catalog, or native/server producer evidence exists. |
| 2026-06-08 | F-030 / audit F-037 realm-transfer known-character guard | Implemented | `ClientRealmTransferHandler` now returns `RealmTransferFailed_InvalidCharacter` when the session has a logged-in `Player` or loaded character list and the requested `ClientRealmTransfer.CharacterId` is not known to that session. Sessions without loaded character data keep the existing compatibility fallback, and unknown/offline/online target realm results remain unchanged. `RealmTransferProtocolTests` pin both character-list and logged-in-player mismatch cases. Focused realm-transfer verification passed 14/14, and broader pregame/realm verification passed 44/44 from the alternate output directory. | Decode real destination eligibility/success results, `TransferFlag` semantics, online handoff/session mutation, `0x06EA` producer timing, PTR copy mutation, and `0x03EF` payload semantics before implementing transfer or copy state changes. |
| 2026-06-08 | F-036 / audit F-043 zone-completion stale table-cache guard | Implemented | `ZoneCompletionProgressTracker` now recomputes `EpisodeQuest` and `MapZone`/`WorldZone` progress inputs from the current `GameTableManager` on each title-reward lookup, so a missing/partial table provider cannot cache an empty result and suppress progress after a later complete provider refresh. `WorldZone` parent traversal is cycle guarded. `ZoneCompletionRewardResolverTests.TryGetTitleReward_RechecksMapZoneTablesAfterPreviousMissingLookup` pins the provider-refresh regression. Focused zone-completion verification passed 12/12, and broader map verification passed 443/443 from the alternate output directory. | Decode non-title reward sources, path-specific completion semantics, live UX/timing, and category-total validation before awarding broader zone-completion rewards. |
| 2026-06-08 | F-034 / audit F-041 persisted datacube duplicate-row merge guard | Implemented | `DatacubeManager` now merges duplicate persisted datacube rows for the same id/type with a progress bitmask union instead of throwing during character load. `PathManagerTests.DatacubeManager_Constructor_MergesDuplicatePersistedRows` pins the duplicate-row boundary. Focused datacube-manager verification passed 3/3, and archive/datacube verification passed 28/28 from the alternate output directory. | Verify datacube/journal pickup chains, decode remaining Codex progression/link UX semantics, and reconcile path-mission archive rules before claiming full archive parity. |
| 2026-06-08 | F-032 / audit F-039 leaderboard duplicate-score dedupe guard | Implemented | `DatabaseLeaderboardStore` and `LeaderboardAggregation` now sort duplicate persisted rows for the same character by best PvE completion time or best PvP rating, keep only the best row before per-scope/category caps, and preserve one visible response row/rank per character. `LeaderboardProviderTests` pin best-row retention in store caps, and `LeaderboardAggregationTests` pin best-row retention in PvE/PvP responses. Focused leaderboard verification passed 17/17 from the alternate output directory. | Keep season rules, medal-filter semantics, exact retail row caps/refresh cadence, and broader live score-ingestion hooks evidence-gated. |
| 2026-06-08 | F-035 / audit F-042 achievement checklist-bit mask guard | Implemented | `AchievementProgressRules.TryBuildChecklistBit()` now rejects checklist bit indexes outside the 32-bit progress mask, so `Achievement.IsComplete()`, direct grants, checklist progress, and checklist-count crediting no longer alias bit 32+ to bit 0. `AchievementProgressTests.IsComplete_DoesNotAliasChecklistBit32ToBit0` and `QuestCompleteChecklistCount_IgnoresChecklistBitsOutsideMask` pin the boundary. Focused achievement verification passed 25/25 from the alternate output directory. | Keep Steam payload grammar, achievement-id correlation, data-only/unowned achievement families, exact realm-first UI/timing, and wider achievement UI edge cases evidence-gated. |
| 2026-06-08 | F-033 / audit F-040 challenge progress overflow guard | Implemented | `ChallengeManager.TryAdvanceProgress()` now widens progress accumulation before clamping to the current tier goal, preventing large `uint` progress deltas from wrapping below the goal. `ChallengeManagerTests.TryAdvanceProgress_LargeProgressClampsAtGoalWithoutWrapping` pins that a near-`uint.MaxValue` count completes cleanly and still emits the quest challenge-completion hook. Focused challenge-manager verification passed 12/12 from the alternate output directory. | Keep `Client0x00C8`, share-init ownership, reward-tier/medal win-chance payloads, and live objective/reward sequence evidence-gated before claiming full retail challenge parity. |
| 2026-06-08 | F-005/F-013 auction bidder-refund offline-credit guard | Implemented | Auction bidder refunds now compose with bid updates, auction deletes, or item-return mail saves, and the old `CreditCharacter` marketplace fallback was removed. `MarketplaceAuctionHandlerTests.AuctionBid_WithOfflinePreviousBidderAndNoCharacterDatabase_RestoresNewBidAndRefundsBidder` and `AuctionCancel_WithOfflineTopBidderAndNoCharacterDatabase_DoesNotRefundReturnOrRemoveAuction` pin that no-DB offline-bidder refunds leave auctions active before new-bid acceptance, seller item return, or bidder refund loss. Focused auction verification passed 51/51 and marketplace/mail verification passed 86/86 from the alternate output directory. | Keep marketplace aux `0x06DF`/`0x07D5`, CREDD owned-order tails, advanced retail filters, live DB/order-book validation, and commodity partial-stack precision evidence-gated. |
| 2026-06-08 | F-005/F-013 commodity-fill offline-credit guard | Implemented | Commodity fills now compose resting order update/delete with offline seller proceeds and offline buyer price-improvement refund credit mail in one required character DB save. `MarketplaceAuctionHandlerTests.CommodityForceImmediateBuy_WithOfflineSellerAndNoCharacterDatabase_LeavesSellOrderUnfilled` pins that no-DB fills requiring an offline seller credit leave the match unfilled before buyer item delivery, seller credit, fill notifications, or order removal. Focused auction verification passed 49/49 and marketplace/mail verification passed 84/84 from the alternate output directory. | Auction bidder refunds were the remaining source-local settlement-credit path; keep marketplace aux `0x06DF`/`0x07D5`, CREDD owned-order tails, advanced retail filters, and commodity partial-stack precision evidence-gated. |
| 2026-06-08 | F-005/F-013 commodity buy-order offline expiration refund guard | Implemented | Expired offline commodity buy orders now compose commodity-order deletion and refund credit mail in one required character DB save. `MarketplaceAuctionHandlerTests.CommodityBuyOrderExpire_WithOfflineOwnerAndNoCharacterDatabase_DoesNotRefundOrRemoveOrder` pins that no-DB offline-owner expirations leave the order active before escrow is lost. Focused auction verification passed 48/48 and marketplace/mail verification passed 83/83 from the alternate output directory. | Auction bidder refunds were the remaining source-local settlement-credit path; keep marketplace aux `0x06DF`/`0x07D5`, CREDD owned-order tails, advanced retail filters, and commodity partial-stack precision evidence-gated. |
| 2026-06-08 | F-005/F-013 item-auction offline seller settlement guard | Implemented | Item-auction sale persistence now composes auction row deletion, moved item state, and offline seller credit mail in one required character DB save for direct winner-inventory delivery and auction-won mail delivery. `MarketplaceAuctionHandlerTests.AuctionBuyout_WithOfflineSellerAndNoCharacterDatabase_DoesNotDeliverDebitOrRemoveAuction` and `AuctionExpire_WithOnlineWinnerOfflineSellerAndNoCharacterDatabase_DoesNotDeliverCreditNotifyOrRemoveAuction` pin that no-DB offline-seller sales stay pending before buyer delivery/debit, seller credit, winner notification, or auction removal. Focused auction verification passed 47/47 and marketplace/mail verification passed 82/82 from the alternate output directory. | Auction bidder refunds were the remaining source-local settlement-credit path; keep marketplace aux `0x06DF`/`0x07D5`, CREDD owned-order tails, advanced retail filters, and commodity partial-stack precision evidence-gated. |
| 2026-06-08 | F-026 / audit F-033 costume unlock non-equippable guard | Implemented | `AccountCostumeManager.UnlockItem()` now rejects missing/non-equippable inventory items with `CostumeUnlockResult.InvalidItem` before account unlock creation, item soulbind, achievement update, or success result. `AccountCostumeManagerTests.UnlockItem_WithNonEquippableItemSendsInvalidWithoutUnlockingOrSoulbinding` pins the boundary. Focused F-026 verification passed 34/34 from the alternate output directory. | Keep item/error aux producers, supply-satchel/costume aux producer semantics, pet lifecycle response timing, generic unlock deltas, and deeper costume lifecycle parity evidence-gated. |
| 2026-06-08 | F-027 / audit F-034 keybinding character-scope guard | Implemented | `InputKeySetScopeValidator` now requires non-zero keybinding `CharacterId` values in `ClientRequestInputKeySet` and `BiInputKeySet` to match the logged-in player before character keybinding readback/update. `OptionHandlerTests.RequestInputKeySet_WithDifferentCharacterIdThrowsBeforeReadback` and `UpdateInputKeySet_WithDifferentCharacterIdThrowsBeforeMutatingManagers` pin that mismatched character ids throw before manager mutation/build or response enqueue. Focused option verification passed 35/35 and the combined option/support bucket passed 160/160 from the alternate output directory. | Keep additional option types, broader account-vs-character option ownership/readback, and item/options aux `0x056B..0x056D` producer semantics evidence-gated; support case workflow/readback remains product/backend evidence-gated. |
| 2026-06-08 | F-014 / audit F-019 loot-bag single-stack delete-failure guard | Implemented | `GlobalLootManager.TryUseLootBag()` now catches source-item delete exceptions for single-stack no-charge loot bags and returns `item-delete-failed` before generated reward delivery. `LootBagUsageTests.TryUseLootBag_SingleStackDeleteFailureDoesNotGrantLoot` pins that no currency or granted-loot packets are emitted after the failed delete. Focused loot-bag verification passed 11/11 and broader loot verification passed 87/87 from the alternate output directory. | Keep standalone `ServerLootCanLoot`, exact parent/source selection, bind-on-pickup confirmation, and roll/master UI parity evidence-gated. |
| 2026-06-08 | F-004 / audit F-004 housing decor-move scale guard | Implemented | `ResidenceMapInstance.DecorMove()` now rejects negative scale before updating decor fields, calling `IDecor.Move`, or broadcasting a residence-decor update. `ResidenceMapInstanceInteriorWallpaperTests.DecorMove_NegativeScale_ThrowsBeforeMutatingDecor` pins the handler-facing boundary. Focused verification passed 4/4 from the alternate output directory. | Keep edit-mode ack/broadcast, neighborhood `0x0501`/`0x0506` producer triggers/fields, community/session precision, and decor ownership/unlock/refund semantics evidence-gated. |
| 2026-06-08 | F-029 / audit F-036 DataMapping residual-classification guard | Implemented | `Tools/DataMapping/test_laughingws_worlddb_classifications.py` now covers the current WorldDB analyzer residual buckets, not only broad new-zone/test-zone/tutorial replacement decisions: Algoroc, Celestion, Galeras, Thayd, Whitevale, Deradune, Ellevar, Illium, Everstar Grove, Northern Wilds, Auroria, Crimson Isle, Levian Bay, Wilderrun, and Northern Wilds duplicate vendor rows are pinned as blocked/rejected or partial-seed-covered without `extract_additive_world_overlay`. Python syntax checks for the analyzer/row-review/test scripts passed, and the classifier regression ran 6/6 against the local external snapshot. | Keep WIP overlays and branch residual rows mapped-only/rejected until row-level client/runtime smoke or asset proof exists; do not promote broad replacement SQL from classifier coverage alone. |
| 2026-06-08 | F-005/F-013 marketplace offline-currency atomicity recheck | Mapped only | Source recheck confirms the remaining final settlement gap is offline currency, not item/mail delivery: `GlobalMarketplaceManager.CreditCharacter()` can send offline credit mail or fall back to `CharacterDatabase.CreditCharacterCurrency()` after marketplace row/item mutation, and `CreditCharacterCurrency()` opens a fresh `CharacterContext`/`SaveChanges` outside composed marketplace/mail saves. Existing local hardening still covers auction/commodity delete-save guards, item-owner restoration on mail failures, and same-save mail/order/item settlement. Focused marketplace/mail verification passed 72/72 from the alternate output directory. | Design and test a composable offline currency-credit transaction path before claiming final marketplace settlement atomicity; keep marketplace aux `0x06DF`/`0x07D5`, CREDD owned-order tails, advanced retail filters, and commodity partial-stack precision evidence-gated. |
| 2026-06-08 | F-036 / audit F-043 zone-map partial-table guards | Implemented | `ZoneMapManager`, `ZoneMap`, and `ZoneCompletionProgressTracker` now fail closed when map/progress tables are unavailable during setup or partial game-table loads: missing persisted `MapZone` rows are skipped, missing hex-group tables leave maps incomplete with 0% explored, and missing quest/datacube/volume/world-zone tables resolve as no completion progress instead of crashing or granting titles. `ZoneCompletionRewardResolverTests` pin missing progress-table, hex-table, and persisted-map-row boundaries. Focused zone verification passed 13/13, and broader `FullyQualifiedName~Map` verification passed 441/441 from the alternate output directory. | Decode non-title reward sources, path-specific completion semantics, live UX/timing, and category-total validation before awarding broader zone-completion rewards. |
| 2026-06-08 | F-034 / audit F-041 Galactic Archive partial-table guards | Implemented | `GalacticArchiveManager` now fails closed on partial archive table loads: missing `ArchiveArticle` skips persisted rows and rejects unlock/view requests, missing `ArchiveEntryUnlockRule` suppresses rule auto-unlocks during login refresh, and missing `ArchiveEntry` skips entry-title rewards while preserving article unlock flags. `GalacticArchiveManagerTests` pin missing article/rule/entry table behavior, and focused archive/game-table verification passed 91/91 from the alternate output directory. | Verify datacube/journal pickup chains, decode remaining Codex link/progression UX, and reconcile path-mission archive rules before claiming full archive parity. |
| 2026-06-08 | F-033 / audit F-040 challenge missing-table guards | Implemented | `ChallengeManager` now uses nullable challenge/tier table lookups: missing `Challenge` data returns `GenericFail`, and missing `ChallengeTier` data produces zero tier goals instead of throwing during update construction. `ChallengeManagerTests.Activate_WhenChallengeTableMissing_SendsGenericFail` and `Activate_WhenChallengeTierTableMissing_SendsUpdateWithZeroGoal` pin the partial-table boundary. Focused challenge verification passed 44/44 from the alternate output directory. | Decode `Client0x00C8`, share-init ownership, reward-tier/medal win-chance payloads, and live objective/reward sequence before claiming full retail challenge parity. |
| 2026-06-08 | F-032 / audit F-039 leaderboard database-unavailable cache guard | Implemented | `DatabaseLeaderboardStore` now returns empty PVE/PVP views without populating `cachedPve`/`cachedPvp` when `CharacterDatabase` is unavailable, so startup/setup requests do not permanently suppress later DB hydration. `LeaderboardProviderTests.DatabaseStore_WhenDatabaseUnavailable_ReturnsEmptyWithoutCaching` pins the retryable boundary. Focused leaderboard verification passed 13/13 from the alternate output directory. | Decode season rules, medal-filter semantics, exact retail row caps/refresh cadence, and live score-ingestion hooks before claiming full retail leaderboard parity. |
| 2026-06-08 | F-028 / audit F-035 support stuck missing-table guard | Implemented | `ClientStuckHandler.RecallToZoneExit()` now treats a missing `WorldLocation2` table the same as a missing exit row, sending `ServerSpellCastResult` with `SpellPreRequisites` before teleport or cooldown mutation. `SupportStuckHandlerTests.RecallTransmat_WhenWorldLocationTableMissingSendsSpellPreRequisitesResult` pins the setup/partial-table guard. Focused `FullyQualifiedName~Support` verification passed 125/125 from the alternate output directory. | Decode support-case readback/result semantics, remaining cooldown display precision, durable case lifecycle, moderation workflow, and report/survey admin tooling before widening support behavior. |
| 2026-06-08 | F-026 / audit F-033 supply-satchel stack-limit precision | Implemented | `SupplySatchelManager` now treats a missing, invalid, or non-positive `TradeskillMatStackLimit` reward property as the default 100-material cap, clamps oversized caps to `ushort.MaxValue`, returns the full remainder without mutation or update when a saved material count is already at/above cap, and skips material ids outside the fixed 512-slot login packet. `SupplySatchelManagerTests` pin the fallback, over-cap, clamp, and packet-capacity cases. Focused supply-satchel verification passed 4/4 from the alternate output directory. | Capture `ServerSupplySatchelAux` producer semantics and broader material/item eligibility edges before widening satchel aux behavior or claiming full retail satchel parity. |
| 2026-06-08 | F-025 / audit F-032 realm-bank database-unavailable load-cache guard | Implemented | `RealmBankManager.EnsureLoaded()` now resolves `CharacterDatabase` before adding the `(AccountId, RealmId)` loaded key, so a missing DB during setup/startup does not permanently suppress later realm-bank hydration for that account/realm. `RealmBankInventoryTests.EnsureLoaded_WithoutDatabase_DoesNotMarkAccountRealmLoaded` pins the guard. Focused realm-bank verification passed 8/8 from the alternate output directory. | Capture item-location converter values and retail realm-bank open/close/snapshot packet behavior before adding new packet types or claiming exact open-snapshot parity. |
| 2026-06-08 | F-006 / audit F-007 storefront purchase offer-item type guard | Implemented | `StorefrontPurchaseService.TryBuildAccountItemList()` now rejects non-type-0 catalog offer-item rows with `StoreError.CannotUseOffer` before account-currency debit, purchase-history record, success emit, or account-item delivery. `StorefrontPurchaseHandlerTests.AccountPurchase_WithUnsupportedOfferItemType_ReturnsCannotUseOfferAndDoesNotCharge` pins the guard while preserving catalog transport for type-1/type-2 rows. Focused storefront/CREDD verification passed 27/27 from the alternate output directory. | Map retail type-1/type-2 offer-item purchase effect semantics, Protobucks/VC request-confirm flow, coupon native sender, `0x026A` owned-order tail, `0x0986`/`0x098F`, and non-zero `096A..096C` producers before widening purchase behavior. |
| 2026-06-07 | F-031 / audit F-038 Fortune target-scoped coin auto-claim guard | Implemented | `FortuneSessionManager.Start()` already auto-claims a matching `CanClaim` Fortune Coin account-item bundle before re-checking the one-coin start cost. `FortuneSessionManagerTests.Start_WithMismatchedTargetFortuneCoinItemSendsResetWithoutClaimingBundle` now pins the character-target boundary: a Fortune Coin bundle targeted at another character is not claimed, no currency is added or debited, the inventory row remains, and the client receives the mapped click-empty reset. Focused Fortune verification passed 24/24 from the alternate output directory. | Keep exact per-item retail weights, active rotation catalog, money reward arrays, and non-item card rewards blocked until retail `ServerFortuneRewards`, storefront catalog, or native/server producer evidence exists. |
| 2026-06-07 | F-015 / audit F-020 PvP duel, cooldown, and open-world boundary recheck | Mapped only | Duel lifecycle and PvP flagging core are already implementation-backed: challenge/countdown/start, pending timeout, decline/cancel zero winner/loser ids, active disconnect cancellation, defeat/forfeit achievement credit, leash left-area/cancel-warning behavior, durable PvP toggle-off cooldown persistence, `ServerPvpCooldownUpdate`/`ServerPvpCooldownClear`, `ServerUnitPvpStateChange`, and active-duel-only open-world `Player.CanAttack` are covered. Focused PvP/adventure verification passed 29/29 from the alternate output directory. | Add observer broadcasts, broader forced-map/open-world PvP rules, arena/battleground score/stat/reward parity, and PvP adventure match behavior only after native/live producer proof or completed queue/match smoke bundles. |
| 2026-06-07 | F-012 / audit F-017 ICComm, friendship social option, and chat aux boundary recheck | Mapped only | ICComm transient scoped-channel behavior is implementation-backed: offline/stale group/guild members are pruned before delivery, directed recipient delivery is filtered by current membership, auto-response messages remain transient player state, ignore-strangers echoes the transient client flag, and chat aux `0x01B8`/`0x01C1`/`0x01C4`/`0x01EF` packet shapes are covered. Focused ICComm/friendship/chat-aux verification passed 12/12 from the alternate output directory. | Add persistence, entitlement/rate gates, reconnect restore, exact leave/logout signalling, and chat aux producer/runtime semantics only after two-client smoke, backend workflow proof, or native/live producer evidence. |
| 2026-06-07 | F-011 / audit F-016 guild bank request boundary | Mapped only | `ClientGuildBankMoneyTransaction`, `ClientGuildBankTransaction`, `ClientGuildBankTransaction2`, and `ClientGuildBankTabOpen` remain log-only request surfaces with no plaintext or encrypted bank-state emit while guild bank economy, item movement, influence, and tab backing state are blocked. `ServerGuildBankTabInventory` keeps its mapped identity/tab/count shape. Focused guild bank verification passed 7/7 from the alternate output directory. | Model/persist guild influence, bank-tab count/state, item slots, permissions/limits, and money transaction semantics before mutating bank state or emitting tab inventory responses from client requests. |
| 2026-06-07 | F-010 / audit F-015 matching replacement boundary recheck | Mapped only | Replacement request handling is already implementation-backed at the safe boundary: `ClientMatchingMatchInitiateLookingForReplacements` and `ClientMatchingStopLookingForReplacements` validate in-progress match membership, accept only native role bits `0..2`, and intentionally avoid starting a server backfill queue or emitting blocked replacement/status packets. Focused replacement/packet/raid-info verification passed 40/40 from the alternate output directory. | Capture or map retail replacement queue anchor timing, accepted replacement merge, teleport into the running instance, and multi-slot fill sequencing before implementing server backfill/merge. |
| 2026-06-07 | Placeholder inventory prerequisite/objective enum guard | Mapped only | Current source still has exactly 24 `PrerequisiteType.UnknownNNN` entries and 2 quest objective placeholders (`QuestObjectiveType.Unknown27`/`Unknown29`). `PrerequisiteTypeNamingTests` already grouped the prerequisite leftovers by duplicate-body alias, orphan helper, skipped dispatcher, no-op slot, or diagnostic field-owner blocker; `QuestObjectiveTypeNamingTests` now pins the two quest objective placeholders as unknown until objective-data ownership is proven. Focused naming verification passed 119/119 from the alternate output directory. | Keep the remaining prerequisite and quest objective placeholders neutral until native handler ownership, table row references, objective text/data semantics, or live script/update evidence proves a safe rename/runtime path. |
| 2026-06-07 | F-003 / audit F-003 `Server0x0015` shared-reader false-rename guard | Mapped only | Source/tests already pin the structural boundary: `Server0x0015` writes the mapped 5-bit plus `uint32` shape, remains a neutral numeric placeholder with `Value0`/`Value1`, and does not inherit `ServerMatchingAverageWaitTimeUpdate.Type/AverageWaitTime` even though opcode `0x0628` uses the same native reader. Focused placeholder/matching verification passed 116/116 from the alternate output directory. No behavior change is evidence-safe because prior pass 151 found only shared reader/registration/Fortune row-helper evidence and no opcode-specific `0x0015` apply owner or producer. | Keep `Server0x0015` neutral and non-emitted until a native apply/producer path, client post-read consumer, or live `0x0015` payload capture proves semantics. |
| 2026-06-07 | F-002 / audit F-002 `Client0x0701` diagnostic false-rename guard | Mapped only | Source/tests already pin the safe boundary: `Client0x0701` reads the mapped 2-bit plus `uint32` payload, remains numeric through `PacketPlaceholderNamingTests`, and its handler is log-only with no plaintext or encrypted server emit. Focused diagnostic verification passed 106/106 from the alternate output directory. No behavior change is evidence-safe because pass 153 still found only registration evidence and no opcode-specific sender, post-read consumer, callback/table owner, indirect send rail, or live capture. | Keep `LeadingBits` and `TrailingValue` diagnostic-only until a native/live `0x0701` owner proves semantics; do not alias it to public-event, queue, or movement behavior from adjacency. |
| 2026-06-07 | F-007 / audit F-009 reward rotation claim delivery | Blocked | `RewardRotationGrantClaimService` records schedule-backed grant rows only; `RewardRotationScheduleBuilder` and `RewardRotationPerContentRewardCatalog` explicitly use deterministic catalog selection because client tables expose no direct content-to-reward linkage. Native findings map schedule rows and entry-state rows (`RewardRotation_ApplyServerScheduleUpdate`, `RewardRotation_ResolveActiveRewards`, `RewardRotation_ApplyEntryStateToLoadedContent`) but still leave the server-authored active schedule source and `0x07CD` flag/throttle assignment unproven. `RewardRotationItemEntry`, `RewardRotationEssenceEntry`, and `RewardRotationModifierEntry` contain deliverable item/currency/property fields, but using the deterministic local selector for payouts would promote compatibility schedule rows into authoritative reward grants without proof. | Capture retail/live `0x07CA`/`0x07CD` reward rows or map a server schedule producer/catalog source before item/currency/property delivery is implemented from reward-rotation claims. |
| 2026-06-07 | F-036 / audit F-043 zone completion tracker reconciliation | Mapped only | Source and tests show `ZoneCompletionRewardResolver` already selects Dominion/Exile `ZoneCompletionFactionEnum` rows and gates title rewards through `ZoneCompletionProgressTracker` counts for episode quests, task quests, challenges, datacubes, tales, and journals. `ZoneCompletionEntry` exposes `CharacterTitleIdReward` as the only reward payload, so the old broad blocker for faction/category integration was stale while non-title reward and path/live-UX semantics remain real blockers. Updated `CURRENT_STATUS.md`, `MISSING_FEATURE_MATRIX.md`, and `INITIAL_FINDINGS.md` to reflect this narrower state. | Decode a non-title reward source or path-specific/live-client zone-completion semantics before widening reward grants beyond title awards. |
| 2026-06-07 | F-033 / audit F-040 challenge concurrent cap and hook reconciliation | Implemented | Source and tests already enforce the documented retail cap: `ChallengeManager.TryActivate` rejects a third active challenge through `RetailCertainRules.MaxConcurrentActiveChallenges`, with `Activate_ThirdConcurrentChallenge_ReturnsGenericFail` pinning the behavior. The same source pass confirmed combat challenge progress is wired from `UnitEntity.RewardKiller` through `ChallengeCombatHooks.OnCreatureKilled`, direct and nested target groups are covered by focused tests, and challenge completion emits `QuestObjectiveType.CompleteChallenge`. Updated `CURRENT_STATUS.md`, `MISSING_FEATURE_MATRIX.md`, and `GAMEPLAY_ECONOMY_SOCIAL_STATUS.md` to remove stale missing-hook language. | Decode `Client0x00C8`, share-init ownership, reward-tier/medal win-chance payloads, and live objective/reward sequence before claiming full retail challenge parity. |
| 2026-06-07 | F-032 / audit F-039 leaderboard scope cache limiting | Implemented | `DatabaseLeaderboardStore` previously loaded a global top-800 table slice before request-scope filtering, so a busy PvE scope or PvP category could hide valid rows from another scope/category. The store now maps rows first and keeps the top 100 rows per PvE type/map/prime scope and per valid PvP category before provider ranking/personal-placement logic. Focused leaderboard verification passed 12/12 from an alternate output directory because a local `NexusForever.WorldServer` process locked the default build output. | Decode season rules, medal-filter semantics, exact retail row caps/refresh cadence, and live score-ingestion hooks before claiming full retail leaderboard parity. |
| 2026-06-07 | F-034 / audit F-041 archive-link client unlock authorization | Implemented | `ClientGalacticArchiveUnlock` no longer calls raw `UnlockArticle`; it delegates to `UnlockLinkedArticle`, which accepts only existing child articles whose `ArchiveLink.tbl` parent is already unlocked, while server-owned creature interaction still uses the direct unlock path. `ArchiveLink.tbl` is now default-loaded and covered by `GameTableManagerGameDataContractTests`; focused archive/game-table verification passed 55/55 from the alternate output directory because a local `NexusForever.WorldServer` process locked the default build output. | Verify datacube/journal pickup chains, decode remaining Codex link UX/progression semantics, and reconcile path-mission archive rules before claiming full archive parity. |
| 2026-06-07 | F-035 / audit F-042 achievement trigger and Steam boundary reconciliation | Mapped only | Source and tests confirm the achievement row is implementation-complete at the core manager/packet boundary: `BaseAchievementManager` owns checklist/value progress, prerequisites, scalar clamping, completion marking, and title grants; `GlobalAchievementManager` loads completed character/guild realm-first ids and claims each realm-first once per process before broadcasting the mapped packet; runtime callsites already cover kills/kill groups, quests/contracts, zone entry/map completion, activation/discovery/secret stash, crafting/tradeskills, costume/costume-set unlocks, reputation, level/path level, path mission/type, currency/account currency/primal essence, items/titles, duels, critical deathblows, guild/circle/group/friend joins, housing plug/decor actions, public-event objectives, and targeted emotes. `ClientSteamAchievementsHandler` remains diagnostic-only and tests assert no achievement mutation. | Map Steam payload grammar and achievement-id correlation before ingesting client Steam data; audit remaining data-only achievement type families only after their event source is server-owned; decode exact realm-first UI/timing semantics before changing broadcast behavior. |
| 2026-06-07 | F-028 / audit F-035 support stuck cooldown validation | Implemented | `ClientStuckHandler` now records stuck cooldown only after the selected server-owned action validates its local prerequisites: transmat validates the exit `WorldLocation2`, house recall resolves residence/entrance/map lock, and free suicide validates the player is alive. This keeps missing server destination data from burning the next valid stuck attempt while preserving the existing per-action cooldown after a successful action. Focused option/support verification passed 154/154 from the alternate output directory because a local `NexusForever.WorldServer` process locked the default build output. | Decode support-case readback/result semantics, remaining cooldown display precision, durable case lifecycle, moderation workflow, and report/survey admin tooling before widening support behavior. |
| 2026-06-07 | F-026 / audit F-033 generic unlock consume-failure result | Implemented | `ClientItemGenericUnlockHandler` now sends `GenericUnlockResult.Invalid` when a valid locked generic-unlock item reaches mutation but `Inventory.ItemUse` fails, and it grants no locked entries. This closes the one generic-unlock failure branch that previously logged only while the other invalid/set/already-unlocked paths returned deterministic results. Focused account/generic-unlock verification passed 25/25 from the alternate output directory because a local `NexusForever.WorldServer` process locked the default build output. | Decode account/character unlock-list delta timing, generic unlock persistence precision, and nearby item/account aux producer semantics before widening unlock behavior. |
| 2026-06-07 | F-005/F-013 / audit F-005/F-018 marketplace and mail settlement atomicity pass | Mapped only | Source and tests already cover the currently local hardening: online-inventory auction buyout/cancel/expiration delete-save gates, direct commodity cancel/expiration/fill gates, same-save auction/commodity mail settlement, marketplace mail content persistence, and item-owner restoration on mail persistence failure. The remaining "final settlement DB transaction/save atomicity" gap is narrower and still real: offline credit paths can fall back to `CharacterDatabase.CreditCharacterCurrency` after marketplace row mutation, which is outside the composed marketplace/mail save action. Closing it needs a broader currency-credit persistence abstraction or transaction composition, not a guessed packet/runtime behavior change. | Design and test a composable offline currency-credit transaction path before claiming final marketplace settlement atomicity; keep marketplace aux `0x06DF`/`0x07D5`, CREDD owned-order tails, and advanced retail filters evidence-gated. |
| 2026-06-07 | F-030 / audit F-037 realm transfer and PTR compatibility result pass | Implemented | `ClientRealmTransferHandler` now returns the mapped `RealmTransferFailed_InvalidRealm` result for unknown target realms instead of throwing, while offline targets still return `ServerDown` and online targets still return conservative `Internal`. `RealmTransferProtocolTests` also now pin both PTR-copy request handlers as non-emitting/diagnostic-only until `ServerPtrCharacterCopyQueued` producer timing and copy mutation are mapped. Focused realm-transfer/PTR verification passed 12/12 from the alternate output directory because a local `NexusForever.WorldServer` process locked the default build output. | Decode real destination eligibility/success results, `TransferFlag` semantics, online handoff/session mutation, `0x06EA` producer timing, PTR copy mutation, and `0x03EF` payload semantics before implementing transfer or copy state changes. |
| 2026-06-07 | F-027 / audit F-034 option keybinding clear-before-save mutation | Implemented | `KeybindingSet.Update` now materializes stale input-action ids before mutating the backing dictionary, fixing the pending-created binding clear-before-save path that could invalidate `bindings.Keys.Except(...)` enumeration. `KeybindingSetTests` pin removal of pending-created rows and persisted binding delete visibility. Focused option/keybinding verification passed 33/33 from the alternate output directory because a local `NexusForever.WorldServer` process locked the default build output. | Decode exact client-facing option readback/initialisation, account-vs-character option ownership beyond current keybindings, and `0x056B`/`0x056C`/`0x056D` producer semantics before widening option behavior. |
| 2026-06-07 | F-014 / audit F-019 loot producer-boundary review | Mapped only | Source and tests already cover the current safe loot boundary: parent ids are accepted only from callers that can supply a distinct source, runtime diagnostics mark owner-mirror versus custom-parent cases, `ServerLootCanLoot` remains a packet-shape/evidence template without an emitter, and recent regressions cover non-looter guards, delivery-time update suppression, group recipient range filtering, owner/looter indexes, immediate cleanup, offline master-loot rejection, and mixed-delivery retryability. No additional source-local defect was found in this pass. | Capture live/client `ServerLootCanLoot` timing and exact parent-source selection before emitting standalone can-loot packets or changing parent id selection policy. |
| 2026-06-07 | F-022 / audit F-027 path reward missing-Spell4 guard | Implemented | `PathManager.GrantPathReward()` now skips a spell reward when `PathReward.Spell4Id` points at a missing `Spell4` row instead of aborting the rest of the reward grant with a null dereference. `PathManagerTests` pin that a mixed item+missing-spell reward still grants the item and does not call `ISpellManager.AddSpell`. Focused path verification passed 89/89 from the alternate output directory because a local `NexusForever.WorldServer` process locked the default build output. | Decode exact path reward presentation, overflow/flag semantics, full per-mission reward precision, broader `Spell4` reward-row coverage, and path unlock sequencing before claiming full path reward parity. |
| 2026-06-07 | F-026 / audit F-033 pet stance producer boundary | Mapped only | `ClientPetSetStanceHandlerTests` now pin the current evidence boundary: owned pet requests update `IPetEntity.Stance`, `PetUnitId=0` resolves the active vanity pet, foreign pets do not mutate state, invalid stance values are rejected, and no blocked `ServerPetStanceChanged` response is emitted. Focused pet stance verification passed 5/5 from the alternate output directory because a local `NexusForever.WorldServer` process locked the default build output. | Capture live/client-log or native server producer evidence tying `ClientPetSetStance` (`0x068E`) to exact `ServerPetStanceChanged` (`0x068F`) response timing/scope before adding a pet lifecycle emit. |
| 2026-06-07 | F-009 / audit F-012/F-013 vehicle embark/disembark producer boundary | Mapped only | `TransportHandlerTests` now pin the current vehicle handler boundary: `ClientVehicleEmbark` logs the decoded vehicle id without mutating player/platform state or emitting a response, `ClientVehicleDisembark` ignores unplatformed players, and mounted players are delegated to the existing `IPlayer.Dismount()` path. Existing packet tests continue to cover vehicle embark input, `ServerVehicleEmbarkAux`, self-passenger state, passenger add/remove broadcasts, and shared passenger rows. Focused transport verification passed 20/20 from the alternate output directory because a local `NexusForever.WorldServer` process locked the default build output. | Capture native/live producer evidence for embark acceptance, seat assignment, passenger lifecycle, deployable vehicle semantics, and `ServerClusterAuxPackets` vehicle/passenger rows before widening runtime vehicle behavior. |
| 2026-06-07 | F-008 / audit F-010 crafting material availability overflow guard | Implemented | `CraftingCraftRequestHelper.TryBuildMaterialDebit` now widens the satchel-plus-inventory availability check before comparing against the required material count, preventing a saturated inventory stack total from overflowing into a false missing-material rejection. `CraftingSimpleCraftHandlerTests` pin a satchel-plus-`uint.MaxValue` inventory material case that debits one satchel unit and one inventory unit, then sends craft success. Focused crafting verification passed 40/40 from the alternate output directory because a local `NexusForever.WorldServer` process locked the default build output. | Decode discovery roll/unlock producer semantics, station service-key names, current-craft emit timing, `0x084B`/`0x0855` aux producer semantics, non-success sigil rules, and `0x056C` microchip patch producer timing before widening crafting beyond fixed recipes. |
| 2026-06-07 | F-001 / audit F-001 STS non-`None` transaction ordering | Implemented | `StsSession` now rejects packets whose handler declares a non-`None` required state when the session is not in that state, restoring strict ordering for `/Auth/LoginStart` and `/Auth/KeyData` without breaking the current `SessionState.None` compatibility routes used by login-finish, game-token, account, and presence flows. `StsSessionStateTests` pin reject/match/compatibility dispatch behavior. Focused STS verification passed 6/6 from the alternate output directory because a local `NexusForever.WorldServer` process locked the default build output. | Keep token/RSA optional flows, external-account routes, optional envelope fields, and exact optional-route state transitions blocked until startup STS captures or native crypto/token semantics prove them. |
