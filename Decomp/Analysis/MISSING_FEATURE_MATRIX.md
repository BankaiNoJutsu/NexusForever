# Missing Feature and Opcode Decode Matrix

Updated: 2026-05-22

This is the system-by-system workboard for restoring retail features from
evidence. It complements `CONTINUATION_GUIDE.md`, `INITIAL_FINDINGS.md`,
`RETAIL_FEATURE_EVIDENCE.md` (online retail behavior by certainty tier), and
`plan-evidenceBackedRestorationEpic.prompt.md`.

The current opcode coverage baseline is structurally closed:

| Direction | Structural state | Semantic state |
| --- | --- | --- |
| Client | `346/346` models, no missing handlers | 17 `Client0xNNNN` rows are diagnostic-only. |
| Server | `692/692` models | 72 `Server0xNNNN` rows remain shape-only or semantically blocked; named-but-partial packets stay tracked by feature rows. |
| Core | `3/3` models | No current backlog. |

Closing structural coverage does not mean retail feature support is complete.
Every row below should be treated as a feature task that must climb the evidence
ladder before mutating server state.

## Status Legend

| Status | Meaning |
| --- | --- |
| Structural | Packet shape or source surface exists, but the behavior is not decoded. |
| Diagnostic | Server parses or emits safe diagnostics only. |
| Compatibility | Server returns empty, failure, or conservative output so the client remains usable. |
| Partial | Some real state exists, but retail parity is incomplete. |
| Blocked | More client-reader, sniff, table, or runtime evidence is required. |
| Ready | The next task is implementation or verification, not more broad discovery. |

## How To Work A Row

1. Pick one system row, not one random opcode.
2. Decode only the opcodes and client functions listed in that row.
3. Produce a function map with binary, address, labels, fields, callers,
   matching NexusForever files, confidence, and blockers.
4. Rename packet/model fields only after read/write order and intent are mapped.
5. Implement the smallest server behavior that is proven by the evidence.
6. Verify with serialization tests, focused xUnit tests, and live smoke when
   the feature crosses runtime state.

## Master Feature Matrix

| ID | System / feature | Current support | Missing feature surface | Decode / evidence targets | Owning code surface | Next task |
| --- | --- | --- | --- | --- | --- | --- |
| F-001 | STS token crypto and optional auth routes | Login/account/game-token compatibility is implemented. | Token crypto handshake, external-account edge routes, and optional envelope fields remain blocked. | `StsConn_SendTokenKeyData`, `ServerRand`, `ServerPublicKey`, `ServerSignature`, `PremasterSecret`, `AuthnToken`; startup STS captures. | `Source/NexusForever.Network.Sts`, `Source/NexusForever.StsServer` | Keep blocked until crypto/token semantics are mapped safely. |
| F-002 | Client diagnostic receive opcodes | 17 conservative models plus diagnostic handlers exist; every diagnostic packet's current scalar/string/opaque wire shape is pinned by focused tests. | The client request intent and server response behavior are unknown. | `Client0x003D`, `00C8`, `00ED`, `011B`, `011D`, `012D`, `0142`, `0550`, `062A`, `0634`, `063E`, `0701`, `0760`, `0762`, `07B6`, `07E3`, `0928`. | `ClientUnresolvedDiagnosticPackets.cs`, `ClientUnresolvedDiagnosticHandlers.cs`, `GameMessageOpcode.cs`, packet tests | Decode client writer functions by feature cluster; keep handlers non-mutating until mapped. |
| F-003 | Server unresolved output opcodes | 70 remaining numeric `Server0xNNNN` output models exist. | Packet names, field names, emit sites, and feature semantics are unknown for the remaining numeric models; some named packets still have unresolved fields tracked in their feature rows. | See the "Server Opcode Decode Matrix" below. | `ServerUnresolvedOutputPackets.cs`, `GameMessageOpcode.cs`, packet tests | Work by adjacent named opcode cluster, then replace each structural model with a named packet. |
| F-004 | Housing residence, neighbors, communities, decor, plugs | Basic residence surfaces and handlers exist; direct residence-identity visits now reuse the mapped privacy/teleport path; housing return now creates/loads the player's residence before returning from another residence map; community rename responses now preserve the computed validation `HousingResult`; the retail `HousingResult_Neighbor_Success` enum value is now exposed and packet-covered; default game-table initialisation now includes `HousingContributionInfo.tbl` for plug vendor/repair cost lookups; accepted neighbor persistence and permission updates exist; expired pending neighbor invite responses now emit `Neighbor_RequestTimedOut`; `ServerHousingNeighbors` native `0x0507` row shape and consumer semantics are mapped, including row `+8` as a reserved/unused slot and target residence identity as the client action key; `ServerHousingCommunityDonateUpdate`, `ServerHousingNeighborhoodEntry`, `ServerHousingNeighborhoodList`, `ServerHousingNeighborInvitePrompt`, `ServerHousingNeighborInviteResult`, `ServerHousingNeighborUpdate`, `ServerHousingCommunityPlotReservation`, `ServerHousingCommunityPlacement`, `ServerHousingCommunityPrivacyLevelUpdate`, `ServerHousingCommunityRenameResult`, `ServerHousingVendorList`, the early housing `0x00CA..00D1` output cluster, and the `0x010D/0x0110` housing-basics follow-ups are mapped and packet-covered at the wire-shape level; community plot reservation, placement, privacy, and rename handlers now emit the mapped retail update/result packets; `ClientHousingInteriorWallpaperUpdate` now preserves the six-slot interior wallpaper decor payload, resolves `DecorInfoId` through `HousingWallpaperInfo`, validates slot flags/default ids, debits mapped wallpaper currency costs, treats native default restore id `5` as valid/free for every slot, persists native decor data/hook/active-prop fields, blocks unsupported wallpaper prerequisite/unlock rows, and echoes changed decor rows through `ServerHousingResidenceDecor`; `ClientHousingDecorUpdate` now preserves the native per-row trailing flag bits diagnostically; `ClientHousingPlugUpdate` now parses the five native 20-byte contribution records as first-class diagnostic fields while keeping non-zero contribution payloads non-mutating; unsupported decor unlock prerequisites now return `Decor_PrereqNotMet`; `ClientHousingCommunityDonate` now validates rows before mutation and mirrors the native `HousingDecorInfo.Flags & 0x8` no-donate guard with `Decor_CannotDonate`. | Retail parity still needs exact roommate/neighbor invite duration and UI timeout broadcast timing, eviction notification edge cases, roommate permission semantics beyond the mapped 0..2 row value, community donation resource/contribution cost and transfer semantics beyond the mapped no-donate flag, full decor/plug ownership and entitlement checks, vendor pricing/list semantics beyond the mapped plug row and contribution-point display, exact retail visit/session/privacy/return-state behavior, non-default interior wallpaper refund and inventory-collection precision, and remaining neighborhood/community field names. | Housing `ServerHousingResidenceKeyedUpdate`, `ServerHousingNeighborhoodEntry`, `ServerHousingNeighborhoodList`, `ServerHousingNeighbors`, `ServerHousingCommunityDonateUpdate`, `ServerHousingNeighborInvitePrompt`, `ServerHousingNeighborInviteResult`, `ServerHousingNeighborUpdate`, `ServerHousingCommunityPlotReservation`, `ServerHousingCommunityPlacement`, `ServerHousingCommunityPrivacyLevelUpdate`, `ServerHousingCommunityRenameResult`, `ServerHousingVendorList`, `Server0x00CB..00D1`, `Server0x010D`, `Server0x0110`; `Housing_*` client labels; `ClientDB_RegisterHousingContributionInfo`. Houston64 is a separate housing/client-tool context and should not override WildStar64 runtime housing result semantics without a Houston-specific feature mapping. | `Source/NexusForever.Game/Housing`, `ResidenceMapInstance.cs`, housing handlers/models | Keep blocked until client-reader/sniff/backing-store evidence maps resource contributions, ownership/unlocks, exact visit/session timing, unresolved housing output consumers, and provisional neighborhood/community fields. |
| F-005 | Marketplace, auction, commodity exchange, CREDD | Request payloads are mapped; transient in-memory auction/commodity order paths exist; focused auction tests now pin post, search, buyout, item transfer, buyer credit debit, and online seller credit payout boundaries. Commodity sell order submit now enforces the mapped client preflight quantity cap from `GameFormula` `0x439`/`Dataint02` with retail fallback `200`; successful commodity-order cancel now refunds/rematerialises the transient order and emits `ServerCommodityAuctionRemoved` with `AuctionEventType.Cancel`. CREDD info requests now emit the mapped empty `ServerCREDDExchangeInfoResults` (`0x026A`) compatibility snapshot plus an operation result; CREDD history requests now emit mapped transient runtime operation rows when submit/match/cancel activity exists. Mail COD sender cash settlement is covered in F-013. | Persistent listing/search storage, offline seller/bidder settlement, marketplace mail settlement, commodity cancel-failure/result precision, commodity matching precision beyond submit preflight, non-empty CREDD exchange info rows, and durable CREDD history persistence remain missing. | `Marketplace_*WritePayload`, `Marketplace_SendClient*`, `Marketplace_ValidateCommodityOrderBeforeSubmit`, `ServerCREDDExchangeInfoResults`, `06DF`, `07D3`, `07D5`; auction/commodity response readers. | `WorldServer/Network/Message/Handler/Marketplace`, marketplace models/tests, mail manager | Decode marketplace result/update packets, commodity matching/fill precision, cancel-failure behavior, and CREDD non-empty price/order rows before promoting the transient order book or CREDD service to durable storage. |
| F-006 | Storefront, account inventory, pending item groups, generic unlocks | Catalog, supported purchase, account currency charge, claim/return paths, online and offline pending-group delivery with auth DB persistence (`account_pending_item`), incremental pending/cache opcodes (`0976`, `097D`, `096A`+`0975`), storefront purchase success packets (`098C` character / `098D` account, correlated to client `082A`/`0828`), offline storefront gifts, gift-to-account reserved-zero guard, `096E` daily-login snapshot on catalog open with durable claim state (`account_daily_login`, client `0x078F` verified @ `1400070f0`), hardcoded coupon redemption (`0x0790`, sender opcode still unverified), emulated VC package purchase (`0x082E` @ `1404f1d50` → grant currency + `0990`/`0991`), `0980` wallet updates, `098E` purchase history on catalog open (`account_store_purchase_history`), `0989` catalog-updated emit, `0971` privilege restriction plus DB-backed purchase-velocity gate (10 purchases / hour), `097B`+`0x0268` CREDD redeem (1000:1 to character credits), structured `0x026A` `0x50` order-book header (`CREDDExchangeInfo_BuildLuaResults` @ `14042b9a0`), character generic-unlock sync (`0983`/`0984` on enter world), durable CREDD orders/history (`account_credd_order`, `account_credd_history`) with `097A` order-cache rows on info requests, store offer trailing amount fields, `ServerStoreError` rejection paths, and field-level wire shapes for `0969..0991` covered by focused packet tests (`52` tests). | Real-money VC billing; full retail `0x026A` owned-order row pointer semantics when count is non-zero; `0x0986/098F` live emit (no mapped client consumer); exact `096A..096C` leading `uint32` producer semantics; unsupported offer item effects beyond current placement checks; coupon client sender proof for `0x0790`. | Account/store opcodes `096A..0991`; `ServerAccountItems*`, `ServerStore*`, gift/claim/client CREDD senders; `Storefront_SendClientPurchaseVirtualCurrencyPackage`. | `Game/Account/Inventory`, `Game/Storefront`, `Database.Auth`, account handlers/models/tests | Apply migrations `20260522120000_AccountPendingItem`, `20260522130000_AccountCREDDExchange`, and `20260522140000_AccountDailyLoginAndStoreHistory`; map non-empty `0x026A` owned-order rows and coupon sender before claiming full retail parity. |
| F-007 | Reward rotation and reward properties | Game-table refresh emits `0x07CD`/`0x07D3` content context (correlated throttle uints), `0x07CA` schedule rows, and empty `0x07C8` entry-state for fresh accounts. | Per-content authoritative reward mapping, account grant persistence, and `0x07CD` apply/`Flag` consumer remain blocked. | `ServerRewardRotation*`, `ServerRewardPropertySet`, `ServerRewardRotationContentContext`, `ClientRewardUpdateRequest`. | `Game/Account/Reward`, reward handlers/models/tests | Capture retail schedule rows or map content→reward tables before replacing correlated schedule selection; add DB grant persistence before sending non-empty entry-state. |
| F-008 | Crafting, tradeskills, rune crafting | Packet layouts, validation, profession persistence, conservative fixed-recipe crafting, and focused crafting packet-shape tests exist; behavior tests pin direct-output craft success, LootId-output generation/delivery, craft-item count/catalyst debit, satchel material debit, crafting XP/achievement updates, and missing-material failure. | Discovery rolls, station constraints, complex/random output parity beyond current LootId delivery, broader material-source precision, rune item data, and sigil result meanings remain incomplete. | `ClientCrafting*`, `Tradeskill_SendClient*`, `Server0x084B`, `0855`, `ServerTradeskillSigilResult`. | `WorldServer/Network/Message/Handler/Crafting`, crafting models/tests, supply satchel, item manager | Decode crafting result/update packets, discovery/station semantics, and sigil readers before widening crafting beyond fixed recipes. |
| F-009 | Rapid transport, taxi, flight path, vehicles | Pricing/request validation is partial; proven packet shapes for rapid transport, flight-path purchase/update, vehicle embark, self-passenger vehicle state, passenger add/remove broadcasts, and shared vehicle-passenger rows now have focused protocol tests. | Service-token bypass, route state, taxi embark and completion, charge/teleport rules, passenger/seat modes, and vehicle deployable semantics are incomplete. | `ClientRapidTransport_WritePayload`, `ServerFlightPathUpdate`, `Server0x077E`, `ClientFlightPathPurchase`, vehicle server outputs near `01B2`, `08CC`. | `Game/Entity/PathManager`, player path handlers, vehicle handlers, spell vehicle effects, packet tests | Decode flight/taxi read paths, passenger/seat-mode semantics, and route-table correlation before charging, teleporting, or deploying vehicles broadly. |
| F-010 | Group, raid, queue, matching | Group and matching handlers exist; queue/match runtime is partial; queue validation now checks existing `IMatchingCharacterQueue` proposals when blocking solo/group mixing; leave-as-group now fans out the selected queue leave to online group members; matching role enforcement is covered for one-tank/one-healer/three-DPS flexible-role reduction plus over-cap/no-role rejection; group flags and group-member flag/role-change request-to-broadcast bridges are covered through the internal message boundary; group loot state preserves the published harvest loot rule; loot-rule request/response packet shapes are covered; raid-info requests return empty state or conservative active solo runtime lock rows while skipping residence locks. | Queue status timing, replacement/vote flows, exact role-selection lifecycle and client signaling, durable raid lock rows, group/raid save-state integration, full group member flag effects, loot-rule effects, cross-realm group data, and position updates need parity. | Provisional group/raid outputs `ServerGroupInstanceDifficultyResponse` (`0x0414`), `ServerGroupKickResult` (`0x042A`), `ServerGroupLootRuleValidationResult` (`0x0431`), `ServerGroupRosterUpdate` (`0x0436`), `ServerGroupMemberRoleChange` (`0x0438`), `ServerGroupReadyCheckStatusUpdate` (`0x0441`), `ServerGroupRequestJoinWindow` (`0x045A`), `ServerQuestShareResult` (`0x0461`), `ServerGroupMemberDetailUpdate` (`0x0468`), `ServerRaidQueueStatus` (`0x0718`); matching `Client0x062A`, `0634`; raid-info response readers. | `Game/Group`, `Game/Matching`, group/matching handlers/models/tests | Decode group output cluster, queue status packets, matching `Client0x062A/0634`, exact role-change/status client behavior, group-member flag side effects, and durable raid-info row semantics before widening queue lifecycle or saved-instance lock behavior. |
| F-011 | Guild, recruitment, war party | Guild handlers and recruitment output models exist; war-party boss-token request/response, detailed recruitment-guild identity request, recruitment details/list/update rows, demand payloads, and recruiter online flag payloads are covered by focused packet tests. | Bank transactions, perks, holomarks, standards, recruitment subscriptions, real war-party boss-token inventory/results, and warplot plug state need validation or implementation. | Recruitment outputs `0767..076D`, `ServerWarParty*`, guild handler request labels, nearby `Server0x077E` if correlated. | `Game/Guild`, guild handlers, war party models/tests | Map recruitment subscription/update timing and warparty readers, then choose one persistent guild subfeature. |
| F-012 | ICComm, chat, friendship, social options | ICComm has transient membership, offline-player cleanup coverage, and current result/ordered-echo/directed-recipient delivery plus named-recipient filtering covered by tests; friendship has partial account/block/note support; chat mostly works; transient ignore-strangers echo and auto-response message storage are covered by tests. | Entitlement checks, persistent channels, retail delivery precision beyond the current transient directed/ordered behavior, exact leave/logout client signaling, throttling, auto-response delivery/persistence precision, ignore-strangers persistence, and some chat aux packets remain missing. | `Client0x0550`, chat `Server0x01B8`, `01C1`, `01C4`, `01EF`, `Lua_FriendshipLib_*`, `ClientFriendship*`. | `Game/ICComm`, `WorldServer/Network/Message/Handler/ICComm`, chat/friendship handlers/tests | Decode chat/ICComm auxiliary outputs and social-option persistence before adding persistent channel or durable auto-response state. |
| F-013 | Mail | Mail request handlers exist; focused transaction tests now pin cash collection, return, attachment-deletion persistence, delayed pending-mail promotion without duplicate delivery, COD sender cash settlement through instant outgoing mail after buyer payment, delete result/unavailable signaling, return success/cannot-return signaling, and the basic expiration sweep for pending/available mail. | Marketplace settlement, broader delete-state parity across saves/reloads and multi-client views, exact expiration timing/result parity, and broader attachment/cash atomicity still need retail parity. | Mail request handlers, `ServerMailResult`, `ServerMailItemDeprecation`, `Server0x05A1`, account gift/pending group flows. | `Game/Entity/MailManager`, mail handlers/models, `Game.Tests/Mail` | Decode remaining delete/result semantics, exact expiration client behavior, and marketplace/offline settlement flows before widening live mail mutation. |
| F-014 | Loot, bindcheck, rolling, master loot | Basic loot delivery, group roll/master-loot runtime paths, Rider's Reef fixes, handler-level roll/assign delegation tests, roll/assign/result packet-shape tests, and the `ServerLootItemUpdate` (`0x08A0`) reader/model are mapped. `ServerLootNotify` ingestion copies each full `LootItem` row into the client loot tree, including the Lua-facing `bCanLoot`/`CanLoot` slot; `ParentUnitId` is mapped to a client-side loot visual source field but runtime still mirrors `OwnerUnitId` because no distinct source entity is tracked. `ServerLootGrant`, `ServerLootNotification`, `ServerLootRoll`, and `ServerLootWinner` consumers are labeled. `ServerLootBindOnPickup` (`0x011C`) is mapped as two uint32 fields with `LootUnitId` driving client `LootBindcheck`; adjacent `Client0x011B/011D` are not proven BOP acknowledgements. `0x08A8` is reclassified as compact entity visual info, not loot. | Bind-on-pickup confirmation policy and first `ServerLootBindOnPickup` uint, exact roll/master eligibility rules, distinct parent source tracking, master-loot UI packet parity, standalone `ServerLootCanLoot` consumer/timing, and loot auxiliary output emission policy remain open. | `Loot_*` labels, `ServerLoot*`, `ServerLootItemUpdate`, `ClientLootAssignMaster`, `ClientLootRollAction`. | `Game/Loot`, loot handlers/models, loot request handlers/tests | Keep standalone `ServerLootCanLoot` diagnostic-only until a scalar consumer or live capture proves when it should be emitted; live-capture `LootBindcheck` acceptance to prove whether it reuses `0x014F ClientLootItem` before adding pending BOP confirmation state; then decode remaining roll/master UI semantics and prove when a distinct parent/source entity should be sent. |
| F-015 | PvP and duels | Transient duel lifecycle and PvP toggle exist; focused tests now pin challenge, countdown/start timing, timeout cancellation, defeat result, duel achievement updates, duel participant/result/failure packet layouts, PvP cooldown/state packets, and client PvP/ignore-duel toggles. | Observer broadcasts, duel leash/warning state, PvP cooldown persistence, forced-map PvP, rewards/stats, and exact cancel/result semantics still need parity. | Duel/PvP labels, `ServerDuel*`, `ServerPvpCooldown*`, `Server0x00EE` if correlated to duel/path boundary. | `Game/Pvp`, PvP handlers/models, `Game.Tests/Pvp` | Decode duel warning/result readers and leash/cancel evidence before adding observer broadcasts or broader PvP state mutation. |
| F-016 | Spell runtime: procs | Conservative holder-side proc dispatch exists for mapped events/routes. | Unsupported trigger events and targetData tails, exact chance/cooldown ordering, recursion rules, and school-specific edge cases need validation. | `Proc.DataBits00..09`, `ProcRuntimeEvidenceCollector`, `!spell procreport`, `ServerSpellUInt32TripletList`, `ServerSpellUInt32TripletListVariant`, `ServerSpellFourUInt32` if spell auxiliary. | `Game/Spell`, spell commands/tests | Validate event fixtures and widen only through `ProcDispatchEvidenceBoundary`. |
| F-017 | Spell runtime: damage/heal/shields/vitals | Many families are conservative or partial. | Retail formula parity, distance/distribution splitting, shield/absorb packet parity, HoT cadence, SapVital modes, ClampVital modes, alias vitals, and result side effects remain incomplete. | `Spell Effect Evidence Matrix.md`, `Spell System Progress Tracker.md`, spell diagnostics, combat log readers. | `Game/Spell`, `Game/Entity/UnitEntity`, combat log models | Work family-by-family with fixture captures; do not widen from one witness row. |
| F-018 | Spell runtime: stack groups, buffs, CC, movement | Several states are tracked locally with timed cleanup; focused tests now pin CC/stun/knockdown/tether/buff-remove packet boundaries. | Stack arbitration, broader aura persistence, DR/stun breakout semantics, tether/additional CC data, force-move physics, facing blend modes, and runtime packet parity are incomplete. | `Spell4StackGroup`, `CCStateSet`, `CCStateBreak`, `ForcedMove`, `ForceFacing`, `ServerEntityCC*`. | `Game/Spell`, `Game/Entity/Movement`, combat logs, packet tests | Decode stack arbitration or CC packet tails before broad behavior changes. |
| F-019 | Spell runtime: summons, traps, vehicles, AI services | Conservative creation/removal exists for some summon families. | Ownership, AI/controller hookup, trap trigger behavior, formation, service payloads, turret/deployable state, and summoned vehicle seat modes are incomplete. | `SummonCreature`, `SummonTrap`, `SummonVehicle`, `NpcExecutionDelay`, service-token labels. | `Game/Spell`, entity factory, combat AI, content scripts | Pick one placed fixture and implement only its proven controller/trigger loop. |
| F-020 | Spell runtime: RavelSignal and scripted receivers | Structural diagnostics and `OnSignal(signalId)` bridge exist. | Receiver graph, signal modes, payload values, and script-state effects are not decoded. | `RavelSignal`, `SpellRouteEvent_*`, content scripts, runtime signal logs. | `Game/Spell`, scripts, `IWorldEntityScript` | Decode receiver/callback graph before adding generic signal side effects. |
| F-021 | Action set, LAS, ability, AMP, attributes | LAS size/spec/tier/AMP and some preflight checks exist; client action-set, non-spell shortcut, and AMP commit request parsing plus `ServerActionSet`, `ServerAmpRespecResult`, `ServerSpecChanged`, and cache-clear payloads are covered by focused protocol tests. | `UpdateSpellInProgress`, async spell update transaction, authoritative attribute allocation/refund, bonus ability/AMP unlock persistence, and action-bar lock state remain incomplete. | `Server0x00B0`, `016B`, `016D`, `016E`, `019C`, `01A4`, `ClientRequestActionSetChanges`, `ActionSet_CheckUpdateSpellInProgress`. | `Game/Spell`, ability models, spell handlers | Map async spell update transaction before adding synthetic update gates. |
| F-022 | Quests, path missions, public events | Core quest/objective and many public-event packets exist; recent tutorial fixes landed. | Path mission edge types, public-event votes/scoreboards, event phase scripts, objective notification parity, quest share precision, and some objective types remain incomplete. | `Server0x0139`, `06F7`, path opcodes around `0160/0181`, public-event labels, content smoke logs. | `Game/Quest`, `Game/PublicEvent`, path manager, scripts | Use one content flow at a time: NPE, Rider's Reef, or world `3404`. |
| F-023 | Content: NPE and Rider's Reef | Recent Rider's Reef/final-departure and mine/loot fixes landed; NPE login-world smoke works. | Manual client playthrough validation remains for quest acceptance, kill loops, rewards, respawn, CSI, hoverboard/projector reliability, and final terminal/checklist flow. | Runtime logs, `I:\WildStar` client smoke, tutorial scripts, quest direction tables. | `Source/NexusForever.Script.Main/Tutorial`, quest/entity/runtime code | Run manual client smoke and turn each observed blocker into a focused code task. |
| F-024 | Content: Evil from the Ether / world 3404 | Map asset and world import are staged; scripts exist. | Manual expedition playthrough, public event `781`, doors, interactables, teleports, phase transitions, encounter mechanics, and unsupported spell/content blockers remain. | `Plan Evil From The Ether Expedition Port.md`, world `3404` scripts, runtime smoke logs. | `Source/NexusForever.Script.Instance`, map/instance/public-event code | Start local stack and validate one encounter phase at a time. |
| F-025 | Entity create/update, visibility, phasing, interaction, CSI | Core entity create/update works; `ServerEntityCreate` world-placement and mapped tail bit layout now have a focused packet regression; busy target and interaction gates are partial. | Unknown `ServerEntityCreate` substructures, deferred action queues, current object/target semantics, CSI parity, phase visibility, threat/entity stat packets, and map-tracked-unit parity remain open. | `Server0x025F`, `0260`, `0261`, `0263`, `0264`, `0889`, `08CC`, `08F4`, `0939`, `093D`, `093E`; `Interaction_*`, `DeferredActionQueue_*`. | `Game/Entity`, world entity models, entity handlers, scripts | Decode the remaining entity-create/update substructures before runtime mutation. |
| F-026 | Items, supply satchel, costumes, pets, titles, generic unlocks | Inventory, title, pet, costume, unlock surfaces exist in parts; generic-unlock item use now has focused invalid-set, invalid-entry, already-unlocked, and partial-unlock lifecycle tests; generic unlock account/character list/result packets and costume unlock/forget result packets have focused wire-shape coverage. | Item swap/error aux packets, item eligibility, supply satchel precision, costume forget/unlock parity, pet flair, generic unlock persistence, and account/character unlock list delta semantics remain incomplete. | `Server0x00B7`, `0183`, `019A`, `037F`, `0567`, `056B`, `056C`, `056D`, `0980`, `ServerAccountUInt64Payload`. | `Game/Entity/Item`, account unlock/costume/pet/title managers | Decode item/unlock packet tails and account/character unlock delta semantics before adding broader unlock or costume lifecycle behavior. |
| F-027 | Options, keybindings, combat-log preferences | Keybinding and mapped option parsing exists; casting, shared-challenge, disable-other-player-log, and combat-log disable preferences now load/save on character rows and suppress outbound combat logs where mapped. Focused handler tests pin `ClientCombatOptions`, `ClientOptions`, `ClientCombatLogDisableOthers`, and `ClientCombatLogDisables` mutation plus validation of known casting/combat-log/shared-challenge values. | Additional option types, account-vs-character option split, exact client-facing option initialisation/readback, and nearby item/options server opcodes remain incomplete. | `ClientCombatOptions_WritePayload`, `ClientOptions`, `BiInputKeySet`, nearby item/options server opcodes `056B..056D`. | Option handlers, account/character keybinding managers, `Database.Character` character model/migrations, `Game/Entity/Player`, `Game/Entity/WorldEntity` | Decode remaining option result/readback packets before adding broader option types or account-level preference semantics. |
| F-028 | Support, reports, surveys, stuck | Parsed diagnostics, injectable submission-store boundary, JSONL default capture, deterministic ticket result/failure, incident-report validation/capture, bug-report validation/capture, suggestion capture, mapped `ClientCustomerSurveySubmit` 32-bit context parsing, and conservative stuck request branch behavior are now covered. Stuck free-suicide only mutates living players, clamps zero-health damage to one, invalid unstick types do not mutate state, and recall-transmat honors the teleport gate. | Moderation workflow, durable database-backed case lifecycle, user-facing support-case result semantics beyond ticket success/failure, stuck-result client signaling, full recall-house/transmat destination evidence, and report/survey administrative tooling are missing. | `ClientIncidentReport`, `ClientSupportTicket`, `ClientReportBug`, `ClientStuck`, `ClientSuggest`, `ClientCustomerSurveySubmit`, `Server0x0347..0351`, `ServerSupportTicketResult`. | Support handlers, `WorldServer/Support`, account/support models if introduced | Decode support-case result/readback semantics, stuck result signaling, and residence/transmat destination proof before adding a database case workflow, moderation UI state, broader support-case responses, or extra stuck side effects. |
| F-029 | Client DB, data mapping, EF placeholder proof | Table registration pattern is mapped and DataMapping exists. | More table registrations, staging-to-runtime promotions, EF placeholder renames, and migration validation remain case-by-case. | Client DB registration labels, `Tools/DataMapping`, `AccountInventoryModel.Unknown*`, `DataBits*`, table fixture SQL. | GameTable, Database projects, DataMapping tools | Rename fields only after packet/data contract plus migration proof. |
| F-030 | Realm, character select/list/transfer, PTR/copy | Character list/select works; realm transfer destination requests now return an empty non-mutating compatibility list with packet-shape coverage; selecting the current realm is ignored for client safety; selecting an offline target realm returns `ServerRealmTransferResult.RealmTransferFailed_ServerDown`; some realm and PTR/copy packets exist. | Real realm transfer destinations/results, online transfer handoff parity, PTR copy state, new realm notices, optional realm messages, and character admin result packets need parity. | `Client0x0760`, `0762`, `Server0x03EF`, `Server0x0347..0351`, `ServerPtrCharacterCopyQueued`, realm message/list readers. | Character handlers, realm/server managers, pregame models | Decode realm/client-list auxiliary requests and online transfer handoff semantics before changing realm state beyond current compatibility responses. |
| F-031 | Fortune minigame | Client and server packet families are modeled and packet-shape tested; handlers now maintain a conservative account-scoped in-memory session, send empty reward/status packets, update flipped-card state, reset invalid/no-session flips, and reset flipped state when a new session starts for the same account. | Reward payout, eligibility/cost, real reward selection, storefront/game synchronisation precision, and durable retail resume semantics remain missing. | `ClientFortuneNotifyGame`, `ClientFortuneNotifyStorefront`, `ClientFortuneStart`, `ClientFortuneFlipCard`, `ServerFortuneCards`, `ServerFortuneCardUpdate`, `ServerFortuneRewards`, `ServerFortuneReset`; fortune UI readers. | `WorldServer/Network/Message/Handler/Fortune`, fortune models, storefront/account inventory surfaces if linked | Decode reward generation and payout evidence before linking Fortune to account inventory, currency, storefront, or persistent resume state. |
| F-032 | Leaderboards | PvE and PvP request handlers use an injectable provider; the default provider returns deterministic empty compatibility responses; non-empty PVE/PVP response row and team-member wire layouts are pinned by focused packet tests. | Real leaderboard rows, rank/stat aggregation, personal placement, season/filter semantics, and map or prime-level category rules are missing. | `ClientLeaderboardPveRequest`, `ClientLeaderboardPvpRequest`, `ServerLeaderboardPve`, `ServerLeaderboardPvp`; leaderboard UI readers and category selectors. | `WorldServer/Network/Message/Handler/Leaderboard`, `WorldServer/Leaderboard`, leaderboard models, backing store/service if introduced | Decode leaderboard UI category/filter semantics and source-of-truth score rows before introducing ranking storage or cached results. |
| F-033 | Challenges and shared challenges | Challenge packet models exist; shared-challenge preference toggles exist; `ClientChallengeChoice` now receives a deterministic non-mutating `ServerChallengeResult.GenericFail` compatibility response; `Client0x00C8`'s mapped uint32 diagnostic shape is pinned by a focused test; challenge choice/result/update/share/share-timeout wire payloads are covered. | Active challenge lifecycle, share/accept/timeout state, objective/result updates, reward tiers, and quest/objective integration are missing. | `ClientChallengeChoice`, `ServerChallengeUpdate`, `ServerChallengeResult`, `ServerChallengeShared`, `ServerChallengeShareTimeout`, `Client0x00C8`, challenge UI readers, `QuestObjectiveType.CompleteChallenge`. | challenge models, option handlers, `GameTable/Challenge*`, quest/objective surfaces | Decode the accept/decline, update, and reward-tier sequence before mutating quest, group, or reward state. |
| F-034 | Datacubes, journals, Galactic Archive / Codex | Datacube and archive persistence, login init, client unlock/view flows, rule refreshes, packet models, creature `ArchiveArticleIdInteractUnlock` activation hooks, SimpleEntity datacube/journal activation boundaries, and datacube/archive request/update packet shapes are covered. | Broader content hookups, archive-link parent/child authorization, full journal/datacube progression semantics, path-mission rule parity, and wider Codex UX coverage remain incomplete. | `ServerDatacubeUpdateList`, `ServerDatacubeUpdate`, `ServerDatacubeVolumeUpdate`, `ClientGalacticArchiveUnlock`, `ClientGalacticArchiveViewed`, `ServerGalacticArchiveRefresh`, archive link/unlock rules, creature `ArchiveArticleIdInteractUnlock`. | `Game/Entity/DatacubeManager`, `Game/Entity/GalacticArchiveManager`, `Game/Entity/SimpleEntity`, galactic archive handlers/models | Verify one datacube or journal pickup chain and one archive-link unlock flow end-to-end, then close remaining field and trigger gaps. |
| F-035 | Achievements and realm-firsts | Character, guild, and global achievement managers, persistence, init/update packets, many runtime updaters, diagnostic `ClientSteamAchievements` parse/handler tests, and achievement init/update/realm-first packet shape coverage exist. | Full trigger coverage, Steam achievement payload grammar and ingest mapping, exact realm-first broadcast semantics, remaining updater parity, and achievement UI edge cases are incomplete. | `ServerAchievementInit`, `ServerAchievementUpdate`, `ServerRealmFirstAchievement`, `ClientSteamAchievements`, `AchievementType` callsites, realm-first and guild-achievement flows. | `Game/Achievement`, achievement models, `ClientSteamAchievementsHandler`, achievement updaters/tests | Map Steam payload grammar and achievement-id correlation before ingesting client Steam achievement data. |
| F-036 | Zone maps and zone completion | Hex discovery, persistence, login sync, `ServerZoneMap` output, `AchievementType.MapComplete`, unambiguous exploration-only `ZoneCompletion` title rewards, and `ServerZoneMap`/`ServerMapUpdateHexGroup` wire shapes exist; `ZoneCompletion.tbl` is now loaded by the default game-table initialisation path. | Full zone completion semantics, non-title rewards, faction/path-specific completion rows, and integration with quest/challenge/datacube/journal totals from `ZoneCompletion` remain incomplete. | `ServerZoneMap`, `MapZone*`, `MapZoneHexGroup*`, `ZoneCompletion`, `AchievementType.MapComplete`, zone-map UI and completion readers. | `Game/Map/ZoneMapManager`, `Game/Map/ZoneMap`, `Game/Map/ZoneCompletionRewardResolver`, zone-map models, achievement/title surfaces | Decode completion row category/faction semantics and objective-total sources before awarding broader zone-completion rewards. |

## Client Diagnostic Opcode Matrix

These are receive-side client opcodes with safe logging only. The first decode
step is to find the native writer/sender path and prove when the client sends
the request.

| Likely cluster | Opcode(s) | Shape | Neighbor evidence | First task |
| --- | --- | --- | --- | --- |
| Login/world entry | `Client0x003D` | `0x18` bytes | Between max-character-level and player-entered-world. | Find writer around world-entry/client-ready state. |
| Challenge/housing boundary | `Client0x00C8` | `0x4` bytes | Between challenge update and housing privacy. | Check whether this is challenge ack, housing ack, or UI state. |
| Duel/path boundary | `Client0x00ED` | `0x20` bytes | After duel initiate, before path scientist dismiss scanbot. | Search sender callsites from duel and path UI. |
| Character/loot/mail | `Client0x011B`, `Client0x011D` | `0x1`, `0x4` bytes | Around character list, loot bind, and mail delete. | Decode whether these are loot bind acks or mail/list UI requests. |
| Quest/pet customisation | `Client0x012D` | `0x8` bytes | After quest objective world location, before pet list. | Map client request owner: quest tracker or pet UI. |
| Rapid transport/appearance | `Client0x0142` | `0x10` bytes | After rapid transport, before appearance result. | Correlate with rapid transport service-token follow-up. |
| ICComm/spell list | `Client0x0550` | `uint32` | Between directed ICComm message and spell list. | Check if this is ICComm ack or ability-book request. |
| Matching/movement control | `Client0x062A`, `Client0x0634` | `uint32`, `uint32` | Between matching average wait and movement control ack. | Decode matching queue UI acks before queue state changes. |
| Auth denied/marketplace status | `Client0x063E` | wide string | Between auth denied and marketplace status. | Determine if this is crash/report/status text or marketplace filter. |
| Public event/queue | `Client0x0701` | `0x8` bytes | After public event trigger UI, before queue finish. | Map public-event UI ack vs queue action. |
| Realm list/messages | `Client0x0760`, `Client0x0762` | `0x58`, `0x10` bytes | Around realm first achievement, realm list, realm messages. | Decode realm-list refresh or realm-message request path. |
| Instance/account item | `Client0x07B6` | `0x20` bytes | After reset instances, before account item return. | Correlate with instance reset UI or pending item groups. |
| Character/destination arrow | `Client0x07E3` | `uint32` | Between character list and destination arrow clear. | Decode pregame/world UI request source. |
| Cooldown/reward property | `Client0x0928` | `0x8` bytes | Between cooldown list and reward property set. | Check reward-property or cooldown-list client ack path. |

## Server Opcode Decode Matrix

This matrix tracks remaining shape-only or semantically incomplete server-output
opcodes. After the latest housing community rename result pass, 72 still carry `Server0xNNNN`
names. The "likely cluster" is based on enum ordering and neighboring named
opcodes, not a semantic claim.

| Likely cluster | Opcode(s) | Shape evidence | Neighbor evidence | First decode task |
| --- | --- | --- | --- | --- |
| Achievement / LAS opening | *(mapped wire)* `ServerLasOpeningUInt18` (`0x00B0`) | `18`-bit Spell4Id | Between achievement update and action-set request. | Map consumer intent before emitting; field meaning remains opcode-specific. |
| Item context / friendship bridge | `0x00B7` | `0x1` byte | Between item context action and friendship block. | Check item result vs social block response readers. |
| Early housing / instance UI | `ServerHousingResidenceKeyedUpdate` (`0x00CA`), `0x00CB`, `0x00CC`, `0x00CD`, `0x00CE`, `0x00D1` | `uint64 + uint32`, empty, 15-bit scalar, 15-bit scalar, wide string, empty | After housing privacy request, before instance settings. | Wire shapes are mapped and modeled; keep consumer/emit intent blocked until UI handlers or captures identify the fields. |
| Datacube / path / resurrection edge | `0x00DF`, `0x00EE`, `0x0101`, `0x0160`, `0x0181` | byte and uint shapes | Around datacube, duel, resurrection, path scientist scanbot. | Split by reader registration; do not assume one subsystem. |
| Housing basics | `0x010D`, `0x0110` | empty, `uint32 + 18-bit scalar + uint32 + 8-bit scalar` | Around guild holomark and `ServerHousingBasics`. | Wire shapes are mapped and modeled; field meanings remain diagnostic-only. |
| Public event auxiliary | `0x0139`, `0x06F7` | `0x10`, `0x8` bytes | Around public-event map/objective/bomb status. | Trace public-event UI update readers and scoreboard paths. |
| Appearance / repair / instance / character admin | `0x0143`, `0x014D`, `0x0157`, `0x0347`, `0x0348`, `0x034C`, `0x034D`, `0x034E`, `0x0351` | byte, `0x8`, `0x10..0x40` bytes | Around rapid transport/appearance, repair, instance reset, survey/character delete. | Decode each reader before grouping; several adjacent systems collide here. |
| LAS / abilities / reputation / path XP | `0x01A6`, `0x01A7`, `0x01A8`, `0x01A9` | `0x4..0x10` bytes | Around reputation and path XP after the mapped LAS aux cluster. | Decode remaining reputation/path XP reader registrations. |
| Inventory / supply / item/options | `0x0183`, `0x019A`, `0x0567`, `0x056B`, `0x056C`, `0x056D` | `uint32+flag`, `0x8`, `0x10..0x28` bytes | Around item move, supply satchel, mail item deprecation, item error, keybinds. | Decode item-error/swap/options readers before implementing item side effects. |
| Entity/player create and CREDD | `0x0186`, `0x0187`, `0x025F`, `0x0260`, `0x0261`, `0x0263`, `0x0264`, `0x026A` | `0x1..0x50` bytes | Around entity select, flight path, player/entity create, CREDD exchange. | Decode `025F..0264` with entity-create reader evidence; decode `026A` with CREDD separately. |
| Vehicle / chat / cinematic boundary | `0x01B2`, `0x01B8`, `0x01C1`, `0x01C4`, `0x01EF` | `0x10`, `0x68`, `0x20`, `0x20`, `0x10` bytes | Vehicle embark, chat flag/list/message, cinematic start. | Trace chat auxiliary readers and vehicle reader separately. |
| Costume / emote | `0x037F` | `0x18` bytes | Between emote and costume item forget. | Decode costume/emote UI response path. |
| Realm transfer / account gift | `0x03EF` | `0x10` bytes | Between realm transfer destinations and account item gift. | Decode realm-transfer or gift pending-group response. |
| Group / raid / queue | `ServerGroupInstanceDifficultyResponse` (`0x0414`), `ServerGroupKickResult` (`0x042A`), `ServerGroupLootRuleValidationResult` (`0x0431`), `ServerGroupRosterUpdate` (`0x0436`), `ServerGroupMemberRoleChange` (`0x0438`), `ServerGroupReadyCheckStatusUpdate` (`0x0441`), `ServerGroupRequestJoinWindow` (`0x045A`), `ServerQuestShareResult` (`0x0461`), `ServerGroupMemberDetailUpdate` (`0x0468`), `ServerRaidQueueStatus` (`0x0718`) | `0xC..0x60` bytes | Around group difficulty, invite/kick/mark/member flags/action result, queue status, raid info. | Map group reader table, then implement one group status/update packet at a time. |
| Housing neighbor/community | `ServerHousingCommunityDonateUpdate` (`0x04FE`), `ServerHousingNeighborhoodEntry` (`0x0501`), `ServerHousingNeighborhoodList` (`0x0506`), `ServerHousingNeighborInvitePrompt` (`0x0514`), `ServerHousingNeighborInviteResult` (`0x0516`), `ServerHousingNeighborUpdate` (`0x0519`), `ServerHousingCommunityPlotReservation` (`0x051F`), `ServerHousingCommunityPlacement` (`0x053A`), `ServerHousingCommunityPrivacyLevelUpdate` (`0x053B`), `ServerHousingCommunityRenameResult` (`0x078C`) | `0x10..0x30` bytes; `0x051F` is target residence plus uint32 plot index; `0x053A` is target community residence plus placed residence id and property index; `0x053B` is target community residence plus type-7 cache flags and Lua privacy value; `0x078C` reads four uint32 fields but the UI event descriptor exposes only the first result field | Around community donate, neighborhoods, neighbors, neighbor invite/evict/permission, community plot reservation/placement/privacy/rename. | Confirm remaining neighborhood/community field names and wider permissions/community persistence semantics. |
| Realm info / mail | `0x05A1` | `0x10` bytes | Between realm info response and mail result. | Check realm-info vs mail-result reader before implementing. |
| Marketplace / reward / generic map | `0x06DF`, `0x07CD`, `0x07D3`, `0x07D5` | `0x20`, `0x28`, `0x10`, `0x14` bytes | Around auction post, reward update request, generic map node, auctions by filter. | Decode marketplace/reward/generic-map readers separately. |
| Story / flight / pets | `0x074A`, `0x077E` | `0x18`, `0x10` bytes | Story communicator/unit and recruitment/pet despawn; `077E` shares flight-path read shape. | Decode story text auxiliary and flight update alias before emit-site changes. |
| Spell/cooldown auxiliary | `ServerSpellUInt32TripletList` (`0x080F`), `ServerSpellUInt32TripletListVariant` (`0x0810`), `ServerSpellFourUInt32` (`0x0812`) | counted uint32-triplet rows; four uint32 fields | Around cooldown, target list, buff remove. | Decode row/field gameplay semantics before wiring spell runtime emitters. |
| Map / crafting / tradeskill | `0x0846`, `0x084B`, `0x0855` | `0x4`, `0x18`, `0xC` bytes | Around time-of-day/map tracked unit and crafting current/profession load. | Split map-tracked-unit from crafting result readers. |
| Unit combat / entity stat | `0x0889`, `0x08CC`, `0x08F4`, `0x0939`, `0x093D`, `0x093E`; `ServerEntityVisualInfoUpdate` (`0x08A8`) flag meanings | `0xC..0x18` bytes plus mapped `0x08A8` compact visual-info payload | Around reputation override, death, vehicle passenger, entity visual/stat, emote/item use. | Decode entity-stat readers and the remaining `0x08A8` flag semantics with packet tests before runtime mutation. |
| Account / storefront / unlock terminal cluster | `0x0986`, `0x098C`, `0x098D`, and `0x098F` remain semantically unresolved or variant-blocked after the latest pass; `0x096A/096B/096C` are now named as account item cache add/list-append/remove packets, but their leading fields and producer intent remain unresolved; `0x097A` is named as an internal CREDD exchange order-cache refresh, but its row fields remain unresolved; the rest of `0969..0991` now has mapped consumer semantics. `0x026A` is now named `ServerCREDDExchangeInfoResults` for the empty compatibility snapshot, but non-empty price/order rows remain blocked. | Field-level reader shapes are mapped. Account consumer semantics are mapped for cooldown list, account item cache mutation, daily-login update, privilege restriction update, pending-item cache mutation, CREDD history/redeem result, CREDD order-cache refresh, and wallet update. Store consumer semantics are mapped for `0989` `StoreCatalogUpdated`, `098A` `StoreError`, `098E` `StorePurchaseHistoryReady`, `0990` `StoreCompleteOrderVirtualCurrencyPackageResult`, and `0991` `StorePurchaseVirtualCurrencyPackageResult`; retail `CodeEnumStoreError` and `CodeEnumPurchaseResultDisplayType` values are implemented. `098C/098D` both dispatch `StorePurchaseOfferResult` with the mapped display type but need opcode variant proof. `0986` has only an 8-byte reader registration so far, and the mapped store dispatcher excludes it. | Around account currency/entitlements/items, support ticket result, generic unlocks, store categories/offers, and CREDD info/history. | Trace producer/sniff evidence for the unresolved leading fields on `096A/096B/096C`, native emit sites and remaining client UI/event consumers for `0986/098C/098D/098F`, `097A` row field consumers, plus the non-empty `ServerCREDDExchangeInfoResults` row writers; do not rename unresolved fields or widen account/store/CREDD state mutation until packet names, state transitions, and result semantics are proven. |

## Suggested Execution Order

| Order | Slice | Why first |
| ---: | --- | --- |
| 1 | Account/storefront terminal cluster `0969..0991` | Dense unresolved server-output cluster with direct ties to account inventory, store offers, generic unlocks, cooldowns, and pending groups. |
| 2 | Housing neighbor/community cluster | Large feature gap with many adjacent unresolved opcodes and clear source surfaces. |
| 3 | Crafting/tradeskill success | Packets are already mostly mapped; missing work is stateful backend behavior. |
| 4 | Marketplace/auction/commodity | Backing service is missing, but request packet coverage is strong. |
| 5 | Group/matching/queue | Needed for multiplayer content and has a coherent unresolved server cluster. |
| 6 | Spell auxiliary `ServerSpellUInt32TripletList`, `ServerSpellUInt32TripletListVariant`, and `ServerSpellFourUInt32` plus proc/shield validation | High gameplay value, but must stay family-first. |
| 7 | Loot/group-roll/master-loot | Retail feel improvement and direct dependency for group content. |
| 8 | Content smoke blockers for NPE, Rider's Reef, and world `3404` | Turns protocol/runtime work into visible gameplay restoration. |

## Task Template

Use this block when starting any row above:

```markdown
### Target

- Matrix row:
- One-sentence question:
- Candidate opcode(s):
- Candidate client function(s):
- Current NexusForever surface:
- Evidence level before work:

### Decode Plan

- Search terms:
- Export files to inspect:
- Expected reader/writer shape:
- Unknowns to keep diagnostic-only:

### Implementation Gate

- Minimum evidence required:
- Files likely to change:
- Tests or smoke command:
- Done state: Mapped only / Implemented / Rejected
```
