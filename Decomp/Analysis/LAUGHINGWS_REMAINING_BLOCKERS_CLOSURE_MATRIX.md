# LaughingWS Remaining Blockers Closure Matrix

Updated: 2026-05-27

This matrix closes the remaining unchecked tasks from
`Decomp/Analysis/LAUGHINGWS_REMAINING_BLOCKERS_IMPLEMENTATION_PLAN.md` after
the row-level WorldDB review pass. Each item ends in one of the allowed states:
implemented with verification, mapped-only with an explicit blocker, or
rejected with the reason recorded. No broad LaughingWS SQL is promoted by this
matrix.

## Milestone 2 Smoke Gates

| Task | State | Closure |
| --- | --- | --- |
| LWS-036 | Mapped-only blocker with harness preset and dry-run guard | `Decomp/Analysis/LAUGHINGWS_CITY_CHECKLIST_SMOKE_GATE_REVIEW.md` records the missing smoke bundle requirements for Illium Ringo Hax, Thayd/Illium housing intro story panels, Auroria, Crimson Isle, Levian Bay, Everstar Grove, Wilderrun, and Northern Wilds. `Start-BlockerEvidenceHarness.ps1 -Lws036ChecklistSmoke` now creates a row-specific worksheet and negative-case checklist, but no WIP/GUESSED row is promoted until a completed client/server evidence bundle exists. `test_blocker_evidence_harness_presets.py` dry-runs the preset and verifies the manifest, worksheet, default world ids, helper files, and negative-case scaffold. |
| LWS-037 | Mapped-only blocker | `Decomp/Analysis/LAUGHINGWS_CITY_CHECKLIST_SMOKE_GATE_REVIEW.md` keeps WIP/GUESSED checklist/entity conversion blocked until LWS-036 row-specific smoke proof exists. |

## Milestone 3 New-Zone And Sandbox Gates

| Task | State | Closure |
| --- | --- | --- |
| LWS-040 | Mapped-only blocker with review, harness preset, analyzer guard, and dry-run guard | Dreadmoor remains blocked as `blocked_laughingws_new_zone_broad_rows`; `Decomp/Analysis/LAUGHINGWS_NEW_ZONE_BROAD_ROW_REVIEW.md` records `1,256` broad insert rows with destructive world-delete hazards and no current client/runtime proof. `Start-BlockerEvidenceHarness.ps1 -NewZoneAssetProof` now creates a row-level asset-proof worksheet and negative/safety checklist. `test_laughingws_worlddb_classifications.py` asserts the analyzer keeps this source blocked and not additive. `test_blocker_evidence_harness_presets.py` dry-runs the shared asset-proof preset and verifies the manifest, worksheet, default world ids, helper files, and negative-case scaffold. |
| LWS-041 | Mapped-only blocker with review, harness preset, analyzer guard, and dry-run guard | Halon Ring keeps only already-covered curated city/transport slices; `Decomp/Analysis/LAUGHINGWS_NEW_ZONE_BROAD_ROW_REVIEW.md` records `1,692` broad insert rows with destructive world-delete hazards and no safe broad runtime promotion. `-NewZoneAssetProof` provides the future row-level proof worksheet but does not promote broad residual rows. `test_laughingws_worlddb_classifications.py` asserts the broad source stays blocked and not additive. The dry-run guard verifies the shared asset-proof preset only. |
| LWS-042 | Mapped-only blocker with review, harness preset, analyzer guard, and dry-run guard | Murkmire remains blocked as `blocked_laughingws_new_zone_broad_rows`; `Decomp/Analysis/LAUGHINGWS_NEW_ZONE_BROAD_ROW_REVIEW.md` records `841` TODO-heavy broad insert rows with destructive world-delete hazards and no current script/client proof. `-NewZoneAssetProof` provides the future row-level proof worksheet. `test_laughingws_worlddb_classifications.py` asserts the analyzer keeps this source blocked and not additive. The dry-run guard verifies the shared asset-proof preset only. |
| LWS-043 | Rejected for runtime with checklist, harness preset, analyzer guard, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_SANDBOX_ZONE_ASSET_PROOF_CHECKLIST.md` lists all `23` sandbox/test/unknown/not-in-client files, `2,786` insert rows, destructive hazards, and required client-asset/runtime/placement/negative/safety proof. `-NewZoneAssetProof` creates the future sandbox investigation worksheet; runtime promotion remains rejected until a concrete row is explicitly proven. `test_laughingws_worlddb_classifications.py` asserts all `23` test-zone files remain sandbox-only and not additive. The dry-run guard verifies the shared asset-proof preset only. |
| LWS-044 | Rejected for build 16042 with analyzer guard | Historical Dominion/Exile arkship tutorial rows remain audit-only because build 16042 uses Rider's Reef/surface start routing. The analyzer classifies Dominion Arkship (`3` insert rows) and Exile Arkship (`40` insert rows) as `rejected_historical_arkship_tutorial_rows`. `test_laughingws_worlddb_classifications.py` asserts both files keep that rejection and are not additive. Reopen only for a historical/pre-16042 target. |
| LWS-045 | Rejected for current runtime with analyzer guard | Rider's Reef `New Tutorial.sql` replacement rows remain audit-only because the official `New Player Experience.sql` import/current cleanup is authoritative. The analyzer classifies the file (`38` insert rows) as `skip_replaces_required_riders_reef_import`. `test_laughingws_worlddb_classifications.py` asserts the replacement file keeps that skip and is not additive. |

## Milestone 4 WorldDB Special Gates

| Task | State | Closure |
| --- | --- | --- |
| LWS-050 | Mapped-only rejection | `Decomp/Analysis/LAUGHINGWS_DUNGEON_CHASE_SMC_PLACEHOLDER_REVIEW.md` records why Dungeon Chase hidden SMC placeholder item `86919` remains skipped: source type `0` exceeds the current `store_offer_item_data.itemId` model and the 15-bit type-`0` `ServerStoreOffers` transport. |
| LWS-051 | Mapped-only blocker with harness preset and dry-run guard | `Decomp/Analysis/LAUGHINGWS_DUST_STALKER_Q4516_REVIEW.md` records the quest/objective/creature proof for Boss Xagg and bridge-control placement, rejects source-only timing behavior from SQL alone, and defines the required harness smoke bundle before implementing self-destruct or exit-panel behavior. `Start-BlockerEvidenceHarness.ps1 -DustStalkerQ4516Smoke` now creates the row-specific target worksheet, default world/objective filters, and negative-case checklist, but no runtime sequence is implemented until a completed client/server bundle proves timer and exit semantics. `test_blocker_evidence_harness_presets.py` dry-runs the preset and verifies the manifest, worksheet, default ids, helper files, and negative-case scaffold. |
| LWS-052 | Mapped-only blocker with harness preset and dry-run guard | `Decomp/Analysis/LAUGHINGWS_ARCTERRA_PALAVER_SOURCE_ONLY_REVIEW.md` records the three preserved source-only rows, their evidence boundary, and the required Arcterra/Palaver harness smoke before removing WIP/GUESSED or implementing portal/interaction behavior. `Start-BlockerEvidenceHarness.ps1 -ArcterraPalaverSourceSmoke` now creates the row-specific target worksheet, default world/objective filters, and negative-case checklist, but the rows remain WIP/GUESSED until a completed client/server bundle proves placement, portal target, quest state, and interaction semantics. The dry-run guard verifies the manifest, worksheet, default ids, helper files, and negative-case scaffold. |
| LWS-053 | Mapped-only blocker with harness preset and dry-run guard | `Decomp/Analysis/LAUGHINGWS_SKYPLOT_HOUSING_REVIEW.md` records the `411` WIP/GUESSED Skyplot rows, source destructive-delete/`@GUID`/`OutfitInfo` hazards, and the required housing smoke bundle before promoting vendor placement, return-pad interaction, catalogue completeness, or active Skyplot lifecycle behavior. `Start-BlockerEvidenceHarness.ps1 -SkyplotHousingSmoke` now creates the row-specific target worksheet, default world filter, and negative-case checklist, but the overlay remains WIP/GUESSED until a completed client/server bundle proves placement, catalog, return-pad, and lifecycle behavior. The dry-run guard verifies the manifest, worksheet, default world id, helper files, and negative-case scaffold. |
| LWS-054 | Mapped-only blocker with harness preset and dry-run guard | `Decomp/Analysis/LAUGHINGWS_LIVE_EVENT_SMOKE_REVIEW.md` records the `440` preserved WIP/GUESSED live-event rows, skipped source hazards, and per-event harness bundles required for Battle Chase, Dungeon Chase, Shades Eve, Space Chase, Starfall, Sim Chase 1, and zPrix before implementing spawn timing, vendor timing, lifecycle activation, cleanup semantics, or event-state tests. `Start-BlockerEvidenceHarness.ps1 -LiveEventSmoke` now creates the live-event target worksheet and negative-case checklist, but lifecycle behavior remains blocked until completed client/server bundles prove concrete event timing and cleanup semantics. The dry-run guard verifies the manifest, worksheet, helper files, and negative-case scaffold. |
| LWS-050 | Verified rejection | `Decomp/Analysis/LAUGHINGWS_DUNGEON_CHASE_SMC_PLACEHOLDER_REVIEW.md` rejects the Dungeon Chase type `0` SMC account item `86919` because the current world `store_offer_item_data.itemId` model and type-`0` `ServerStoreOffers` transport cannot represent it. `LaughingWsStoreCatalogSeedTests.StoreCatalogSeed_DoesNotEmitDungeonChaseUnsupportedType0AccountItem` verifies the seed records the skip note and emits no unsafe type `0` row. |
| LWS-055 | Mapped-only blocker with runtime-boundary tests, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_QUEST_VIRTUAL_LOOT_REVIEW.md` records the `7,650` preserved quest-loot rows, current `QuestObjectiveActive`/`VirtualCollect` runtime boundary, sample objective/virtual-item rows, and the representative capture required before changing trigger cadence, probability, corpse/source selection, or objective integration behavior. Focused loot tests now pin condition `8` as `IsActiveObjectiveId` gating and virtual-item delivery as `QuestObjectiveType.VirtualCollect` plus mapped loot grant, without changing cadence/probability/source selection. `Start-BlockerEvidenceHarness.ps1 -QuestVirtualLootSmoke` now creates the representative target worksheet, default objective filters, and negative-case checklist, but quest-loot behavior remains at the safe preservation boundary until completed client/server bundles prove cadence, probability, source selection, and objective integration. The dry-run guard verifies the manifest, worksheet, default objective ids, helper files, and negative-case scaffold. |

## Milestone 5 Path/Fortune Gates

| Task | State | Closure |
| --- | --- | --- |
| LWS-060 | Partially implemented with blocker | `character_path_mission` plus EF migration `20260527191317_CharacterPathMissionState` now persist active/completed path mission state (`PathEpisode`, `PathMission`, state, completion, progress payloads, and XP). `PathManager` restores completed state, replays unfinished episode packets on login only for table-backed missions that still match the active path, saved `PathEpisode`, faction, and prerequisite context, and no longer lets different-path, missing-episode, or mismatched-episode-path persisted missions replay or suppress current-path zone activation for the same episode id. Focused path tests passed `49/49` after the replay/prerequisite slice; active path object-id state, per-character reward history, broader zone/reload negatives, and exact replay ordering remain blocked pending stronger proof. |
| LWS-061 | Partially implemented with blocker | `Decomp/Analysis/LAUGHINGWS_PATH_FORTUNE_BLOCKER_REVIEW.md` records the decompile-backed mission XP fallback: `Lua_PathMission_GetRewardXp` reads `GameFormula` `0x017a` (`378`) `Dataint0` with client fallback `50`. Focused reward/path tests now pass `74/74`, including mission reward predicate coverage for matching `ObjectId`, supported payloads, flagged rows, and the zero-count item fallback. Exact per-mission reward precision, presentation, overflow, reward history, and retail flagged-row semantics remain blocked pending stronger `PathReward`/packet evidence. |
| LWS-062 | Partially implemented with blocker | `Decomp/Analysis/LAUGHINGWS_PATH_FORTUNE_BLOCKER_REVIEW.md` records the current table-backed current-zone activation filters plus regression coverage for completed-mission no-replay, persisted-active no-duplicate zone activation, and persisted replay rejection for different-path, wrong-faction, unmet simple path-prerequisite, missing-episode, and mismatched-episode-path missions. Client-smoked filtering semantics, exact replay/completion ordering, and broader path/zone reload transitions remain blocked. |
| LWS-063 | Partially implemented with blocker | Settler_Hub mission progress now persists through `character_path_mission`; `CompleteMissionBySettlerImprovementGroupId_WithPersistedPartialHubProgress_CompletesAfterReload` verifies a partially progressed hub mission can reload, complete after the next accepted build-tier request, and save completion/progress updates. Focused rejection tests now pin zero/missing improvement groups, missing hubs, different hub ids, non-Settler active paths, out-of-range build ids, unmapped tiers, zero improvement ids, and unwritable improvement ids without unsafe mission updates or malformed build status/result packets. Built-group identity, resources, avenue totals, non-success result values, and failure ordering remain blocked pending smoke/client proof. |
| LWS-064 | Partially implemented with blocker | `Mission.ObjectiveCompletionFlags`/`StateFlags` are renamed to `ProgressCount`/`ProgressData` after decompile proof from `Lua_PathMission_GetNumCompleted` (`14067c370`) and `PathMissionRuntime_GetCurrentProgress` (`14056d0d0`): Soldier_Assassinate (`0x04`) and Settler_Hub (`0x13`) read the first progress payload, and focused path/settler/explorer tests passed `36/36`. The 2026-05-27 SoldierEvent pass labels the `Game.SoldierEvent` Lua accessors (`140682d60` through `140683dd0`) and maps wave count, waves released, health, and unit-kind reads from the client runtime object. The Settler packet pass labels `ServerPathSettlerBuildStatusRow_ReadPayload` (`14007a730`), `ServerPathSettlerBuildStatus_ReadPayload` (`14007a7b0`), `ServerPathSettlerBuildStatusList_ReadPayload` (`14007a800`), `ServerPathSettlerBuildResult_ReadPayload` (`14007aa70`), `PathSettler_HandleBuildStatusList` (`140618400`), and `PathSettler_HandleBuildStatus` (`1406185a0`); packet tests now pin hub id, improvement group id, tier, remaining time, bundle count, and result improvement id. `PathSettlerBuildHandlerTests` now pin the current no-emit guard for unmapped or unwritable Settler build acknowledgements. The 2026-05-28 Soldier packet-shape follow-up adds source comments and `PacketPlaceholderNamingTests` coverage for holdout status, next-wave, end, and death serialization, with packet tests passing `51/51` and the network-world build passing. Server-side holdout status/wave producer timing, wave spawning, unit ownership, death/failure semantics, alternate `ProgressData`, Settler non-success result values/failure ordering, resource costs, and durable built-group/avenue behavior remain blocked. |
| LWS-065 | Implemented with verification | `Decomp/Analysis/LAUGHINGWS_PATH_FORTUNE_BLOCKER_REVIEW.md` records the focused path-edge test slice; `PathManagerTests`, `PathRewardGrantTests`, `ClientPathExplorerProgressReportHandlerTests`, and `GalacticArchiveUnlockRuleTests` passed `56/56`. |
| LWS-066 | Mapped-only blocker with harness preset, dry-run guard, and runtime-boundary tests | `Decomp/Analysis/LAUGHINGWS_PATH_FORTUNE_BLOCKER_REVIEW.md` keeps Fortune weights complete-but-approximate pending retail `ServerFortuneRewards` or storefront catalog dumps with per-item/rotation evidence. `Start-BlockerEvidenceHarness.ps1 -FortuneRewardsSmoke` now creates the targeted evidence worksheet for probabilities, active rotation/storefront context, card deal/flip/payout, reload persistence, and negative cases; no weight changes are promoted without a completed evidence bundle. `test_blocker_evidence_harness_presets.py` dry-runs the preset and verifies the manifest, worksheet, helper files, and negative-case scaffold. `FortuneRewardPoolTests` now exercise the real pool with synthetic account-item/item rows so mapped item2 probabilities stay display-only, Fortune Coin/non-item account rewards are not advertised in `ServerFortuneRewards`, and current picker-only account rewards remain guarded without claiming retail rotation parity. |

## Milestone 6 Shared Instance/Public-Event Gates

| Task | State | Closure |
| --- | --- | --- |
| LWS-070 | Implemented with verification | `Start-BlockerEvidenceHarness.ps1` now creates timestamped evidence bundles with `manifest.json`, command/server/client/negative-case templates, screenshots/video/log folders, and log-tail/collection helpers. `-CreateBundleOnly` verifies bundle generation without touching running services. Content smoke gates remain open until real bundles are captured. |
| LWS-071 | Mapped-only blocker with packet-shape/runtime-hardening progress and dry-run guard | `Decomp/Analysis/LAUGHINGWS_SHARED_PUBLIC_EVENT_BLOCKER_REVIEW.md` keeps public-event vote state blocked pending packet/UI proof for vote open, choice, tally, timeout, and result behavior. Decompile labels now map `ServerPublicEventVoteAux_ReadPayload` (`14007c0c0`, opcode `0x06F7`) as a 15-bit value plus flag and `ServerPublicEventAux_ReadPayload` (`14007b930`, opcode `0x0139`) as a uint32 value plus counted uint32 list; packet tests pin those diagnostic shapes and the client vote/server initiate/detailed-initiate/tally/end shapes. `PublicEventVote.Choice` now ignores finalised, non-participant, duplicate, and invalid responses, the client vote handler forwards the full mapped event/vote/team/choice envelope, and runtime validation ignores stale vote ids or cross-team replies before mutating vote state. Focused vote lifecycle tests cover initiate, tally/end, timeout default choice, handler forwarding, vote-id mismatch rejection, and negative responses. `Start-BlockerEvidenceHarness.ps1 -PublicEventVoteScoreboardSmoke` now creates the targeted vote evidence worksheet, without claiming retail UI timing, mismatch feedback, or result-order parity. `test_blocker_evidence_harness_presets.py` dry-runs the preset and verifies the manifest/worksheet. |
| LWS-072 | Mapped-only blocker with runtime snapshot progress and dry-run guard | `Decomp/Analysis/LAUGHINGWS_SHARED_PUBLIC_EVENT_BLOCKER_REVIEW.md` keeps cross-event reward behavior blocked pending score-field precision, exact live subscription cadence, reward tier semantics, result ordering, and delivery proof. Decompile labels map scoreboard request `0x06FA`, stats update `0x0130`, end `0x00D6`, personal stat `0x013B`, and shared team/participant row readers; runtime tests now pin subscribe-request snapshot routing, unsubscribe no-op before map/event lookup, non-member suppression, and combined team/participant stat rows without adding reward behavior. `Start-BlockerEvidenceHarness.ps1 -PublicEventVoteScoreboardSmoke` now creates the targeted scoreboard/reward evidence worksheet. `test_blocker_evidence_harness_presets.py` dry-runs the preset and verifies the manifest/worksheet. |
| LWS-073 | Mapped-only blocker with packet-shape/runtime-hardening progress and dry-run guard | `Decomp/Analysis/LAUGHINGWS_SHARED_PUBLIC_EVENT_BLOCKER_REVIEW.md` keeps objective/phase/completion notification parity and quest-share behavior blocked pending mode/text/audience/order proof. Decompile labels now map objective notification mode `0x0133`, objective status update `0x0134`, objective update `0x0132`, and objective start `0x06F9`; packet tests pin the 15-bit objective id, status row, notification mode, location list, and map-region list shapes without widening text, audience, ordering, or quest-share behavior. The existing `0x0132` objective update producer now suppresses duplicate busy-state packets when the state is unchanged, with focused objective tests covering that no-duplicate boundary. `Start-BlockerEvidenceHarness.ps1 -PublicEventObjectiveNotificationSmoke` now creates the targeted LWS-073 worksheet, and `test_blocker_evidence_harness_presets.py` dry-runs the preset and verifies its manifest/worksheet. |
| LWS-074 | Mapped-only blocker with runtime-hardening progress | `Decomp/Analysis/LAUGHINGWS_SHARED_PUBLIC_EVENT_BLOCKER_REVIEW.md` keeps generic trigger/door behavior blocked pending trigger geometry, door state, cleanup, and respawn/despawn proof. The existing mapped `ParticipantsInTriggerVolume` and `Turnstile` producers now ignore zero object ids and non-player entities, and focused trigger tests cover volume enter/leave deltas, duplicate in-range suppression, world-location horizontal radius plus vertical clamp behavior, turnstile entry, and no turnstile leave/decrement emit without widening branch door semantics. |
| LWS-075 | Mapped-only blocker with packet-shape progress | `Decomp/Analysis/LAUGHINGWS_SHARED_PUBLIC_EVENT_BLOCKER_REVIEW.md` keeps completion-only cinematic/communicator placeholders WIP until actor/camera/text/timing payloads and send conditions are mapped. Communicator/story-panel packet models now carry export-anchor comments and focused tests for `ServerCommunicatorMessage`, `ServerStoryTextCommunicator`, `ServerStoryPanelCustomShow`, and shared `StoryMessage` actor rows, without widening runtime send behavior. |
| LWS-076 | Rejected for now with static guard | `Decomp/Analysis/LAUGHINGWS_SHARED_PUBLIC_EVENT_BLOCKER_REVIEW.md` rejects unused branch-only public-event/communicator/catalog imports until an active runtime consumer needs the rows. `BranchCatalogCleanupTests` now guards the decision by asserting the rejected Datascape Hydroflux/Mnemesis all-in-one script names and known empty or door/platform/marker-only branch catalog files remain absent from runtime source. |

## Milestone 7 PvP And Adventure Gates

| Task | State | Closure |
| --- | --- | --- |
| LWS-080 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_PVP_ADVENTURE_SMOKE_REVIEW.md` records the WIP Cryo-Plex scaffold and keeps queue/match smoke, score/capture/round flow, rewards, and PvP stats blocked. `Start-BlockerEvidenceHarness.ps1 -PvpAdventureSmoke` now creates the shared PvP/adventure smoke worksheet with Cryo-Plex world/event ids and negative cases. `test_blocker_evidence_harness_presets.py` dry-runs the shared preset and verifies the manifest, worksheet, default world/public-event ids, helper files, and negative-case scaffold. |
| LWS-081 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_PVP_ADVENTURE_SMOKE_REVIEW.md` records the Daggerstone Pass binding and keeps queue/start/end, scoring, rewards, capture objectives, and stat smoke blocked. `-PvpAdventureSmoke` includes Daggerstone Pass world/event ids and queue/objective/reward negative cases; the dry-run guard verifies the shared preset structure only. |
| LWS-082 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_PVP_ADVENTURE_SMOKE_REVIEW.md` records the Halls of the Bloodsworn binding and keeps queue/start/end, scoring, rewards, objective flow, and stat smoke blocked. `-PvpAdventureSmoke` includes Halls of the Bloodsworn world/event ids and abandon/early-finish negative cases; the dry-run guard verifies the shared preset structure only. |
| LWS-083 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_PVP_ADVENTURE_SMOKE_REVIEW.md` records the Walatiki Temple binding and keeps mask capture/return/scoring, round end, rewards, and stat proof blocked. `-PvpAdventureSmoke` includes Walatiki world/event ids and wrong-team/wrong-phase negative cases; the dry-run guard verifies the shared preset structure only. |
| LWS-084 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_PVP_ADVENTURE_SMOKE_REVIEW.md` records the War of the Wilds totem scaffold and keeps faction start events `170`/`171`, end delay, chat timing, rewards, and smoke blocked. `-PvpAdventureSmoke` includes War of the Wilds public events `158`, `170`, and `171` for faction-start proof; the dry-run guard verifies the shared preset structure only. |
| LWS-085 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_PVP_ADVENTURE_SMOKE_REVIEW.md` records Rage Logic's WIP `ChooseAVehicle` phase and keeps vehicle choice, objective routing, rewards, and encounter behavior blocked. `-PvpAdventureSmoke` includes Rage Logic world/event ids and vehicle-choice proof requirements; the dry-run guard verifies the shared preset structure only. |

## Milestone 8 Expedition Gates

| Task | State | Closure |
| --- | --- | --- |
| LWS-090 | Rejected until proof with harness preset and dry-run guard | `Decomp/Analysis/LAUGHINGWS_EXPEDITION_SMOKE_REVIEW.md` keeps Outpost M-13 shuttle trigger id `5` rejected because it is zero-vector and lacks world-location proof. `Start-BlockerEvidenceHarness.ps1 -ExpeditionSmoke` now creates the shared expedition worksheet with the Outpost world/event id and zero-vector trigger negative case. `test_blocker_evidence_harness_presets.py` dry-runs the shared preset and verifies the manifest, worksheet, default world/public-event ids, helper files, and negative-case scaffold. |
| LWS-091 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_EXPEDITION_SMOKE_REVIEW.md` records the tested Space Madness scaffold and keeps door ids, direct teleport, trigger timing, real cinematic payload, combat/spawn behavior, and full smoke blocked. `-ExpeditionSmoke` includes Space Madness world/event ids and door/teleport/trigger proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-092 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_EXPEDITION_SMOKE_REVIEW.md` records the tested Fragment Zero scaffold and keeps trigger timing, real cinematic payload, communicator timing, door behavior, and entity cleanup blocked. `-ExpeditionSmoke` includes Fragment Zero world/event ids and cinematic/door/cleanup proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-093 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_EXPEDITION_SMOKE_REVIEW.md` records the tested Gauntlet scaffold and keeps cinematics, announcer/communicator timing, arena doors, exact trigger timing, and full smoke blocked. `-ExpeditionSmoke` includes Gauntlet world/event ids and arena-door/announcer proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-094 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_EXPEDITION_SMOKE_REVIEW.md` records the tested Infestation scaffold and keeps cargo-ship placement/range, door/open-vent choreography, real cinematic payload, medical-bay attack timing, parasite activation, and full smoke blocked. `-ExpeditionSmoke` includes Infestation world/event ids and cargo-ship/medical-bay proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-095 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_EXPEDITION_SMOKE_REVIEW.md` records the tested Evil from the Ether WIP route pieces and keeps exact triggers, medbay transition, cleanup, crew-log order, visuals, boss mechanics, cinematics, rewards, full smoke, and residual no-hook placements blocked. `-ExpeditionSmoke` includes Evil from the Ether world/event ids and residual no-hook placement review requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-096 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_EXPEDITION_SMOKE_REVIEW.md` records the Deep Space scaffold and keeps follow-up routing, doors, real cinematic payload, encounter order, rewards, and full smoke blocked. `-ExpeditionSmoke` includes Deep Space world/event ids and routing/reward proof requirements; the dry-run guard verifies the shared preset structure only. |

## Milestone 9 Dungeon Gates

| Task | State | Closure |
| --- | --- | --- |
| LWS-100 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_DUNGEON_SMOKE_REVIEW.md` records the tested Coldblood Citadel scaffold and keeps route weights, trigger placement, doors, Hailstone cadence, and full route smoke blocked. `Start-BlockerEvidenceHarness.ps1 -DungeonSmoke` now includes Coldblood world/event ids and route/trigger/door proof requirements. `test_blocker_evidence_harness_presets.py` dry-runs the shared preset and verifies the manifest, worksheet, default world/public-event ids, helper files, and negative-case scaffold. |
| LWS-101 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_DUNGEON_SMOKE_REVIEW.md` records the tested Protogames Academy scaffold and keeps boss mechanics, trigger/communicator timing, cleanup, and choreography blocked. `-DungeonSmoke` includes Protogames Academy world/event ids and boss/trigger/launcher cleanup proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-102 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_DUNGEON_SMOKE_REVIEW.md` records the tested Ruins of Kel Voreth scaffold and keeps boss mechanics, triggers, doors, optional routes, communicator targeting, real cinematic payload, and trigger split proof blocked. `-DungeonSmoke` includes Ruins world/event ids and duplicate-owner trigger split proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-103 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_DUNGEON_SMOKE_REVIEW.md` records the tested Stormtalon's Lair scaffold and keeps boss combat, trigger placement/count, real cinematic payload, optional weights/objectives, and boss spawn/version selection blocked. `-DungeonSmoke` includes Stormtalon's Lair world/event ids and reborn cinematic/boss proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-104 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_DUNGEON_SMOKE_REVIEW.md` records the tested Skullcano scaffold and keeps boss mechanics, route weights, Chief Kaskalak choreography, doors/platforms, communicator timing, optional objectives, and full smoke blocked. `-DungeonSmoke` includes Skullcano world/event ids and cave/chasm/door/platform proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-105 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_DUNGEON_SMOKE_REVIEW.md` records the tested Sanctuary scaffold and keeps miniboss mechanics, route weights/objectives, trigger placement, doors, communicator timing, and full smoke blocked. `-DungeonSmoke` includes Sanctuary world/event ids and Temple/Moldwood route proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-106 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_DUNGEON_SMOKE_REVIEW.md` records Ultimate Protogames dungeon's coarse `RandomEvent1` stop and keeps room randomization, objective routing, rewards, boss mechanics, and full smoke blocked. `-DungeonSmoke` includes Ultimate Protogames dungeon world/event ids and post-`RandomEvent1` proof requirements; the dry-run guard verifies the shared preset structure only. |

## Milestone 10 Raid And Event-Instance Gates

| Task | State | Closure |
| --- | --- | --- |
| LWS-110 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_RAID_EVENT_SMOKE_REVIEW.md` keeps Initialization Core Y-83 trigger placement, doors, communicator/cinematic timing, target sets, real cinematic payload, and raid smoke blocked. `Start-BlockerEvidenceHarness.ps1 -RaidEventSmoke` includes Initialization Core world/event ids and trigger/target-set/open-door proof requirements. `test_blocker_evidence_harness_presets.py` dry-runs the shared preset and verifies the manifest, worksheet, default world/public-event ids, helper files, and negative-case scaffold. |
| LWS-111 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_RAID_EVENT_SMOKE_REVIEW.md` keeps Red Moon Terror Ish'amel/engineering timing, Laveka choreography, awakening/challenge mechanics, doors/elevators, and raid smoke blocked. `-RaidEventSmoke` includes Red Moon Terror world/event ids and Laveka/elevator/challenge proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-112 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_RAID_EVENT_SMOKE_REVIEW.md` keeps Genetic Archives boss mechanics, weekly/random selection, communicator/cinematic timing, doors/elevators, and raid smoke blocked. `-RaidEventSmoke` includes Genetic Archives world/event ids and weekly/random/door/elevator proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-113 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_RAID_EVENT_SMOKE_REVIEW.md` keeps Datascape Hydroflux/Mnemesis mechanics, communicator/cinematic timing, choreography, doors/triggers, wing order, challenge mechanics, and raid smoke blocked. `-RaidEventSmoke` includes Datascape world/event ids and wing-order/Hydroflux/Mnemesis proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-114 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_RAID_EVENT_SMOKE_REVIEW.md` keeps Ultimate Protogames raid Downsizer challenge semantics, boss mechanics, rewards, and raid smoke blocked. `-RaidEventSmoke` includes Ultimate Protogames raid world/event ids and Downsizer/reward proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-115 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_RAID_EVENT_SMOKE_REVIEW.md` keeps Shade's Eve gather cleanup, town gate, communicator/cinematic timing, real cinematic payload, vote follow-up, seed interaction smoke, and event smoke blocked. `-RaidEventSmoke` includes Shade's Eve world/event ids and town-gate/vote/seed proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-116 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_RAID_EVENT_SMOKE_REVIEW.md` keeps Protostar SuperMall store/room routing, encounter logic, real cinematic payload, rewards, and full smoke blocked. `-RaidEventSmoke` includes SuperMall world/event ids and store/room/routing proof requirements; the dry-run guard verifies the shared preset structure only. |
| LWS-117 | Mapped-only blocker with verified scaffold, harness preset, and dry-run guard | `Decomp/Analysis/LAUGHINGWS_RAID_EVENT_SMOKE_REVIEW.md` keeps OMNICore real cinematic payload, event routing, rewards, and encounter behavior blocked. `-RaidEventSmoke` includes OMNICore world/event ids and cinematic/routing/reward proof requirements; the dry-run guard verifies the shared preset structure only. |

## Milestone 11 Verification Closure

| Task | State | Closure |
| --- | --- | --- |
| LWS-120 | Implemented with verification | `analyze_laughingws_worlddb.py` now emits the row reconciliation CSV required by the row-review queue builder. A tiny analyzer smoke fixture proved one `branch_only` and one `official_only` row while suppressing the shared row. The analyzer and queue builder were rerun against `artifacts/external/LaughingWS.WorldDatabase.New-Zones-and-more` and `../NexusForever.WorldDatabase`, producing `957,167` reconciliation rows and `7,749` review rows in the scratch audit. All nine regenerated promoted LaughingWS seed SQL files matched the tracked seeds by SHA-256, and Python syntax verification passed for the analyzer and queue builder. |
| LWS-121 | Implemented with verification | No new runtime instance/public-event script behavior is introduced by this tracker pass. Existing focused tests remain the verification surface for prior script work: PvP/adventure `29/29`, expedition `78/78`, dungeon `149/149`, and raid/event-instance `169/169`. |
| LWS-122 | Implemented with verification | This tracker pass changes trackers/docs only and introduces no C# behavior, so no owning project build is required for this pass; prior owning gates are recorded in the focused review docs and audit tables. |
| LWS-123 | Implemented with verification | No cross-service packet, path, storefront, or persistence code changes are introduced by this tracker pass, so full solution build is not required for this pass. |
| LWS-124 | Implemented with verification | The implementation plan, closure matrix, three LaughingWS audit docs, `CURRENT_STATUS.md`, and `MISSING_FEATURE_MATRIX.md` carry a recorded state for each item or point to the focused review doc that does. |
| LWS-125 | Implemented with verification | Every task in the plan has one recorded tracker state: implemented with verification, mapped-only with the next evidence source named, or rejected with a recorded reason. |
