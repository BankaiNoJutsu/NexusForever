# LaughingWS Questing-and-more Audit

Updated: 2026-05-27

Branch: `LaughingWS/NexusForever` `Questing-and-more`

- Merge base: `f8e70e7fda9aefbd8fc91c229fa76abc5dc68bd0`
- Audited head: `863b6f2d9d5d773a8cf0327ea67e9d3336c22782`
- Local tracking ref: `laughingws/Questing-and-more`

This note tracks how the branch is being used. Do not blindly copy the branch:
it contains an old whole-repo/framework snapshot plus later gameplay/content
changes. Current 16042 client-reader evidence, tracked table data, and focused
tests stay authoritative.

Remaining smoke/proof-only blockers from the implementation plan are tracked
task-by-task in
`Decomp/Analysis/LAUGHINGWS_REMAINING_BLOCKERS_CLOSURE_MATRIX.md`: unproven
questing, path, Settler/Soldier, and Fortune behavior stays mapped-only or
explicitly rejected until the named evidence is captured.
The closure pass now covers the remaining questing-side plan rows without
turning mapped-only gates into retail-complete behavior: city/checklist rows,
path/Fortune gates, shared public-event assumptions, and verification-discipline
rows are recorded with an explicit implemented, mapped-only, rejected, or
blocked end state. The final verification reran Python syntax checks for the
LaughingWS tooling, restored analyzer emission of the row reconciliation CSV,
and reran the analyzer/row-review queue path against the local external
LaughingWS worlddb snapshot.

## Ported Or Harvested

| Slice | Status |
| --- | --- |
| Housing portal, housing intro, active-prop housing doors | Ported into `Source/NexusForever.Script.Main/Housing` and branch-script tests; F-004 remains partial for broader housing semantics. |
| City/world transporters, Exo-Lab 71, Exo-Lab 22, Thayd to Defile, coordinate pads | Ported into `WorldLocationTeleporterEntityScript` with table-backed `WorldLocation2` guards and focused transporter tests. |
| Northern Wilds Dominion gate, starter-zone map objective hooks, guarded intro cinematics | Ported into current map/entity scripts and covered by focused quest/script tests. |
| Learning to Shop smart-shopper helper | Ported as `Q10510LearningToShopQuestScript`; focused tests now pin the branch item/title objective credit and keep exact retail refresh timing marked WIP-guessed in code. |
| Crimson Isle/Northern Wilds quest helper scripts, Bramble Trap, Marauder Mine, Engineer Pulse Blast | Useful behavior ported into current script/spell patterns with tests, including Q5573/Q5604 cinematic-complete objective coverage, Q5584 trapped-assistant, Q5594 ship-control interaction coverage, Q8855 Dominion soldier proximity credit/despawn coverage, Q3486 Loftite Crystal reward/despawn coverage, and Q3487/Q3667 interaction/achievement gate coverage; generic branch spell callback plumbing remains intentionally unported. |
| Settler build acknowledgement and hub progress surface | Ported as table-backed `ServerPathSettlerBuildStatus` and `ServerPathSettlerBuildResult` emits plus active Settler_Hub progress from accepted build-tier requests using `PathSettlerHub.MissionCount`; decompile proof maps Settler_Hub current progress to the first mission payload now named `ProgressCount`. Durable hub/resource state, failure/result precision, and alternate progress payload semantics remain blocked. |
| Explorer progress packet surface | Ported conservatively as active-only Explorer Vista mission completion with matching `PathExplorerNode` validation, WIP-guessed active-only Explorer `ExploreZone` completion from current `MapZone.Id`, and active-only Explorer type-`0x12` power-map completion with matching `PathExplorerPowerMap` validation. |
| Current-zone path episode activation | Ported as WIP-guessed table-backed behavior on player zone updates: current world/root zone/path selects a `PathEpisode`, mission rows are faction- and optional prerequisite-filtered, the current episode/mission activation packets are emitted, object-id completion is guarded to active-path rows, known active-path missions without stronger configured XP use client-mapped `GameFormula` `0x017a` (`378`) `Dataint0` with fallback `50`, and already-active Explorer `ExploreZone` missions can complete from the resolved current map zone. Durable path persistence, exact per-mission reward precision, and exact unlock sequencing remain blocked. |
| Instance/adventure/expedition/dungeon/raid event scaffolds | Conservative branch phase/objective chains and map bindings ported where safe; guessed triggers, doors, cinematics, and encounter choreography remain blocked pending proof. |
| LaughingWS worlddb data | Limited to reviewed runtime-owned overlays generated by DataMapping scripts for map entrances, city content, store catalog, and quest loot. |

## Superseded Or Rejected

| Branch slice | Decision |
| --- | --- |
| Initial whole-repo/bootstrap snapshot (`5589ccc193` plus early metadata/obsolete commits) | Superseded by the current repository architecture and `net10.0` projects; not treated as a gameplay delta. |
| Command framework and GM categories (`036cc894b7` through `f46326f668`) | Superseded by the current command system and richer current command categories; branch RewardTrack command is tied to the rejected reward-track surface. |
| Design/wwwroot/auth/sts/client-connector/database scaffold commits | Superseded by current infrastructure and setup docs; no retail gameplay behavior to port. |
| Account/storefront/account inventory commits (`aa9c293a92`, `bcc40872b3`, `5a58a6884b`, `9d3333af48`, `31f87ae871`, `e0133d9492`) | Superseded by current F-006 implementation. Branch `0x097D` transaction-update naming is rejected in favor of current pending-item-group delete evidence; branch `0x03DD` subscription payload remains unmapped/unemitted. |
| Network bootstrap packet/model drops | Superseded by current `NexusForever.Network.World` packet models, opcode coverage, and packet-shape tests; individual useful packets were audited through their feature rows. |
| Entity network command and movement/spline commits (`82e54cea83`, `103d3a0509`) | Superseded by current entity command models, movement command groups, spline modes, generators, and movement/protocol tests. |
| RewardTrack manager and packets | Rejected because branch labels `0x07CD`/`0x07D3` as reward-track traffic; current evidence maps them to reward-rotation content-context packets. |
| Fortune/Gacha commits (`819197dc0f`, `27486d5e8d`) | Rejected/superseded by the current mapped Fortune packet family and account-scoped Fortune subsystem; branch hardcoded item arrays are not retail weight/rotation evidence. |
| Broad path persistence/content scaffold (`92246e8832`, `3777d8e2b6`, `94fc1fd576`) | Mostly blocked or superseded by current path architecture. Useful Settler acknowledgement/build-count progress, Explorer Vista/ExploreZone/power-map, active Soldier_Assassinate kill progress, current-zone PathEpisode activation, active-path object-id completion, known-mission PathMission and PathMissionType achievement credit, client-mapped `GameFormula` `0x017a` XP fallback, and unflagged mission reward surfaces with WIP `PathReward.Count` item quantities were ported; durable episode/mission persistence needs stronger evidence. |
| Duel packets, CC packet pair, falling-damage packet | Superseded or blocked by current PvP/CC/movement evidence. Do not port branch versions as authoritative behavior. |
| SpellManager/prerequisite/quest-objective guesses from `92246e8832` | Rejected: current prerequisite ids and objective updaters have stronger 16042 evidence than branch `Unknown28`/`VirtualCollect` target-group guesses. |
| QuestActivate command and RBAC migration | Intentionally unported as GM tooling/auth churn; current `quest objective` command and runtime objective updaters provide the useful debug surface. |
| Remaining LWS closure rows | Closed in the plan only as tracker discipline. They do not promote new quest/path/Fortune behavior; retail proof still requires the named harness bundles, client smoke, or storefront/packet captures. |

## Still Blocked

- Durable path episode/mission persistence, exact path reward precision beyond
  the `GameFormula` `0x017a` XP fallback, exact path unlock sequencing,
  Settler hub built-group/resource/avenue state, exact Soldier/Settler progress
  packet field names, and generic path edge semantics.
- Exact trigger, door, cinematic, communicator, optional-objective,
  route/randomization, and manual smoke evidence for branch-derived instance and
  public-event scaffolds.
- Retail Fortune weights/rotations without a `ServerFortuneRewards` capture or
  storefront-server catalog dump.
- Remaining branch worlddb rows beyond reviewed overlays; broad SQL replacement
  remains audit-only until row-level review proves it safe.
